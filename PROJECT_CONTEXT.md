# מערכת ניהול דיירים וגבייה — Project Context
- docs/archive/BASE44_REFERENCE.md הוא חומר עזר בלבד. אסור לקרוא אותו 
  באופן פרואקטיבי. קרא אותו רק אם:
  א. המשתמש מבקש במפורש
  ב. אתה צריך להבין לוגיקה עסקית מסוימת (סעיף 6 או 11)
  
  במקרים אלו — ציין שאתה מתייחס ל-reference, ושאל אם זה 
  רלוונטי לגרסה החדשה לפני שתיישם.
## על הפרויקט
מערכת SaaS לניהול נכסים וגביית חובות, המיועדת למנהלי בניינים, 
חברות ניהול נכסים, בעלי נכסים וגורמי גבייה.

המערכת מרכזת תחת קורת גג אחת:
- ניהול דיירים ובעלי דירות
- מעקב וגביית חובות
- ניהול משימות ותקלות בבניין
- לוח שנה ופגישות
- תקשורת פנימית וחיצונית (WhatsApp, אימייל)
- ניהול ספקים ומסמכים

## קהל יעד
- מנהלי בניינים
- חברות ניהול נכסים
- בעלי נכסים פרטיים
- עורכי דין וגורמי גבייה

## סטאק טכנולוגי
- **Frontend**: Next.js + TypeScript
- **Styling**: TailwindCSS + shadcn/ui
- **Backend**: Supabase self-hosted (db.bios.co.il)
- **שפה**: עברית, RTL מלא
- **Deployment**: על שרת פרטי

## עקרונות עיצוב
העיצוב נוצר ב-Claude Design ומועבר מסך-אחר-מסך.
ה-design system מבוסס על פרויקט PMS קיים שנבנה על אותו שרת.
לפני בניית כל מסך, המשתמש יעביר את העיצוב (screenshot + JSX).
קלוד מחויב לשמור על 100% מהעיצוב המוגדר — אין לפרש מחדש.

## סגנון עבודה
- **Planning-first**: לפני קוד, תוכנית מפורטת לאישור.
- **מסך אחד בכל פעם**: בונים פיצ'ר שלם end-to-end 
  לפני שעוברים לבא.
- **ללא ניחושים**: אם משהו לא מוגדר, שואל לפני שמחליט.
- **שיחה נפרדת לכל מסך**: כל מסך = שיחה חדשה בקלוד קוד.

## Clarifications & Decisions (עודכן 23/04/2026)

### Auth
- **החלטה**: שתי דרכי כניסה — username/password + Supabase Auth (OAuth)
- **לא לעשות**: אין להעתיק את מערכת AppUser+Role של Base44 
  עם password_hash ב-base64 (לא מאובטח).
- **כן לעשות**: Supabase Auth כמערכת הראשית, טבלת profiles 
  מחוברת ל-auth.users.

### MVP Scope (פאזה א')
- Auth + App Shell (Layout + Sidebar + Header + Guards)
- Dashboard
- Import (ייבוא מ-Excel)
- Settings

שאר המסכים (Tasks, Calendar, Chat, Issues, Suppliers, וכו') 
נדחים לפאזה ב'.

### Integrations
- **פאזה א'**: כל האינטגרציות החיצוניות (Green API, Bllink, 
  MAKE, Resend, Gmail) מיושמות כ-**Stubs** ב-`src/services/`.
- **פאזה ב'**: החלפת ה-stubs בקריאות אמיתיות.

## Reference Material
- `docs/archive/BASE44_REFERENCE.md` — מפרט האפליקציה הישנה ב-Base44. 
  **חומר עזר בלבד**, לא כמקור חובה. המבנה החדש עשוי להיות 
  שונה לפי החלטות שיתקבלו בדרך.

## Decisions Log
- 08/08/2026 (ערב) — **חלון הנפקה רב-אנשים: בלוקי "בעל צ'יפ" חוזרים + ‏POST /api/chips groups.** רפרנס: `ref/proof/Chip2.html` (מסלול 1; ‏`Chip2.md` = השפה הוויזואלית, זהה ל-`Chip.md` המקורי שהוסר מהדיסק — המצביע ב-DESIGN.md עודכן). **מודל**: בלוק = אדם `(role, name, phone)`; צ'יפים קיימים של הדירה נטענים מקובצים לפי זהות מחזיק (`holderKeyOf`) עם תגיות read-only; מספרים חדשים = תגיות "ממתין להנפקה" (ענבר, X להסרה — טרם נשמרו); **Toggle על צ'יפ שמור פותח מיידית את דיאלוגי ההשבתה/החזרה הקיימים** (אותם קומפוננטים, mounted בתוך ה-Sheet) — אין toggle שקט ואין מסלול מחיקה. **API**: ‏POST /api/chips קיבל צורת `groups` (‏`{contact_id, groups:[{resident_role, holder_name?, holder_phone?, chip_type, numbers≤5, ...}], issuance_fee?, fee_charged?, notes?, limit_override_reason?}`) — טרנזקציה אחת, ‏FOR UPDATE, ‏all-or-nothing; ‏**soft-limit נספר על קיימים + סך כל הקבוצות יחד** (הפיצול פר-סוג בלקוח לא עוקף את הספירה — הבהרה 1); ‏409 מחזיר `{chip_number, group_index}` וה-UI מסמן את התגית המדויקת; כפילות בין/בתוך קבוצות נחסמת גם בשרת. **ה-body השטוח הישן נתמך ב-adapter מלא** (`legacyChipBodyToInput` — כל השדות כולל `limit_override_reason`; מכוסה ב-`tests/chips-issue-groups.test.ts`, כולל תרחיש "ישן + חריגה + סיבה → עובר" — הבהרה 2). **חיתוכי UI מאושרים מול Chip2**: כרטיסי תפקיד 2×2 נשארים (לא השורה הקומפקטית) + מצב "נבחר בבלוק אחר" מעיצוב `.taken`; ‏footer ראשי-בהתחלה; ‏X-בלוק רק לבלוק בלי צ'יפים שמורים; סוג פר-מספר (typemini) עם התווית "פיזי"; ‏`app_platform` לא נבחר בזרימה זו (עריכה בפאנל הפרטים). טוקן יחיד נוסף ל-skin: ‏`--chip-violet-border:#DECDFF`.
- 08/08/2026 — **עיצוב מחדש של מודול הצ'יפים לפי רפרנס + זיהוי מחזיק דו-כיווני + חיפוש (migration 074).** **החלטת מוצר: הרפרנס `ref/proof/whatsapp-broadcast/Chip.{html,md}` גובר על `DESIGN.md` בכל התנגשות, בהיקף מודול הצ'יפים בלבד** — היפוך מקומי של הכלל הקבוע; החריג מתועד ב-`DESIGN.md` ("מודול צ'יפים — חריג מוצהר") עם הפרדה בין (א) נגזר-מהרפרנס (חלון ההנפקה) ל-(ב) הורחב-לפי-הפלטה (טבלה/KPI/פאנלים). **Scope נאכף טכנית**: כל הטוקנים כ-CSS vars תחת `.chips-skin` (`src/app/styles/chips.css`); IBM Plex Mono נטען כ-`--font-chip-mono` ופעיל רק דרך `.chip-num` בתוך ה-scope — `font-num` הגלובלי נשאר Inter, שום גוון לא דולף. פרטים מחייבים:
  - **זהות מחזיק = `(contact_id, resident_role)`** (ולצ'יפי `other`/`staff` גם `holder_name` — כמה מחזיקי "אחר" יכולים לחלוק דירה). `contact_id` נשאר `NOT NULL` (בניגוד לניסוח הסלייס — כך הסכמה מ-072, וכל צ'יפ שייך לדירה גם כשהמחזיק אינו במרשם); `resident_role` הפך `NOT NULL` (074, ‏0 שורות backfill — הטבלה הייתה ריקה) + CHECK ‏`chips_holder_identity_check`: תפקיד snapshot ‏(`other`/`staff`) מחייב `holder_name`. `staff` שומר על סמנטיקת `other` (snapshot, "לא במרשם").
  - **השם מוצג תמיד מהמקור החי**: כל קריאת צ'יפ עושה LEFT JOIN ל-contacts ומחזירה `live_holder_name/phone` + ‏`holder_chip_count`; פונקציה יחידה `resolveChipHolder` (`src/lib/chips/holder.ts`, client-safe) היא הדרך היחידה להציג מחזיק — `holder_name` הוא snapshot היסטורי ("הונפק בשם") שמוצג רק כ-tooltip כשהשם החי השתנה, או כזהות עצמה ב-`other`/`staff`. כל המוטציות מחזירות את השורה המועשרת (re-select בתוך אותה טרנזקציה).
  - **חיפוש דו-כיווני** ב-`listChips`: פרמטר `q` יחיד, פיצול ל-tokens (AND בין tokens, OR בין שדות), **חיפוש שם מוגבל-תפקיד** (התאמה ל-`tenant_name` מחזירה רק צ'יפי `tenant` של הדירה — לא את צ'יפי הבעלים), חוצה-טאבים (ה-tab מדולג כשיש `q`), מיון עדיפות exact→prefix→substring→apartment→name, ו-`match_type` פר-שורה. **pg_trgm הותקן** (billing_app מורשה — trusted extension) + ‏6 אינדקסי GIN (‏chips: number/apartment/holder_name; contacts: owner/tenant/operator_name) + אינדקס `chips_holder_idx (contact_id, resident_role, status)` לכיוון "שם → מספרים". Route חדש `GET /api/chips/holders/[contactId]/[role]` (chips:view).
  - **אכיפת הנפקה**: `resident_role` חובה ב-POST (400 בלעדיו); תפקידי מרשם לוקחים snapshot מהשורה הנעולה של contacts (עם אפשרות override פר-צ'יפ; אין פרטים במרשם → 422 `holder_not_in_registry`); ‏`other`/`staff` מחייבים `holder_name` (400).
  - **UI**: שם המחזיק הוא העמודה הראשית בטבלה (דירה+תפקיד מתחתיו), badge "לא במרשם", חיווי "N צ'יפים" שמסנן לאדם, פאנל מחזיק חדש `ChipHolderPanel` (שם → כל המספרים), בלוק "מחזיק" בראש `ChipDetailPanel` עם קישור לפאנל, empty-state ייחודי לחיפוש, ו-badge ‏match_type על תוצאות. ב-`IssueChipSheet` שורות המספרים הוחלפו בדפוס הרפרנס (קלט+"הוסף מספר"+תגיות; Enter מוסיף; שארית בקלט נשלחת גם בלי "הוסף"); הסרה קיימת **רק לתגית ממתינה** (טרם הונפקה) — אין שום מסלול מחיקה לצ'יפ מונפק, בהתאם לחוק.
- 08/08/2026 — **מודול צ'יפים + הפיכת `contacts` למרשם הדירות (מיגרציות 071–073, קומיטים c1186bc→d925efa).** ההחלטה הארכיטקטונית: **`contacts` הוא מרשם הבניין ומקור האמת היחיד לדירות ולזהות דיירים** (בעל/שוכר/מפעיל — שם+טלפון, `resident_type`, `unit_type`, `source`, `needs_review`); `debtors` הוא תת-קבוצה שמוזנת מ-Bllink בלבד (כספים/משפטי), בלי FK ל-contacts כדי שהסנכרון לעולם לא ייכשל. עמודות הזהות ב-`debtors` (`owner_name`/`tenant_name`/`phone_owner`/`phone_tenant`) הן **legacy קפוא — DROP ב-Phase B**. פרטים מחייבים:
  - **קריאה**: כל 8 אתרי ה-SQL שקוראים שם/טלפון (debtors.ts, whatsappConversations, whatsapp-broadcast, whatsapp-inbound, search, statuses, targets, campaigns) עברו ל-JOIN על `contacts` דרך `debtors.contact_id` עם fallback בנוסח `case when rc.id is null` — **בכוונה לא `coalesce`**: NULL של contact מקושר גובר, כדי שניקוי שדה במרשם לא יחיה ערך ישן מ-debtors. שמות שדות ה-payload נשמרו — ~20 צרכני UI לא נגעו.
  - **כתיבה**: הצינור המשותף (ייבוא אקסל + סנכרון Bllink, `importParsedRows`) הפסיק לכתוב זהות ל-debtors; במקומו hook‏ `ensureContactsForApartments` — **INSERT ... ON CONFLICT DO NOTHING בלבד, לעולם לא UPDATE על contact קיים** — יוצר דירות חסרות (`source='bllink_sync'`, `needs_review=true`) ומקשר מחדש `contact_id` (שורד replace). סיכום פר-ריצה ב-`import_runs.error_summary` (073). עריכת טלפון ידנית (EditPhoneDialog) כותבת ל-contacts דרך `updateContactPhonesByDebtor` (SET ישיר — null מוחק, בניגוד ל-upsert ה-coalesce).
  - **צ'יפים**: `chips.contact_id` FK→contacts ‏ON DELETE RESTRICT‏ + snapshot ‏`apartment_number`/`holder_*`; ‏`chip_number` ייחודי רק בין פעילים (partial unique); **אין DELETE ואין עריכת מספר** — רק deactivate/reactivate; soft-limit 4 פעילים לדירה (חריגה מחייבת `limit_override_reason`, נאכף תחת FOR UPDATE על שורת ה-contact — עמיד למרוץ); הנפקה מרובה (עד 5) all-or-nothing; `chip_events` append-only נכתב באותה טרנזקציה. הרשאות: מודול `chips` יחיד view/edit (המנגנון לא תומך ברמות ביניים) — מנהל view+edit, צופה כלום (fail-closed). התראות: `chip_issued`, `chip_deactivated_lost` (רק lost/stolen), `source_entity_type='chip'`.
  - **ייבוא דיירים** `kind='residents'`: שורה-לאדם (A דירה · B שם · C טלפון · D תפקיד בעל/שוכר/מפעיל), merge בלבד — תא ריק לא מוחק; דירה חסרה **נוצרת** (`source='residents_import'`, `needs_review→false`); `validatePhone` קפדני (שורה נכשלת, בלי phantom); כפילות (דירה,תפקיד) — first-wins והכפולה נכשלת; פורמט `error_details` זהה אות-באות לייבוא ה-contacts (`{row, apartment_number?, error}`, cap 50).
  - **פער המרשם**: contacts≈debtors ‏(290/288, הפרש 2) ⇒ המרשם נבנה היסטורית מנתוני חוב וכנראה חלקי — באנר dismissible ב-`/chips` + KPI "דירות ללא צ'יפ פעיל — מתוך N דירות במרשם" + יצירת דירה חסרה inline מתוך IssueChipSheet‏ (`POST /api/contacts`; ‏`createContact` חותם `source='manual'`).
  - **קונבנציית DOWN חדשה לסלייס הזה**: קובץ `.down.sql` נפרד לכל מיגרציה (071/072/073) במקום בלוק מוער.
- 26/07/2026 — **`npm run check:all` היה אדום על main — שני gates תוקנו (בדיקת סודות לפי צורת-מפתח, והוכחות-כתיבה בארגז חול ללא `CREATEDB`).** שתי הבדיקות שנוספו ב-`052b303` לא יכלו לעבור בסביבה הזו, כך שה-gate הקבוע לא היה שמיש. **(1) `check:secrets`** נכשל על `TESTING.md` כי חיפש את התחילית `sk-ant-` כטקסט חופשי — והתיעוד *מתאר* את התבנית. תוקן ל-`git grep -E` על **צורת מפתח שלמה**: `sk-ant-[A-Za-z0-9_-]{20,}`, `"role"[[:space:]]*:[[:space:]]*"service_role"` (סובל רווחים ב-JWT), `-----BEGIN [A-Z ]*PRIVATE KEY-----` (תופס RSA/OPENSSH/EC/plain, מדלג על `CERTIFICATE`). אומת: שלוש הצורות נתפסות על מפתחות מדומים, אזכור-תחילית בתיעוד לא מפיל. **(2) `check:dupes` + `check:wa`** נכשלו ב-`permission denied to create database` — ל-role `billing_app` אין `CREATEDB`, ולכן הוכחת-הכתיבה על "מסד חד־פעמי" **מעולם לא רצה**. `withTempDb` הוסר (חסר-שימוש ובלתי-שמיש) והוחלף ב-`uniqueViolationProof` ב-`scripts/_check-lib.mjs`: טבלאות `TEMP` בסשן `psql` אחד, בתוך טרנזקציה שנגמרת תמיד ב-`ROLLBACK` — בלי הרשאה מיוחדת ובלי לגעת בנתונים אמיתיים. ההכנסה הכפולה מפילה את הטרנזקציה, לכן `savepoint` סביבה + שורת-סימן `DUP_ACCEPTED` שמבדילה **דחייה** מ**קבלה** (בלי הסימן, `rollback to savepoint` היה מסתיר כפילות שנקלטה ומחזיר ירוק-כזב). **בקרת-שלילה**: אותו proof על טבלה ללא unique key מחזיר `rejected=false` (נתפס). אומת שאין דליפה לנתונים אמיתיים (`A-101`/`A-102` לא קיימים ב-`debtors`), אין טבלאות שרד ואין מסדי `billing_check_%` שנשארו מריצות שנכשלו. **התוצאה**: `check:all` ירוק מקצה-לקצה — typecheck + 284 בדיקות + 7 בדיקות אינווריאנטה. `TESTING.md` עודכן (תיאור שתי הבדיקות, פסקת "ארגז החול", ומספר הבדיקות 235→284). **בקרת-שלילה קבועה (מי שומר על השומר)**: `selfTestUniqueViolationProof` מריץ את אותה הוכחה על טבלה **ללא** unique key ודורש `rejected=false` — **אוטומטית פעם אחת בכל תהליך** לפני ההוכחה האמיתית הראשונה, כך שגלאי ששבר את עצמו (סימן שהשתנה, נוסח שגיאה של psql שכבר לא תואם, stderr שנבלע) לא יכול להחזיר ירוק. נקודת כניסה ידנית: `node scripts/_check-lib.mjs --self-test`. אומת מול גלאי שבור מכוון: נכשל לבד (exit 1) **וגם** דרך `check:dupes`. הכל ב-commit `875170a`.
- 26/07/2026 — **כרטיס דירה: בעלים/שוכרים נוספים ללא הגבלה + גודל דירה ודמי ניהול נגזרים (migration 066).** **סכימה additive בלבד**: `contacts.apartment_size_sqm`, `contacts.management_fee`, וטבלה חדשה `contact_people` (`role` owner/tenant, `is_primary_contact` = "מקבל הודעות", `sort_order`, cascade על מחיקת דירה). **ההחלטה המרכזית**: האדם **הראשון** בכל תפקיד נשאר בעמודות `owner_*`/`tenant_*` הקיימות, והטבלה מחזיקה רק את אדם #2 והלאה — לכן ייבוא ה-Excel, סנכרון בלינק, הדשבורד והשידורים לא שונו כלל (אין מיגרציית נתונים, אין נתיב קריאה שנשבר). `apartment_size_sqm`/`management_fee` נכנסו ל-`PROTECTED_FIELDS` כדי שייבוא לא ידרוס שדה ידני. **דמי ניהול = גודל במ"ר × 150% × מחיר למ"ר מההגדרות** — נוסחה אחת ב-`src/lib/billing/managementFee.ts` (pure, גם הלקוח מייבא) שמשמשת שלושה נתיבים: התצוגה בכרטיס, `POST`/`PATCH /api/contacts` (השרת בעל-הסמכות — ערך מהלקוח לא יכול לסטות מהנוסחה), ו-`recomputeAllManagementFees` שמריצה נגזרת מחדש על כל דירה עם גודל אחרי שינוי תעריף. תעריף ריק → השדה חוזר להזנה ידנית והערכים הקיימים נשמרים. `GET /api/settings/billing` פתוח לכל מחובר (מחיר, לא סוד — עורכי כרטיס דירה לא בהכרח בעלי `settings:view`), `PUT` דורש `settings:edit`. **נמענים**: כל אדם נוסף עם "מקבל הודעות" וטלפון תקין הוא נמען בפני עצמו — בורר הטלפונים בשליחה בודדת (`/api/whatsapp/recipient-phones`), `send-bulk` (job לכל הודעה + cache לשורת החייב + dedup פר-חייב כדי שמספר כפול לא יקבל פעמיים), ושידורים (`resolveBroadcastRecipients`, dedup גלובלי לפי טלפון; `owners`→owner, `tenants`→tenant, `all`/`debtor_ids`→שניהם). `GET /api/whatsapp/audience-count` יונק מאותו resolver, כך שההערכה החיה בממשק כוללת את התוספות. **UI**: הפאנל מציג תת-כרטיס לכל אדם נוסף (אותם שדות + "מקבל הודעות" + מחיקה), `PersonCard` משותף לשני הסקשנים; בטבלת אנשי הקשר chip "+N" ליד הטלפון (המספרים עצמם רק בכרטיס, שורה אחת לדירה); `PEOPLE_JSON` aggregate בשאילתה אחת (בלי N+1). בנוסף: תיבות חיבור ההודעה (בודדת/מרובה/שידור) הוגדלו ל-`rows=18` + `min-h-48`. **מצב**: המיגרציה הורצה, הקוד חי בפרודקשן מה-deploy של 25/07 (15 דירות עם גודל+דמי ניהול, 15 שורות `contact_people`), `tests/managementFee.test.ts` מכסה את הנוסחה — **נכנס ב-commit `9342922`** (14 קבצים ששונו + 7 חדשים, `+1202/−60`; ה-mtime של כל הקבצים החדשים נשאר 23/07 — הקוד לא שונה אחרי ההרצה בפרודקשן, רק אומת ונדחף).
- 25/07/2026 — **משימות מחזוריות: מעבר ממודל HORIZON למודל „מופע אחד” + שורת צ'יפים על הכרטיס (migration 067).** **הבעיה:** `materializeDue` ייצר שורת `tasks` נפרדת לכל מופע עד 45 יום קדימה, ורץ מחדש בכל טעינה של `/tasks` — משימה יומית = ~45 שורות, שבועית ב-3 ימים = ~20 שורות; הלוח וספירות ה-lanes הוצפו באותה משימה. **ההחלטה (המשתמש):** משימה מחזורית = **שורה אחת בלבד**; `due_date` = המופע הנוכחי; „בוצע” לא סוגר אלא **מקדם** למופע הבא ורושם ב-`task_occurrence_completions`. **היקף: משימות בלבד** — `calendar_events` נשאר על מודל ההרחבה שלו (עד 78 שורות לסדרה, `createEvent`); חוב מוצהר, slice נפרד. **הבהרה (אומת 26/07 — הניסוח הקודם התפרש בטעות כאילו היומן מייצר שורות למשימות):** החוב נוגע לסדרת **אירועי יומן בלבד** ואין לו שום נגיעה ל-`tasks` — ל-`calendar_events` אין `task_id` ואין FK ל-`tasks` (ה-FKs היחידים: `owner_user_id`→`users`, `parent_series_id`→עצמה), אף נתיב קוד של משימות לא יוצר אירוע יומן, וההרחבה היא `expandRecurrence` עם `MAX_OCCURRENCES=78` שמכניס שורה למופע ב-`createEvent`. החוב **אמיתי ונשאר פתוח** — הוא פשוט לא חוב של מודול המשימות. **כל המופעים הקיימים נמחקו** (בחירת המשתמש) ולכן לוג ההשלמות מתחיל ריק.
  **התגלית שצמצמה את המיגרציה:** התווית והצ'יפים של הרפרנס **נגזרים מהעוגן + `interval` הקיימים** — אין צורך ב-`bymonth`/`bymonthday`. `כל רבעון` = `monthly/3`, `כל חצי שנה` = `monthly/6`, `ב-15 לחודש` = יום-בחודש של העוגן. אומת מול שלושת כרטיסי הרפרנס (עוגן 09 + `/6` → חודשים 3,9; עוגן 10 + `/3` → 1,4,7,10; עוגן 15 + `/1` → „ב-15 לחודש”) ומכוסה ב-`tests/recurrence-cadence.test.ts`. לכן `frequency`/`interval`/`byweekday`/`coerceRecurrence` **לא שונו**, ו„רבעוני”/„חצי-שנתי” הם presets ב-UI בלבד.
  **סכימה (067):** `task_recurrences.anchor_date` חדש (העוגן עבר מ-`tasks.occurrence_date` לכלל, כי `due_date` הפך מצביע-מתקדם והעוגן חייב להישאר קבוע ל-`interval`/`after_count`) · `task_occurrence_completions` (`unique(task_id, occurrence_date)`) · קידום כל template ל-`min` המופע הפתוח העתידי לפני המחיקה · מחיקת כל `is_recurring_instance` **עם** `entity_assignees`/`reminders`/`notifications` (פולימורפיות, אין FK ל-`tasks`) · `tasks_recurrence_occurrence_uniq` הוחלף ב-`tasks_recurrence_single_row_uniq` על `(recurrence_id)` — האילוץ שאוכף „שורה אחת לסדרה” · `occurrence_date`/`parent_task_id`/`is_recurring_instance`/`last_spawned_at`/`spawned_count` הוקפאו עם `comment on column` (לא DROP).
  **קוד:** `recurrence/materialize.ts` → `recurrence/series.ts` (`completeRecurringOccurrence`/`skipTaskOccurrence`/`detachTaskOccurrence`/`endTaskSeries`, כולם דרך `advanceWithin` אחד) · `recurrence/cadence.ts` חדש (pure: kind/label/chips/window/expected) · `engine.nextOccurrenceAfter` + פרמטר `fromDate` ל-`computeOccurrences` · `db/taskCompletions.ts` · `CadenceStrip`+`CadenceProgress` (מקור-אמת יחיד, DESIGN §30) · `materializeAllActive()` הוסר מ-`/tasks` (הטעינה חזרה להיות קריאה טהורה).
  **שתי מלכודות שנפתרו במימוש:** (א) **העוגן** — `applyRecurrenceOnSave` מקבל `dueDateChanged` ומעדכן `anchor_date` **רק** כשהתאריך זז בפועל (השוואה ל-`before.due_date`, לא נוכחות המפתח — הטופס שולח `due_date` בכל שמירה); בלעדיו כל שמירה חוזרת הייתה מאפסת `after_count` ומזיזה את הפאזה. (ב) **סדר ב-PATCH** — `status` מוסר מה-patch, `applyRecurrenceOnSave` רץ **לפני** הקידום, ואז מתבצע הקידום; אחרת השורה הייתה מהבהבת דרך `done` וה-zombie-guard היה מוחק את התזכורות שלה. `cancelled` על סדרה = דילוג (בלי רישום השלמה). **תג ההתקדמות מוצג רק כש-`expected > 1 && done > 0`** — זה מה שמסביר למה ברפרנס רק הכרטיס השבועי עם 3 ימים מציג `1/3 השבוע`. **החישוב server-side בלבד** (תלוי „היום” לפי Asia/Jerusalem → אחרת אי-התאמת SSR/hydration). **אין קידום אוטומטי של מופעים שפוספסו** — משימה שתאריכה עבר נשארת אדומה/באיחור עד שמסמנים אותה, ההתנהגות הנכונה לתחזוקת בניין. `check:all` נקי (300 טסטים). ראה [[project-tasks-module-built]].
- 17/07/2026 — **העשרת תוכן הודעות התראה (וואטסאפ+מייל) + קישור מותנה-נמען.** כל הודעת create/assign (4 ה-routes של issues/tasks דרך `dispatchCreateNotifications`) וכל תזכורת מה-engine כוללת עכשיו שורות פרטים אחידות — תיאור / מיקום (`target_label`; null → השורה לא מופיעה כלל) / תאריך יעד (DD/MM/YYYY [HH:MM]) / „הוקצה ע"י" (create → `created_by_name`, edit → ה-actor העורך) — דרך helper משותף `detailRows` ב-`src/services/createNotify.ts` (+`detailRowsWhatsApp`); המייל דרך `sendNotificationEmail({details})`. **קישור „לצפייה במערכת"** (`/issues?issue=<id>` / `/tasks?task=<id>`, מאחורי login): נשלח **רק לנמעני `kind==='user'`** — במייל היה קיים והפך מותנה, לוואטסאפ נוסף; ב-engine תמיד (הנמענים שם users). **ספק (`kind==='supplier'`) מקבל את כל הפרטים בטקסט, בלי קישור.** בלי migration, בלי route חדש.
- 17/07/2026 — **FOLLOW-UP מאושר (לא נבנה): קישור ציבורי לספק לצפייה בתקלה + תמונות בלי התחברות.** נדחה כי זה surface חשיפה חדש: תמונות ב-`issue-attachments` הפרטי מוגשות רק דרך `/api/files` מאחורי `requirePermission('issues','view')`; `/api/public/wa-media` אינו תקדים (חושף bucket שהוא ממילא public). הסקופ כשיבוצע: דף ציבורי `/public/issue/[token]` מחוץ ל-`(app)` layout + טוקן לא-ניתן-לניחוש ב-DB (migration additive, עם תפוגה/ביטול) + route ציבורי להזרמת תמונות scoped-לתקלה דרך ה-chokepoint (`getObjectStream` — לא שובר את guard-no-storage-leak) + החלטת מדיניות תפוגה. עד אז ספק מקבל פרטים מלאים בטקסט בלבד (ההחלטה מעל).
- 17/07/2026 — **טופס תקלה/משימה — מודל התראות חדש (`ChannelCards`) + כרטיסים גלויים-תמיד (deployed + נדחף).** מטריצת נמען×ערוץ (`NotifyMatrix`) הוחלפה ב**שני כרטיסי ערוץ גלובליים** (וואטסאפ/מייל) הנשלחים **לגורם המטפל הנבחר בלבד** (הרחבה דרך `channelsToSelection` ב-`src/lib/notify/selection.ts` → אותו `body.notify` שה-routes כבר צורכים; **ה-routes לא שונו**). **החלטה 17/07:** הכרטיסים **גלויים תמיד** (בוטלה ההסתרה-עד-„הוסף-תזכורת” מתיקון 3) — שליחה מיידית לגורם המטפל היא מסלול עצמאי; אזור התזכורות עצמאי מתחת ויורש את הערוץ הגלובלי. שינוי **display-only** בשני הטפסים (`{channelCards}` ראשון + הסרת ה-guard `reminders.length > 0`). **„שלח גם אליי” (self)** נשאר **תזכורת-סקופד** — enabled רק עם תזכורת מתוארכת → `reminders.notify_owner` (**migration 065**) + פעמון in-app מיידי לבעלים. **תזכורות**: reminders-first, default „עכשיו” (עיגול 5 דק׳), **חסימת עבר** (client `hasNewPastReminder` + server `reminderInPast`, פטור לרשומות קיימות). **„גורם מטפל”** = שני בוררים 50/50 (`sm:grid-cols-2`). תועד ב-DESIGN.md **§33**. `tsc --noEmit` נקי. ראה [[project-notify-channel-redesign]], [[project-multi-assignee-junction]], [[project-two-reminders-systems]].
- 24/06/2026 — **תיקון React #418 (hydration mismatch) — server רץ UTC, לקוח Asia/Jerusalem (deployed; commit ממתין ל„תדחוף”).** השורש: ערכי תאריך/שעה שמרונדרים ב-SSR ב-UTC ובלקוח ב-Jerusalem (הפרש ~3ש') → text/element mismatch. **אבחון (workflow קודם) זיהה formatters בלי `timeZone`; ביקורת אדוורסרית (workflow, 3 reviewers) הרחיבה את השורש** למחלקה שלמה של `new Date()`-כ-today שמניע **רינדור מותנה** (לא רק מחרוזת) — בראשה `DebtorsTable` בדשבורד (#418 **מבני**, הדף הכי נטען). **שלושה סוגי תיקון**: **(A) דטרמיניסטי — `timeZone:'Asia/Jerusalem'`** לכל `Intl.DateTimeFormat`/`toLocale*` שהשמיט אותו: `LastImportIndicator`, `chat/format.ts`, `messages/format.ts`, `documents/helpers.ts`, `registry.ts` (חדש `formatAbsoluteTime`); ועיגון "היום/אתמול" ל-Jerusalem דרך `@/lib/dates` (`todayInJerusalem`/`addDaysToIsoDate`, כדפוס הקיים ב-`overview/format.ts`) → דטרמיניסטי, בלי flash. **(B) render-after-mount** — hook משותף חדש `src/lib/hooks/useHasMounted.ts` (`useState(false)`+`useEffect`), לערכים תלויי-`Date.now()` באמת (severity ב-`LastImportIndicator`, "לפני X ד׳" ב-chat `ConversationList`, `formatRelativeTime` ב-`notifications-table` — `/notifications` מרנדר items ב-SSR): SSR/paint ראשון = placeholder אבסולוטי דטרמיניסטי, אחרי mount = הערך היחסי. **(C) הרחבת הביקורת** — `DebtorsTable.compareToToday` (bucket past/today/future) ל-Jerusalem string-compare + `formatDueDate` ל-Intl-Jerusalem (במקום date-fns `format` בלי TZ); `tasks-table`/`tasks-kanban` `isOverdue` → `todayInJerusalem()`; `calendar` cursor init עוגן ל-Jerusalem-noon; `InviteCard.daysUntil` → הפרש ימים קלנדרי ב-Jerusalem; `LoginForm` year. **לא נגעתי בתקין**: `overview/format.ts`+`SupplierActivity` (כבר TZ), `Sidebar` (localStorage ב-useEffect), `NotificationBell`/`IssuesWidget` (on-demand / server-component → לא עושים hydrate). **QA**: `tsc --noEmit` נקי → `npm run deploy` (PID 3664281, BUILD_ID `OK9DAsx3EjZtHndyY2XhY`); smoke: root 307/login 200/chunk 200/journal נקי, ה-SSR מרנדר **19:41 (Jerusalem)** ולא 16:41 (UTC) (ה-ISO ה-UTC ב-props המסוריאלים הוא data עקבי, לא display). **אימות ידני בדפדפן (super_admin)**: Console נקי מ-#418 ב-6 המסכים (דשבורד/משימות/צ'אט/WhatsApp/התראות/לוח-שנה), שעות שעון-ישראל. 14 קבצים. ראה [[build-breaks-live-server]].
- 24/06/2026 — **אבטחה — גל תיקון 3b: CSP מ-Report-Only ל-enforce (deployed; commit ממתין ל„תדחוף”).** אחרי גל 3 (CSP כ-`Content-Security-Policy-Report-Only`) ואיסוף הפרות בדפדפן כ-super_admin על **כל הייצואים (PDF/Excel/הדפסה) + המסכים המרכזיים** שהחזיר **אפס הפרות** — ב-`next.config.ts` שם הכותרת שונה בלבד מ-`Content-Security-Policy-Report-Only` ל-**`Content-Security-Policy`** (אוכף). **ה-policy זהה byte-for-byte** למחרוזת ה-Report-Only (`default-src 'self'; img-src 'self' data: blob: https:; style-src 'self' 'unsafe-inline'; script-src 'self' 'unsafe-inline'; connect-src 'self' https:; font-src 'self' data:; frame-ancestors 'self'`) — אפס directives נוספו/הוסרו; 5 הכותרות האחרות (HSTS/X-Frame/nosniff/Referrer/Permissions) ללא נגיעה; ההערה ב-next.config עודכנה. **הסתייגות ה-Excel מגל 5 נבדקה ונסגרה**: ה-browser-bundle של exceljs מכיל `Function()` (eval-class) וה-`script-src` בלי `'unsafe-eval'`, אך **אימות ידני בדפדפן תחת enforce** הראה שייצוא ה-Excel (וגם PDF/הדפסה) **עובד** — ה-`Function()` לא יורה בנתיב ה-`writeBuffer`, ולכן `'unsafe-eval'` לא נדרש ולא נוסף. **QA**: `tsc --noEmit` נקי → `npm run deploy` (PID 1427109, BUILD_ID `QRczu8IasFtS88zc5UGFY`). **Smoke**: root 307→/login, login 200, כל ה-CSS/JS/woff2 מ-'self' ב-200, Heebo self-hosted (next/font, לא Google CDN בזמן ריצה), journal נקי; `curl -sI` (local + public דרך nginx) אישר `Content-Security-Policy` **בלי** variant של Report-Only; **אימות ידני** של 3 הייצואים + מסכים מעוצבים — Console נקי מהפרות CSP. ראה `SECURITY_AUDIT.md`.
- 24/06/2026 — **אבטחה — גל תיקון 4: גיבוב session id ב-DB (deployed; commit ממתין ל„תדחוף”).** ה-session id עבר מאחסון **plaintext** ל-**hash-only** ב-`public.sessions.id`, בדיוק כמו reset/invite tokens — אותו `hashToken` (`src/lib/auth/tokenHash.ts`, `createHash('sha256').digest('hex')`, 64-hex). **מודל**: ה-cookie `almog_sid` מחזיק את ה-id ה**גולמי (raw)**; ה-DB שומר רק `hashToken(raw)`; כל ה-lookups מגבבים את ערך ה-cookie לפני החיפוש (DB read/דליפת גיבוי/replica לא נותנים session id שמיש). **שינוי יחיד בלוגיקה** — `src/lib/auth/session.ts` (4 פונקציות): `createSession` (INSERT שומר `hashToken(id)`, ה-cookie נשאר raw, מאפייני ה-cookie httpOnly/secure/sameSite/maxAge ללא שינוי), `getSession`/`getActiveSessionId`/`deleteSession` (lookup/delete לפי `[hashToken(sid)]`). `deleteAllUserSessions` + המחיקות ב-`users/[id]` (לפי `user_id`) ו-`middleware` (presence-check בלבד, אין DB lookup) — **ללא שינוי**; כל 3 יוצרי ה-session (login/google-callback/accept-invite) עוברים דרך `createSession`; ה-`.sid` המוחזר מ-getSession לא נצרך בשום מקום במורד הזרם. **מיגרציה 057** (`057_session_id_hash.sql`, **additive-בלבד** — `id` נשאר `text primary key`, hash של 64-hex נכנס ללא DDL, ה-PK index נשאר תקף, אין DROP): `delete from public.sessions` — **30 sessions פעילים בוטלו** (ניתוק חד-פעמי **מכוון** של כל המשתמשים, כולל המפעיל; שורות plaintext לעולם לא יתאימו ל-lookup המגובב). **תיקון נלווה** (נתפס בביקורת): 3 סקריפטי בדיקה (`test-contacts-import.sh`/`test-contacts-api.sh`/`test-tasks-api.sh`) שמנפיקים session ידנית עודכנו להכניס `sha256(raw)` ל-DB (ול-cleanup) תוך שמירת ה-cookie raw — אחרת 401 בכל בקשה מאומתת. **אבחון+ביקורת**: workflow קריאה-בלבד (3 reviewers בעדשות שלמות/נכונות/מיגרציה + סינתזה) — אישר אפס touchpoints שפוספסו ואפס באגים. **QA**: `tsc --noEmit` נקי → מיגרציה (30→0) → `npm run deploy` (PID 52130, BUILD_ID `UXtQOhuBqEOnKL50q4nqU`). **Smoke**: root 307→/login, login 200, chunk 200, journal נקי; מחזור מלא — cookie גולמי→`/api/contacts` 200 (getSession מצא דרך hash), `sha256(cookie)==db_id` ו-raw לא ב-DB, מזויף→401 (API)/307→/login (page), logout מוחק לפי hash→401; ה-E2E המתוקן 14/14. **מסגר את הפריט ב-SECURITY_AUDIT.md** ('Session IDs stored plaintext', Low/accepted) — בוצע. ראה `SECURITY_AUDIT.md`, [[project-rbac-architecture]].
- 23/06/2026 — **אבטחה — גל תיקון 3c: סגירת פערי rate-limiting (deployed; commit ממתין ל„תדחוף”).** שימוש חוזר במנגנון הקיים `src/lib/auth/rateLimit.ts` (`checkRateLimit(bucket,{max,windowSec})` + `clientIp(req)`, DB-backed ב-`public.auth_rate_limits`, sliding-window, bucket-agnostic — **ללא שינוי במנגנון**) על 3 endpoints שדולגו + אימות forgot. **קבועים חדשים** ב-`constants.ts` (כולם 10 לחלון 15min, מיושרים למשפחה הקיימת login-ip=20/login-user=5/forgot=10/accept-invite=15 — ללא ערכים חריגים): `RESET_PASSWORD_MAX_PER_IP`, `SYNC_BLLINK_MAX_PER_IP`, `WHATSAPP_PULL_MAX_PER_IP`. **(1) `/api/auth/reset-password` (הקריטי)** — לא-מאומת, חשוף brute-force על טוקני reset; gate IP (`reset:ip:`) **לפני** `consumeResetToken` (לפני כל token lookup/DB mutation). **(2) `/api/sync/bllink`** — admin-only; gate IP (`sync:bllink:ip:`) **אחרי** ה-auth ו**לפני** ה-side-effects (CRM scrape/createSyncRun/syncDebtorsFromCrm) — לא-מאומת מקבל 401 ולא 429, וה-limiter סופר רק עומס מאומת; חתימה שונתה `POST()`→`POST(req: Request)` ל-`clientIp(req)`. **(3) `/api/whatsapp/pull`** — `whatsapp:edit`; gate IP (`whatsapp:pull:ip:`) **לפני** ה-auth (חוסם floods לא-מאומתים לפני ה-Green API pull שכותב שורות inbound). **(4) `/api/auth/forgot-password`** — **כבר מוגן ותקין → verify-only, ללא שינוי**. החלטה מודעת: forgot מחזיר **200 `{ok:true}` ולא 429** ב-over-limit (אנטי-enumeration — 429 היה מסגיר rate-limited מול לא); 3 ה-endpoints החדשים מחזירים **429 byte-for-byte כמו login** (`{error:'rate_limited'}` + `Retry-After`) כי כל אחד כבר חושף תגובות non-200 נבדלות (reset:400, sync:401/403/502, whatsapp:401/403/503/502) ולכן 429 גלוי לא מסגיר כלום. **אבחון**: workflow קריאה-בלבד (6 סוכנים) שמיפה את המנגנון, בדק כל endpoint, וסינתז תוכנית אדוורסרית. **QA**: `tsc --noEmit` נקי → `npm run deploy` (PID 512786, BUILD_ID `-2Yh1z_if1SRWi0AeiWXt`). **Smoke** (IP-בדיקה מבודד 203.0.113.77, seeding ישיר ל-`auth_rate_limits` לבדיקת 429 בלי להפעיל side-effects): reset `400×10→429` (לגיטימי בודד עובר), sync unauth→401 / authed+seeded→429 (sync לא הופעל), whatsapp unauth-clean→401 / seeded→429 (אין pull), forgot 12×200 + bucket מוגבל ל-10 (limiter פעיל); root 307→/login, login 200, chunk 200, journal נקי; שורות הבדיקה נוקו (0 נותרו, 19 buckets חיים ללא נגיעה). ראה `SECURITY_AUDIT.md`, [[project-rbac-architecture]], [[project-bllink-sync-architecture]], [[project-whatsapp-module]].
- 23/06/2026 — **אבטחה — גל תיקון 5: החלפת `xlsx` (SheetJS) ב-`exceljs` (working-tree, טרם נדחף).** סוגר את 2 ה-CVEs ה-**high** של `xlsx` ש**אין להם fix ב-npm** (Prototype Pollution GHSA-4r6h-8v6p-xvw6, ReDoS GHSA-5pgg-2g8v-p4x9) — הווקטור היה פרסור קובץ Excel **מועלה** (לא-מהימן) בשרת. **helper משותף חדש** `src/lib/excel/workbook.ts`: `worksheetToMatrix(ws,cols=8)` שמשקף 1:1 את `sheet_to_json({header:1,range:1,defval:null,blankrows:false})` (דילוג שורת-כותרת, `row.hasValues` לדילוג שורות ריקות, תאים ריקים→null, 0-indexed A→0..H→7), `cellToRaw` (number/string/boolean/null + richText/formula.result/hyperlink.text/Date), `toArrayBuffer` (Node Buffer→ArrayBuffer כי `xlsx.load` של exceljs דורש מבנית ArrayBuffer), ו-`MAX_EXCEL_BYTES=10MB`. **קוראים** (פרסור נכנס, שימור התנהגות מלא — מיפוי A-H, A-ריק→skip, F טקסטואלי, ריק→null, מיזוג phone_owner, coercion מספרי): `excel/parse.ts` (חייבים — **הפך async**, הקורא `import/runner.ts` עבר ל-`await` + try/catch→`finishRunError` כי הוא fire-and-forget), `contacts/import-runner.ts` (אנשי קשר, כבר async), `import/components/Step1Upload.tsx` (תצוגה מקדימה בלקוח, `import('exceljs')` דינמי לשמירת code-split). **כותב** (ייצוא): `export/debtors-export.ts` — `wb.xlsx.writeBuffer()`→`Blob`→הורדה דרך `<a download>` (במקום `XLSX.writeFile`), גיליון 'חייבים' עם `rightToLeft`, רוחב עמודות `[10,24,16,12,12,12,16]`, מספרים כ-Number (לסכימה), טקסט/טלפון כ-String. **size-cap 10MB** ב-routes (`debtors/import` + `contacts/import`) → HTTP 400 `file_too_large` לפני פרסור. **הוסרה תמיכת `.xls`** (החלטת משתמש; exceljs קורא `.xlsx`+CSV בלבד, לא BIFF הישן — `.xls` הוא legacy לפני-2007): ולידציית סיומת בשני ה-routes ל-`.xlsx` בלבד + `Step1Upload` `accept=".xlsx"` + טקסט עודכן. **תלויות**: `npm uninstall xlsx` + `npm i exceljs@^4.4.0` (כולל types). **`npm audit` 20→19** — 2 ה-high של xlsx (אין-fix) **נעלמו**; exceljs מוסיף **moderate** transitive אחד (`uuid` GHSA-w5hq-g745-h8pq "missing buffer bounds check when buf provided") — exceljs קורא ל-uuid **בלי** ארגומנט buf ⇒ **לא-ניצול**; ה"תיקון" של npm = downgrade ל-`exceljs@3.4.0` (breaking) **נדחה**. **⚠️ הסתייגות CSP לגל ה-enforce**: ה-browser-bundle של exceljs (`browser`=`dist/exceljs.min.js`) מכיל **8× `Function("...")`** (מחלקת `unsafe-eval`; **אין** `eval`/`new Function`/Worker/blob ישירים). תחת ה-CSP הנוכחי (**Report-Only**, `script-src 'self' 'unsafe-inline'` בלי `'unsafe-eval'`) **לא שובר כלום** — רק would-be-reports. במעבר ל-CSP אוכף יש להוסיף `'unsafe-eval'` **או** להעביר את עבודת ה-Excel בצד-לקוח (תצוגה מקדימה ב-`Step1Upload` + ייצוא) לשרת. **פרסור הייבוא בשרת (Node) חסין-CSP.** **QA**: `npm run build` נקי (אחרי 3 תיקוני-טיפוס איטרטיביים: `NonSharedBuffer` של Node מול ה-`Buffer extends ArrayBuffer` של exceljs → `toArrayBuffer`; cast של union ה-`CellValue` דרך `unknown`) → **`npm run deploy` כיחידה אחת** (PID 2013469, BUILD_ID `6M__iLM4HeamkABgviF_3`). **Smoke**: root 307→/login, /login 200, chunk 200 (אין ChunkLoadError), journal נקי. **ייבוא חייבים אמיתי** (POST `/api/debtors/import` mode=merge, קובץ exceljs): status=success, total_rows=3, **skipped=1** (שורת A-ריקה), `monthly_debt='12,11,10'` (טקסט נשמר), C ריק→`phone_owner` NULL, `"1,234"`→`total_debt=1234` (numeric) — ואז נוקה. **E2E אנשי-קשר** (`scripts/test-contacts-import.sh`, מומר ל-exceljs): **14/14** (empty-לא-דרס, '000000000'→null, כפילות→update, no-apt→skip). **ייצוא**: round-trip של exceljs אומת תקין (גיליון 'חייבים', עברית לא-הפוכה, מספרים numeric, רוחב עמודות, קובץ "Microsoft Excel 2007+") — **הורדת ה-Blob בדפדפן ממתינה לאימות ידני של המשתמש** (פתיחה באקסל). **השלמת גל 5 (23/06/2026, deploy שני):** אבחון לפני commit חשף ש**תמיכת `.xls` לא הוסרה לגמרי** — `contacts/contact-import-panel.tsx` עדיין הזמין `.xls` (`accept=".xlsx,.xls"`, `isExcel` קיבל `.xls`, הודעת "בחר .xlsx או .xls"), כך שבחירת `.xls` הגיעה ל-route ש**הדליף את הקוד הגולמי `invalid_file_type`** ישירות ל-Alert. תוקן ל-`.xlsx` בלבד (יושר ל-`Step1Upload`). בנוסף — **מיפוי שגיאות-לעברית מאוחד**: helper חדש `src/lib/excel/import-errors.ts` (`importErrorFromCode` לקודי-שרת + `importErrorFromThrown` לשגיאות parse של exceljs/JSZip — "Can't find end of central directory…"→"הקובץ אינו קובץ Excel תקין (.xlsx)…", הודעות עברית-קיימות עוברות as-is) מחובר ב**כל שלושת המסלולים**: `Step1Upload` (preview בלקוח — קודם זרק JSZip גולמי), `Step3PreviewProgress` (route-POST + run `error_message`), ו-`contact-import-panel` (POST + `error_summary`). 3 הערות מיושנות תוקנו (`workbook.ts` blankrows, `contacts/import/route.ts` xlsx/.xls, `test-contacts-import.sh` SheetJS). **QA**: `tsc --noEmit` נקי → `npm run deploy` (PID 413002, BUILD_ID `OwswseonlPcesx55cYGku`); smoke: root 307→/login, /login 200, chunk 200, journal נקי, `.xls` נדחה חי (400 `invalid_file_type`, 0 runs — ה-backstop בשרת עובד), debtors parse מבודד 15/15 + contacts E2E 14/14. **לא בוצע commit/push** (ממתין ל„תדחוף”). ראה `SECURITY_AUDIT.md`, [[build-breaks-live-server]], [[project-bllink-sync-architecture]].
- 21/06/2026 — **משימות מחזוריות: אבחון + תיקון שורש + אינדיקטור + טאב „מחזוריות” (working-tree, טרם נדחף).** Audit (read-only, 4 subagents + בדיקת DB) קבע שהמנוע **לא** שבור: `computeOccurrences` (`recurrence/engine.ts`, 12 טסטים עוברים) + `materializeDue` (`recurrence/materialize.ts`) + ה-wiring ב-POST/PATCH/`materializeAllActive` + **migration 053 מיושמת ב-DB** — הכל תקין. **הבאג השורשי (שוחזר על נתונים אמיתיים):** ניתן היה להפעיל מחזוריות **בלי תאריך-יעד**; ה-anchor = `coalesce(occurrence_date, due_date)` → `null` → `materializeDue` יוצא ב-0 מופעים → סדרה מתה, בלתי-נראית בכל מקום (היה template אחד „גנרטור” שבועי ב׳/ה׳×5 עם 0 מופעים). היומן **לא** חוסם מופעים (אין פילטר recurrence ב-`listTasksWithDueDateInRange`) — פשוט לא היו מופעים. **התיקון (decision: דרוש תאריך-יעד — אופציה א'):** (א) **guard דו-שכבתי** — ה-API (`POST /api/tasks`+`PATCH /api/tasks/[id]`) דוחה `recurrence.rule` ללא due_date אפקטיבי (`error:'recurrence_requires_due_date'`); הטופס (`task-form-panel.tsx`) חוסם שמירה עם שגיאת inline אדומה על שדה התאריך + אסטריקס-חובה כשמחזוריות דולקת (ה-hint הענבר ב-`RecurrenceSection` כבר היה קיים). (ב) **migration 055** `055_backfill_recurrence_anchor.sql` (additive DML, idempotent, הורץ ב-`psql $DIRECT_URL`): עיגן כל template פעיל חסר-תאריך ל-`(created_at AT TIME ZONE 'Asia/Jerusalem')::date` כ-`occurrence_date` בלבד (ה-template נשאר חסר due_date מדעת → רק המופעים הממומשים נושאים due_date ומופיעים ביומן); **תוקנה שורה אחת** („גנרטור” → עוגן ל-21/06, ייצר ב׳/ה׳×5 מ-22/06). (ג) **אינדיקטור** (DESIGN §30) — `RecurringBadge` משותף (אייקון `Repeat` slate) בטבלה+קנבן+כותרת-פאנל; ביומן `CalendarOverlayItem.recurring` חדש + `listTasksWithDueDateInRange` מחזיר `recurring=(recurrence_id is not null)` + `calendar-views` מדליק את ה-`Repeat` בצ'יפ (קודם נדלק רק לאירועים). (ד) **טאב „מחזוריות”** — `listRecurringSeries()` חדש (`db/tasks.ts`, מחשב מופע-הבא דרך `computeOccurrences` המשותף, אופק 400 ימים, מכבד exceptions, ממוין עולה), endpoint רענון `GET /api/tasks/recurring`, רכיב `RecurringSeriesList`, טאב שלישי ב-`tasks-page-client` (toolbar מוסתר בו), לחיצה→פתיחת ה-template. **recurrence נשאר tasks-scoped** (לא לתקלות). אפס שכפול: שימוש חוזר במנוע, ב-date-picker הקיים, ב-`Section`/`Table`. `tsc --noEmit` נקי. **לא בוצע commit/push** (ממתין ל„תדחוף”). ראה [[project-tasks-module-built]], [[project-two-reminders-systems]].
- 21/06/2026 — **יומן: רידיזיין 4 מסכים + 4 מקורות פריטים + תאריך-יעד לתקלות (working-tree, טרם נדחף).** היומן עוצב מחדש 1:1 עם `ref/` (חודש/שבוע/יום/טופס אירוע) ו**הורחב מ-2 ל-4 סוגי פריטים** המוצגים על אותו לוח, נבדלים **בצבע בלבד** (`chipTone` ב-`constants/calendar.ts`; DESIGN.md §29): אירוע (color_key), משימה (ירוק `#dcfce7/#22c55e/#15803d`), תקלה (אדום `#fee2e2/#ef4444/#b91c1c`), תזכורת (אפור `#f1f5f9/#94a3b8/#475569`). **שינוי data ב-`GET /api/calendar`** (מותר ומתוכנן): ממזג 4 מקורות עם פילטר מועד בצד-שרת — events כרגיל, `listTasksWithDueDateInRange` (קיים), `listIssuesWithDueDateInRange` (**חדש**, רק `open`/`in_progress` עם `due_date`), ו-`listUserRemindersInRange` (**חדש**, רק `status='pending'` + רק של ה-actor `created_by/assigned_to`, מגודר `user_reminders:view`; המרה ל-Asia/Jerusalem). **(A) תאריך-יעד לתקלות** — **migration 054** `054_issues_due_date.sql` (additive: `issues.due_date date` + `due_time time` + partial index, הורץ ב-`psql $DIRECT_URL`); נוסף ל-`types/issues`, `validation/issues` (DATE_RE/TIME_RE), `db/issues` (ISSUE_COLUMNS+WRITABLE_COLUMNS) — ה-routes מעבירים `result.fields` כמו-שהוא ולכן **לא נדרש שינוי route**. **(B) טופס התקלות = טופס המשימות** — הטופס כבר היה ~90% זהה (אותו `Sheet`+`Section`+`AssigneeSplitFields`+`RemindersSection`+`NotifyMatrix`); הפער היחיד שנסגר = צמד `due_date`/`due_time` (grid 2-עמודות ב-"פרטי התקלה", שיקוף 1:1 של task-form). **recurrence לא שוכפל לתקלות** (תקלות אינן חוזרות; ה-recurrence של tasks הוא feature נפרד uncommitted, tasks-scoped ב-DB). **(C) תצוגות** — שבוע+יום נבנו כ**גריד שעתי 08:00–20:00** (החלטת משתמש: שעות עבודה, חריג מ-ref 07:00; פריט מחוץ לחלון עובר **clamp** לשורת הקצה, לא מוסתר); חודש עם עיגול "היום" + תא `#f5f9ff`. **(D) טופס אירוע** — יישור פיקסלי ל-ref: gradient כהה `120deg`, שדות **44px (`h-11`)** (חריג מוצהר ב-DESIGN.md — חל רק על טופס היומן), בורר צבע `34px` + `ring-2 ring-[#2563eb] ring-offset-2`. ניקוי: `eventBlockClasses`/`eventDotClass`/`COLOR_MAP` (יתומים אחרי השכתוב) נמחקו. **לא בוצע commit/push/deploy** — ממתין לאימות ויזואלי. ראה [[project-tasks-module-built]], [[project-two-reminders-systems]], [[project-multi-assignee-junction]].
- 20/06/2026 — **שיוך שיחות וואטסאפ לספקים (XOR עם דייר) (commit `3574b2d`).** שיחה ב-`chat_messages` משויכת ל**דירה** (`debtor_id`), ל**ספק** (`supplier_id` — **migration 051** `051_chat_messages_supplier.sql`, `uuid` FK→`suppliers(id)` **ON DELETE SET NULL** + index), או **לא-משויכת** — אף פעם לא לשניים. **סוג השיוך נגזר** מאיזה שדה מאוכלס (אין עמודת `type`); ה-XOR נאכף במקור-אמת יחיד `src/lib/whatsapp-link.ts` (`normalizeChatLink` — שיוך לספק מנקה `debtor_id` ולהפך), שבו משתמש `setConversationLink` ב-`db/chatMessages.ts` שמעדכן את **כל ההודעות מאותו `chat_id`/`contact_phone`** (initial-link / re-link / unlink). **יצירת ספק מתוך השיחה**: `POST /api/whatsapp/messages/[id]/create-supplier` (דורש **suppliers:edit + whatsapp:edit**) — טופס מינימלי שם-חובה + טלפון אוטומטי מ-`contact_phone` (נוקה דרך `cleanPhoneField`, נשמר ב-`mobile`) + קטגוריה אופציונלית, ואז שיוך מיידי; בורר ספק קיים דרך `GET /api/suppliers/search` (מגודר **whatsapp:edit** בלבד, כמו `/api/debtors/search`). **הצלבה אוטומטית בנכנס/יוצא** (`processIncomingMessage`/`processOutgoingMessage` ב-`whatsapp-inbound.ts`): מספר לא-מזוהה נבדק מול `suppliers.phone`+`mobile` ב-`findSupplierIdByPhone` (נרמול **last-9 עקבי** עם הצלבת הדייר), **סדר עדיפות דייר→ספק→unlinked**. `listConversations`/`listThread` עושות LEFT JOIN ל-`suppliers`→`supplier_display_name`. **תצוגה**: שם הספק כ**כותרת** + תווית/אייקון **ענבר** (`Wrench`, "ספק") שמבדיל מדירה, ב-`ConversationList` וב-`ChatThread`; כפתורי כותרת `שייך לדירה`/`שייך לספק`/`נתק` + `LinkSupplierDialog` (בחירה/יצירה). **תיקון באג שנכלל באותו commit**: שיחות-ספק הוצגו כ"לא משויך"+טלפון כי שכבת התצוגה (`conversationTitle`, `ConversationList.isUnlinked`) התעלמה מ-`supplier_id`/`supplier_display_name` — ה-backend (שיוך+JOIN) היה תקין; אומת שאין שיחה לא-משויכת שמספרה תואם ספק קיים, לכן **לא נדרש backfill**. טסטים: `tests/chat-link.test.ts` (אכיפת XOR + ניקוי טלפון ביצירה-מהשיחה). ראה [[project-whatsapp-module]], [[project-suppliers-and-infra-gaps]].
- 20/06/2026 — **רידיזיין מסך הלוגין (working-tree, טרם נכנס ל-commit נכון לתאריך זה).** מסך הכניסה עוצב מחדש ככרטיס מעוגל responsive: רכיב חדש `src/components/auth/LoginBrandPanel.tsx` (פאנל מיתוג — גרדיאנט כחול כהה, זהות ALMOG, 3 נקודות ערך) לצד הטופס; `src/app/(auth)/login/page.tsx` החליף את `AuthLayout` בפריסת הכרטיס; `LoginForm.tsx` + `GoogleButton.tsx` — **styling/markup בלבד**. **לוגיקת ה-auth ו-ה-OAuth endpoint (`/api/auth/google/start`) לא נגעו.** תואם `DESIGN.md`, נבדק ידנית. (השינוי הזה הוא שמוליד את התקדים התהליכי ברשומה הבאה.) ראה [[project-google-oauth-module]].
- 20/06/2026 — **תקדים תהליכי — קבצים staged שמתגלגלים בין סבבים = דגל אדום.** קבצי working-tree שנשארים ב-staging בין משימות עלולים להיכנס בטעות לדחיפה הבאה. הפרוטוקול: לפני כל `push` להריץ `git log origin/main..HEAD` + `git status`, לבדוק `diff`, ולהכריע במפורש לכל קובץ לא-קשור — **commit נפרד אחרי אימות / `stash` / `git restore --staged`** — לפני שהוא נכנס בטעות לדחיפה. במיוחד **קבצי auth/login**: **אימות דפדפן חובה לפני commit** כי שבירת מסך הכניסה = נעילה בחוץ. (התקדים עלה בסבב שיוך-הספקים, שבו קבצי הרידיזיין של הלוגין היו staged-uncommitted ונשמרו במכוון מחוץ לדחיפה.) ראה [[build-breaks-live-server]].
- 20/06/2026 — **בהורייזון (Wave 4+, עדכון).** (1) **צ'אט פנימי** — הטיפוס `chat_message` נשאר placeholder ב-notifications registry ללא מפיק; (2) **time-grid ללוח שנה** — prompt ב-`prompts/calendar-timegrid.md`; (3) **סנכרון תמונות פרופיל WhatsApp** — קוסמטי.
- 19/06/2026 — **Auth = password-first; Google הפך לתוסף אופציונלי לכל-משתמש (commit `dab738c`).** מערכת הכניסה הראשית היא **username+password**, לא OAuth. `users.password_hash text **NOT NULL**` קיים מאז **migration 001** (לא נוסף עכשיו) — כל משתמש חייב סיסמה. אימות דרך **bcryptjs** ב-`src/lib/auth/password.ts` (`hashPassword`/`verifyPassword`, `ROUNDS=10`, `import 'server-only'`); מדיניות הסיסמה ב-`src/lib/auth/passwordPolicy.ts` — **`PASSWORD_MIN_LENGTH=8` + לפחות אות אחת (כולל בלוק עברית U+0590–U+05FF) + לפחות ספרה אחת** (`length/letter/digit` booleans). **`username = email`** (אין שדה username נפרד). **Google = תוסף opt-in פר-משתמש**: העמודה `allow_google_auth boolean not null default false` נוספה ב-**migration 036** (`036_users_allow_google_auth.sql`) — ברירת המחדל **false**, ה-callback (`src/app/api/auth/google/callback`) חוסם server-side כל ניסיון התחברות-גוגל למשתמש שלא הופעל לו הדגל ידנית ע"י admin. רכיב `PasswordField.tsx` (`src/components/auth/PasswordField.tsx`) משותף בין מסכי ה-set/reset (toggle הצגה, חיווי חוקי-מדיניות חי). **ניהול עובדים ע"י admin** (אותו commit): `POST /api/users` עם סיסמה → **יצירת עובד מיידית פעילה בלי מייל-הזמנה** (עוקף את זרימת ה-`user_invites`); `PATCH /api/users/[id]` עם סיסמה → **reset + ניתוק כל הסשנים של אותו משתמש בלבד** (לא משתמשים אחרים) כדי לכפות login מחדש; נוסף גם שדה `notification_phone` הניתן לקביעה ע"י admin. ראה [[project-google-oauth-module]], [[project-rbac-architecture]].
- 19/06/2026 — **משימות/תקלות — מעבר ל-multi-assignee (junction `entity_assignees`, migration 047) + מטריצת התראות פר-נמען (commit `f006c95`).** **(א) Multi-assignee** — שיוך המטפל הפך מ-**יחיד** (`AssigneeField`, מודל user-XOR-supplier) ל-**רבים-לרבים פולימורפי**: junction `entity_assignees` (**migration 047**, `entity_type`='task'/'issue' + `entity_id` + `assignee_type`='user'/'supplier' + `assignee_id`) דרך `src/lib/db/entityAssignees.ts`. ה-UI הוא `AssigneeMultiSelect` (`src/components/assignee/`) + `AssigneePills`; הטופס מחזיק `form.assignees: AssigneeInput[]` (תערובת עובדים+ספקים). **העמודות הסקלריות הישנות `assigned_to_user_id`/`supplier_id` (בשתי הטבלאות) הוקפאו** — אפס read חי בקוד האפליקציה (נשארות עד DROP ב-Phase B, ראו רשומת ה-WIP למטה); פילטר ה-`supplier` עבר ל-`assigneeExistsSql` על ה-junction. ראה [[project-multi-assignee-junction]]. **(ב) NotifyMatrix** (`src/components/notify/NotifyMatrix.tsx`) — בעת יצירת משימה/תקלה, מטריצת **נמען × ערוץ** (מייל / וואטסאפ): כל שורה = נמען (`NotifyRecipient`, מפתח `'me'`/`'user:<id>'`/`'supplier:<id>'`), כל תא = checkbox ערוץ. **ברירת מחדל = כלום** (`EMPTY_NOTIFY_SELECTION = {}` ב-`src/lib/notify/selection.ts`) — שום התראה לא נשלחת אלא אם נבחרה במפורש. **תא של ערוץ ללא פרט-קשר מנוטרל** (נמען בלי email → תא המייל disabled עם title "אין אימייל מעודכן ל…"; בלי טלפון → תא הוואטסאפ disabled). השליחה דרך `dispatchCreateNotifications` ב-`src/services/createNotify.ts` שמפענח מחדש את פרט-הקשר בשרת (guard מפני payload מזויף שמכוון נמען בלי email/phone). **התראת ה-in-app נפרדת** ממטריצת המייל/וואטסאפ (נשלחת דרך מנגנון ה-notifications הקיים, לא דרך המטריצה). ראה [[project-notifications-module]].
- 19/06/2026 — **RBAC — צופה (viewer) צומצם למסך חייבים לקריאה-בלבד (commit `066aca7`).** `DEFAULT_VIEWER` (`src/lib/permissions/constants.ts`) שונה ל-**`dashboard:view` בלבד** (`perm('dashboard', true, false)`) — כל שאר המודולים `noPerm` (כולל `contacts` ו-`status_management`). צופה רואה **רק את מסך החייבים** (`/dashboard` "ניהול חיובים") לקריאה: הטבלה, ה-KPIs, טאבי הסטטוס (מכתב-התראה/משפטי), המצב המשפטי וההיסטוריה — **בלי כתיבה, בלי ייצוא, בלי התראות**. כדי שצופה יקרא את מסך החייבים בלי להחזיק `contacts`/`status_management`, קריאות ה-data של המסך עברו ל-**`requireAnyPermission(...)`** (OR בין `dashboard` ל-`contacts`/`status_management`) ב-`/api/debtors/*` וב-`/api/statuses` (`src/lib/auth/actor.ts`). **`requireNotificationsAccess()`** (actor.ts) נוסף ו-**fail-closed לצופה** — כל route ההתראות (`/api/notifications/*`) דוחה viewer. `tests/permissions.test.ts` הורחב (+19 שורות) לאימות הגזרה. ראה [[project-rbac-architecture]].
- 19/06/2026 — **דפוס תשתיתי ידוע — כל `npm run deploy` מייצר ~90ש' של 502 (אינו באג).** ה-restart של `billing.service` שולח SIGTERM, אך התהליך **נתקע על חיבורי ה-SSE הפתוחים** (stream של WhatsApp/notifications) ולא יוצא מיד; systemd ממתין עד `TimeoutStopSec` ואז שולח SIGKILL — ובחלון הזה nginx מחזיר 502 לכל בקשה. הדפוס צפוי ו**חולף לבדו** אחרי שהתהליך החדש עולה (כתיבת `.next/BUILD_ID` + `active (running)`). **אבחון לפני האשמת קוד**: 502 מיד אחרי deploy → לבדוק `systemctl status billing.service` (האם עדיין ב-`deactivating`/`activating`) **לפני** חיפוש רגרסיה בקוד; להמתין ~90ש' ולבדוק שוב. ראה [[build-breaks-live-server]].
- 19/06/2026 — **WIP לא מגובה על השרת (uncommitted נכון ל-19/06/2026) — שתי פרוסות.** מצב working-tree עם שינויים שטרם נדחפו (אם מבצעים `/git` הם ייכנסו; מתועד כאן כרשומת-מצב): **(א) מטריצת התראות בעריכה (edit-form matrix)** — הרחבת ה-NotifyMatrix גם לזרימת **העריכה** (לא רק יצירה): בעדכון משימה/תקלה המטריצה מציגה **רק את הנמענים שנוספו** (added set מול ה-assignees שהיו פתוחים בכניסה לפאנל), כדי לא לשלוח התראה חוזרת לנמענים קיימים. קבצים: `src/components/{tasks/task-form-panel,issues/issue-form-panel}.tsx`, `src/app/api/{tasks,issues}/[id]/route.ts`, `src/services/createNotify.ts` (+`tests/createNotify.test.ts`), `src/lib/db/entityAssignees.ts`. **(ב) Legacy Phase A — הצגת ה-target (חדר/אזור) בטבלאות** (`LEGACY_CLEANUP_AUDIT.md`): פתרון ה-`target` של migration 040 מוצג כעת בטבלאות משימות/תקלות ובקנבן דרך `TargetCell` משותף (`src/components/targets/TargetCell.tsx`, חדש) — ה-label נפתר **בשאילתת ה-list** (subquery ל-`debtors`/`areas`), מה שמסיר את הקריאה של העמודות ה-legacy מהטבלאות. קבצים: `src/components/{tasks/tasks-table,tasks-kanban,issues/issues-table}.tsx`, `src/lib/db/{tasks,issues,targets}.ts`, `src/lib/types/{tasks,issues}.ts`. **Phase A = תצוגה בלבד (בטוח). Phase B = DROP של 5 העמודות ה-legacy** (`tasks.apartment_number`/`assigned_to_user_id`/`supplier_id`, `issues.location_type`/`location_text`/`assigned_to_user_id`/`supplier_id`) — **טרם הורץ, ממתין לאישור מפורש** של המשתמש אחרי אימות ויזואלי ("תריץ שלב B"). אין read חי שחוסם את ה-DROP אחרי Phase A.
- 16/06/2026 - **אבטחה — גל תיקון 3: security headers ב-next.config (CSP כ-Report-Only כשלב ראשון) + זנב ניקיון גל 2.** (1) **`next.config.ts` — `async headers()` חדש** על כל הנתיבים (`source:'/:path*'`, ממוזג עם `output:'standalone'` הקיים): `Strict-Transport-Security: max-age=31536000` (**שמרני — בלי `includeSubDomains`/`preload`** בכוונה, כדי לא לכלוא תת-דומיינים/להיכנס ל-preload-list לפני בדיקה), `X-Frame-Options: SAMEORIGIN`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy: camera=(), microphone=(), geolocation=()`, ו-**`Content-Security-Policy-Report-Only`** (לא אוכף!) `default-src 'self'; img-src 'self' data: blob: https:; style-src 'self' 'unsafe-inline'; script-src 'self' 'unsafe-inline'; connect-src 'self' https:; font-src 'self' data:; frame-ancestors 'self'`. **ה-CSP מכוון Report-Only בלבד בשלב זה** — מתעד הפרות ל-console בלי לחסום, כדי לאסוף את מקורות ה-inline/external האמיתיים לפני מעבר ל-CSP אוכף. **ללא `report-uri`/`report-to`** (איסוף ידני מה-console). **enforce נדחה לגל נפרד** אחרי ניתוח ה-Report-Only. (2) **זנב גל 2**: `src/lib/db/greenApiSettings.ts` נמחק (git rm) — הפך **יתום מלא** אחרי מחיקת הכרטיס+routes בגל 2 (grep רחב: 0 importers, 0 שימושי-סימבול); `src/types/whatsapp.ts` — **תגובות תיעוד בלבד** (לא קוד) עודכנו: ההפניות ל-routes שנמחקו (`/api/whatsapp/webhook`, `/api/settings/green-api/*`) הוחלפו ל-route הקנוני `/api/webhooks/greenapi` + הערה שה-instances מנוהלים דרך `/api/whatsapp/instances/*`. **QA**: `npm run build` נקי → **`npm run deploy` כיחידה אחת** (PID 2737620, BUILD_ID `mwHyizK-yykYEeNQp1Wyv`, NRestarts=0). **Smoke**: root 307→/login, /login 200, chunks 200 (אין ChunkLoadError), journal נקי; **`curl -sI` אישר שש כותרות בערכים המדויקים**, ה-CSP הוא **דווקא ה-Report-Only variant**, ו**אין** `Content-Security-Policy` אוכף (שום דריסה). **איסוף הפרות CSP-Report-Only**: דף ה-login → **0 הפרות** (console ריק; 8 ה-`<script>` ה-inline של Next מותרים ע"י `script-src 'unsafe-inline'`). דפים מאומתים לא נגישים בלי credentials → ניתוח סטטי לגל ה-enforce: המעבר ל-CSP אוכף (הסרת `'unsafe-inline'`/הוספת nonce) יחייב טיפול ב-inline scripts של Next (nonce/`strict-dynamic`), ב-inline styles (next/font/Tailwind), ובאפשרות `eval`/`blob:` workers מ-jspdf/xlsx (script-src נוכחי בלי `'unsafe-eval'`/`blob:`). לא בוצע force-push.
- 16/06/2026 - **אבטחה — גל תיקון 2: ניקיון dead-code + תלות חסרה מפורשת + ארכוב BASE44 (כל מחיקה מותנית באימות אפס-imports).** בהמשך ל-`SECURITY_AUDIT.md` חלקים ב'/ג'/ד'. (1) **`@supabase/storage-js` מפורש** — היה phantom dependency (מיובא ב-4 קבצי storage דרך transitive של `@supabase/supabase-js`); נקבע מפורשות ב-`dependencies` בגרסה ה-resolved `^2.108.1` (אפס שינוי resolution/גרסה בפועל). (2) **מחיקת 11 קבצים מתים** (git rm, אימות grep רחב לכל קובץ — import/dynamic/require/JSX/string — לפני כל מחיקה): 4 stubs פאזה-א' `src/services/{bllink,gmail,make,whatsapp}.ts` (0 importers); `src/lib/db/legalStatusHistory.ts` — **כפיל יתום**, הפיצ'ר החי של legal-status-history מסופק ע"י `db/debtorHistory.ts` (קריאה) + `db/debtors.ts:483` (כתיבה ל-`legal_status_history`), לא ע"י קובץ זה; `src/components/settings/GreenApiSettingsCard.tsx` — כרטיס Green API ישן (חשבון-יחיד) **שאינו מרונדר** (0 importers), ה-UI החי הוא `settings/components/WhatsAppConnections.tsx` (מודל per-instance); **אשכול legacy §158** — שלושת ה-routes `src/app/api/settings/green-api/{route,test,webhook}.ts` (הקורא היחיד שלהם היה הכרטיס המת) + ה-webhook הישן `src/app/api/whatsapp/webhook/route.ts` (Bearer; ה-registration החי מצביע ל-`/api/webhooks/greenapi` הקנוני דרך `greenWebhookUrl()`); ו-`RUN_SUMMARY.md` (scratch). **דולגו** (סיווג ARCHIVE/dual, לא DELETE טהור): `scripts/clean-phone-fields.ts`, `scripts/test-upsert-empty.ts`. **build artifacts**: אין tracked (N/A). (3) **ארכוב** `BASE44_REFERENCE.md` → `docs/archive/BASE44_REFERENCE.md` (git mv, תוכן ללא שינוי; הקובץ נשאר tracked תחת הנתיב החדש; עודכנו 3 הפניות ב-PROJECT_CONTEXT.md). **שארית ידועה (לא חוסם)**: הערות-תיעוד מיושנות ב-`src/types/whatsapp.ts` מצביעות ל-routes שנמחקו (תגובות בלבד, לא קוד); `db/greenApiSettings.ts` הפך יתום (לא נמחק — מחוץ להיקף). **QA**: `npm run build` נקי → **`npm run deploy` כיחידה אחת** (PID 2479906, BUILD_ID `ql9vTB-7Orbbg52_GvTLf`, NRestarts=0); smoke: core 200/307 + chunks (אין ChunkLoadError); **אימות פיצ'רים שנגעו בשמות** — backends חיים מחזירים 401 (handler נטען, אין broken-import): `whatsapp/instances` (Green API), `debtors/<uuid>/history` (legal-status-history), `webhooks/greenapi`; ה-routes שנמחקו מחזירים 404; journal נקי. לא בוצע force-push.
- 16/06/2026 - **אבטחה — גל תיקון 1: עדכון תלויות runtime + מחיקת קובץ סוד-גיבוי (שינוי תלויות בלבד, אפס שינוי קוד).** בעקבות `SECURITY_AUDIT.md`: (1) **`next` 16.2.4→16.2.9** (pin מדויק, לא-שובר) — סוגר את ה-CVEs הישירים בדרגת **high** (DoS ב-RSC/Image/Cache, Middleware/Proxy bypass, XSS ב-CSP-nonces/beforeInteractive, SSRF ב-WS upgrades, cache-poisoning). (2) **`nodemailer` 8.0.6→8.0.11** (נשאר ב-major 8; `latest=9.0.0` major **לא** נלקח) — סוגר CRLF header-injection + TLS-cert-validation. (3) **מחיקת `.env.local.save`** — עותק-צל plaintext של סודות על הדיסק (היה gitignored, לא tracked); `.env.local` ו-`/etc/billing/billing.env` לא נגעו. `.gitignore` הורחב לדפוסי גיבוי-עורך (`*.env.bak`/`*.env.save`/`*.swp`/`*~`/`*.bak`/`*.save`/`*.orig`) מעבר ל-`.env*` הקיים. **`npm audit`: 18→17.** **לא נגעתי ב-`xlsx`** (Prototype-Pollution/ReDoS — אין npm-fix; גל נפרד למעבר `exceljs`) ולא בשאר ה-toolchain. **שארית צפויה:** `xlsx` + dev/build toolchain (`vitest`/`vite`/`esbuild`/`tsx`/`postcss`); הערך `next` שעדיין מופיע ב-audit הוא **moderate transitive בלבד** (`via:["postcss"]` — לא פגיעות runtime של Next; ה-CVEs הישירים של next נסגרו). **QA**: `npm run build` נקי → **`npm run deploy` כיחידה אחת** (PID 2401433, BUILD_ID `qZMEcQkeDNPQ5_O99on3N`, Next.js 16.2.9, NRestarts=0); smoke: root 307→/login, /login 200, כל ה-chunks 200 (אין ChunkLoadError), journal נקי. לא בוצע force-push.
- 16/06/2026 - **התראות — מחיקת שורה בודדת (פח) + חיווי "נקרא" בלחיצה.** כל שורת התראה (בפעמון ובטבלת `/notifications`) קיבלה כפתור **פח** (`Trash2`) למחיקה בודדת. עקבי עם "נקה הכל" — זה **soft-clear** של שורה אחת ולא מחיקה קשה: db חדש `clearNotification(id,userId)` (`cleared_at=now()` scoped לבעלים, WHERE-enforced) + route חדש `PATCH /api/notifications/[id]/clear` (404 אם לא הבעלים/כבר נוקתה, מחזיר `unreadCount`). מקביל ל-`[id]/read` הקיים. UI: בפעמון השורה הומרה מ-`<button>` יחיד ל-`<li>` flex עם **שני אחים** (אסור button-בתוך-button) — כפתור-תוכן ראשי + כפתור פח (`w-11` touch-target, `text-slate-300`→`rose-600` ב-hover, גוון danger §2); ה-hover/`bg-blue-50/40` עברו ל-`<li>` (`group`). בטבלה נוספה עמודת "מחיקה" אחרונה (`w-16`) עם כפתור פח (`stopPropagation` כדי לא להפעיל בחירת שורה) + toast "ההתראה נמחקה". שתי המחיקות **אופטימיות** (השורה נעלמת מיד) ומסנכרנות badge מה-`unreadCount` שחוזר; בטבלה כשל → `fetchPage` משחזר. **חיווי נקרא**: לחיצה על התראה כבר מסמנת `is_read=true`; שופר כך שכשאין `action_url` הפאנל **נשאר פתוח** והשורה נדלקת מעומעמת במקום (כשיש url — ניווט+סגירה כמקודם). DESIGN.md §10 עודכן (דפוס שורה + פאנל). לא נגעתי ב-`createNotification`/`insertNotification`/בסכמה. ראה [[project-notifications-module]].
- 16/06/2026 - **התראות — השלמת מפיקים חסרים + יישור registry (additive על המודול החי).** אבחון read-only מצא ש-3 טיפוסים רשומים ב-registry אך ללא מפיק (לא נוצרה אף התראה), ושתי סטיות. תוקן: (1) **`issue_reported` (מפיק חדש)** — ב-`POST /api/issues` אחרי `createIssue` מוצלח, fire-and-forget (`void`, לא בתוך transaction) של fan-out מסוג `issue_reported` לכל `listActiveAdmins()` **פרט ליוצר** (helper `notifyAdminsOfIssueReported`); priority `high`, channels מה-registry (inapp+email+whatsapp מגודר), `sourceEntityType:'Issue'`, dedupeKey `issue_reported:${issue.id}:${admin.id}`. משלים את `issue_assigned` הקיים ("הוקצתה אליך" לאחראי בלבד). (2) **`task_due_soon` (מפיק מתוזמן חדש)** — `runReminders()` (ה-cron `billing-reminders`, כל 5 דק') מריץ כעת `scanTasksDueSoon()`: שאילתת `listTasksDueSoon(from,to)` ב-`db/tasks.ts` בוחרת משימות לא-בארכיון בסטטוס `open/in_progress` עם `due_date` בטווח **היום+מחר לפי Asia/Jerusalem** (= אופק 24ש' בגרניולריות תאריך). נמען = ה-assignee; אם אין → fallback ל-`listActiveAdmins()`. priority `high`, `sourceEntityType:'Task'`, dedupeKey יומי `task_due_soon:${task.id}:${userId}:${todayJ}` → התראה אחת ליום למשימה (ריצה חוזרת כל 5 דק' לא מכפילה). הסריקה עטופה ב-try/catch נפרד כך שכשל בה לא שובר את לולאת התזכורות (ולהפך). נוסף שדה `dueSoon` ל-`ReminderRunResult` (נראוּת בפלט ה-cron). (3) **`calendar_reminder` (יישור טיפוס)** — `reminders/engine.ts` כתב type גנרי `'reminder'` לכל סוגי האירועים; כעת ל-`entity_type='calendar_event'` הוא מפיק `'calendar_reminder'` (אייקון/tone מה-registry), ושאר הסוגים (task/issue) נשארים `'reminder'` ללא שינוי. אותו פרימיטיב insert ואותה טרנזקציה. (4) **`internal_chat_message` (יישור registry)** — המפיק החי (`internalChat.ts:413`, דרך ה-insert הגולמי) כתב טיפוס שהיה קיים רק כ-`LEGACY_VISUAL`; נרשם כעת כטיפוס מן-המניין ב-`registry.ts` (icon `MessageCircle`/tone `default`/sourceModule `internal_chat`/priority `low`/channels `['inapp']`) והוסר מ-`LEGACY_VISUAL`. כך שירות ה-registry לא יזרוק "unknown type" אם המפיק יעבור אליו בעתיד. הטיפוס `chat_message` הרשום-אך-המת נשאר כ-placeholder עתידי (ללא מפיק). **המפיק לא שונה.** (5) **dedup WhatsApp → Asia/Jerusalem** — `whatsapp-inbound.ts` השתמש ב-`toISOString().slice(0,10)` (UTC), שפיצל/מיזג הודעות-ערב (21:00–24:00 שעון ישראל) סביב חצות-UTC; עבר ל-helper משותף חדש `todayInJerusalem()`. נוסף `src/lib/dates.ts` (`todayInJerusalem()` + `addDaysToIsoDate()`) ושימש גם בתיקון 2. **לא נגעתי** ב-`createNotification`/`insertNotification`/בפאנל/ב-API ההתראות/בסכמה (כולם עובדים), ו-`task_assigned` ללא assignee נשאר ללא התראה (תקין כמתוכנן). ראה [[project-notifications-module]].
- 16/06/2026 - **התראות — סמנטיקת "אופציה א'" (הסתרה מהרשימה הפעילה) + פאנל פעמון עם טאבים.** מיגרציה additive `034_notifications_clear.sql`: עמודת `cleared_at timestamptz` + partial index `notifications_user_active_idx` על `WHERE cleared_at IS NULL`. מודל: **"רשימה פעילה"** = `cleared_at IS NULL`; **"לא נקראו"** = `is_read=false AND cleared_at IS NULL`. שתי פעולות **נפרדות**: (1) **סימון נקרא** (לחיצה/"סמן הכל כנקרא") → `is_read=true` — השורה **נשארת** ברשימה הפעילה אך יוצאת מטאב "לא נקראו" (היסטוריה נשמרת, נצפית דרך פילטר read/unread בדף); (2) **"נקה הכל"** → `cleared_at=now()` — **soft-clear** שמסתיר מכל הטאבים (כולל "הכל") אך **השורות נשמרות ב-DB** לאודיט ול-dedup (אותו `dedupe_key` עדיין ON CONFLICT DO NOTHING). **אין hard delete בשום מסלול.** API: כל ה-routes מסננים `cleared_at IS NULL`; חדש `PATCH /clear-all` (`{cleared}`); `GET /?tab=all|unread|tasks|issues|calendar|whatsapp|internal_chat` מזין את הטאבים בפאנל. הפעמון (Popover `w-[380px]`, החריג המאושר ל-Side Panel) עוצב מחדש: כותרת + 2 פעולות עליונות + רצועת טאבים אופקית (badge unread על "לא נקראו") + רשימה (read מעומעם, ללא נקודה כחולה) + footer "צפה בכל ההתראות". דף `/notifications` קיבל כפתור "נקה הכל" לצד "סמן הכל כנקרא". ראה [[project-notifications-module]] ו-DESIGN.md §10 (Notification panel).
- 16/06/2026 - **מודול ההתראות (Notifications) הושלם.** שכבת תשתית per-user שמאגדת אירועים מכל המודולים לזרם אחד (פעמון Popover ב-Header + דף `/notifications`). Schema מיושר במיגרציה additive `033_notifications_fanout.sql`: הוספת `read_at` ל-`notifications` הקיים + `notification_phone`/`notify_email`(ברירת-מחדל true)/`notify_whatsapp`(ברירת-מחדל false) ל-`users` + index `notifications_user_unread_idx` (ה-dedupe unique וה-FK כבר קיימים מ-023, נוצרים רק אם חסרים). **Registry מרכזי** `src/lib/notifications/registry.ts` = מקור-אמת יחיד למיפוי `type → icon/tone/sourceModule/defaultPriority/channels` (9 טיפוסים); ה-UI מרנדר אך ורק דרך `getNotificationVisual()` (כולל fallback לטיפוסים legacy: `reminder`/`issue_status_changed`/`internal_chat_message`). **Fan-out** ב-`createNotification()` חדש ב-`services/notifications.ts` (נקודת קריאה יחידה לזרם המטופס): גוזר priority/channels/sourceModule מה-registry, INSERT ... ON CONFLICT(dedupe_key) DO NOTHING, ו**רק אם נוצרה שורה** מפעיל ערוצים — email **חי** (transporter קיים, `sendNotificationEmail`) + WhatsApp **מגודר** (`priority≥high` וגם `notification_phone` וגם opt-in `notify_whatsapp` → אף אחד לא מקבל WhatsApp עד הזנה ידנית). הפונקציה לעולם לא זורקת לקורא. `notifyTask`/`notifyIssue` נשארו עם תבנית המייל העשירה שלהם (משתמשים ב-`insertNotification` הפרימיטיבי). **`whatsapp_message_received` חי** דרך `processIncomingMessage` (ה-funnel של webhook+pull): אחרי insert אמיתי בלבד, fire-and-forget להתראת admins פעילים (super_admin+admin), dedup לפי **איש-קשר-ליום** (התראה אחת לכל חייב שהשיב/יום, לא לכל הודעה). `chat_message` נשאר כחוזה מוכן ב-registry ללא חיווט (מודול Internal Chat — Wave 4). **RBAC**: ההתראות אינן מודול ב-matrix — תשתית אישית, כל משתמש מאומת רואה את שלו בלבד (scoped ב-`actor.id`); כניסה לדף דרך footer הפעמון. ה-Popover הוא החריג המאושר למוסכמת ה-Side Panel.
- 12/06/2026 - **Wave 2 (ספקים) הושלם — `suppliers` הוא מודול הספקים היחיד והפעיל; `vendors` נבנה בטעות ונמחק במלואו**: מודול הספקים החי הוא `suppliers` (`display_name`, `category_id`→`supplier_categories`, `phone`+`mobile`, soft-delete דרך `deleted_at`, RBAC key `'suppliers'`; `db/suppliers.ts` + `db/supplierCategories.ts` + `/api/suppliers/*` + `components/suppliers/*`). מודול `vendors` מקביל ודומה-שם (`vendor_categories`+`vendors`) נבנה בטעות ונמחק במלואו: migration **042** הוסיף (טבלאות + seed קטגוריות + backfill הרשאות), ו-migration **043** הסיר אותו לחלוטין (`DROP TABLE` לשתי הטבלאות + מחיקת שורות `vendors` מ-`user_permissions`); כל ה-frontend/backend/RBAC/tests של vendors הוסרו (matrix חזר 21→20). **הלקח — audit-before-build**: לפני בניית מודול "חדש" בעל שם דומה לקיים, להריץ audit על הקיים תחילה. מודול חדש דומה-שם = דגל אדום לבדיקה.
- 12/06/2026 - **Wave 2 — תקלות (issues): מודול קיים end-to-end, הורחב בשיוך גורם מטפל**: המודול כבר קיים מקצה-לקצה (`issues` + `issue_comments`, `db/issues.ts`, `/api/issues/*`, `components/issues/issue-form-panel.tsx`) — לא נבנה מחדש. הורחב additively בשיוך גורם מטפל יחיד (ראה הערך הבא).
- 12/06/2026 - **שיוך גורם מטפל גנרי (user XOR supplier) — מודל הדדי + חילוץ לרכיבים משותפים**: תקלה/משימה מטופלת ע"י גורם **אחד בלבד** — `assigned_to_user_id` (עובד פנימי) או `supplier_id` (ספק), ולא שניהם. נאכף בשרת דרך `assertSingleAssignee` המשותף (reject-both + clear-counterpart, גם ב-PATCH חלקי). חולץ לרכיבים משותפים (כתקדים `TargetField`): `src/lib/types/assignee.ts`, `src/lib/validation/assignee.ts`, ו-`src/components/assignee/AssigneeField.tsx` (בורר תלת-מצבי ללא/עובד/ספק + Combobox עם חיפוש + טלפון לחיץ לספק). גם `issues` וגם `tasks` צורכים את אותו קוד; `supplierExists` משותף מ-`db/suppliers.ts`. migrations: **044** (`issues.supplier_id`) + **045** (`tasks.supplier_id`) — שניהם `uuid` FK→`suppliers(id)` **ON DELETE SET NULL** + index. טבלת ה-list בשני המודולים מציגה עמודת "מטפל" (משתמש/ספק באבחנה ויזואלית), ויש פילטר לפי ספק.
- 12/06/2026 - **קישור משימות/תקלות לאזורים — אומת כעובד end-to-end (לא נדרשה בנייה)**: `TargetField` המשותף (`@/components/targets/TargetField`) כבר מחווט בשני הטפסים (issue + task), `TargetType`=`'room'|'area'`, ו-`/api/targets` מחזיר `rooms`+`areas` (migration **040**). אומת חי (יצירת משימה+תקלה משויכות לאזור → נשמר ונטען נכון). ה-slice שנדחה בבניית מודול האזורים — **סגור**. **שים לב**: `target` (איפה התקלה/משימה) ו-assignee (מי מטפל) הם שני ממדים **נפרדים לחלוטין** — אין לבלבל ביניהם, וגם לא עם `related_entity_type` הגנרי שקיים ב-`tasks`.
- 12/06/2026 - **AGENTS.md — הופסק tracking (auto-generated)**: הקובץ נוצר אוטומטית מ-`gen-catalog.sh` (תבניות ה-KIT הן source-of-truth) וה-diff-ים שלו הם churn קטלוג בלבד (ספירת skills/agents). נוסף ל-`.gitignore` + `git rm --cached` (העותק המקומי נשמר). לא ייכנס יותר ל-staging — כמו `SECURITY_AUDIT.md`.
- 12/06/2026 - **בהורייזון (Wave 4+)**: (1) **צ'אט פנימי** — הטיפוס `chat_message` עדיין placeholder ב-notifications registry ללא מפיק; (2) **time-grid ללוח שנה** — prompt ב-`prompts/calendar-timegrid.md`; (3) **סנכרון תמונות פרופיל WhatsApp** — קוסמטי.
- 23/04/2026 - Stack נבחר: Next.js + Supabase self-hosted
- 23/04/2026 - MVP מצומצם ל-Auth + Dashboard + Import + Settings
- 23/04/2026 - אינטגרציות כ-stubs בפאזה א'
- 23/04/2026 - מסך ראשון לבנייה: Auth + App Shell
- 23/04/2026 - שם המוצר: ALMOG CRM (בעברית: אלמוג), דומיין פרודקשן: billing.bios.co.il
- 23/04/2026 - Custom auth ב-proj_billing.public (טבלאות users + sessions + password_reset_tokens). אין Supabase Auth, אין auth schema. הנימוק: בידוד מלא בין פרויקטים על אותו שרת — אין JOINs חוצי-DB ואין session משותף.
- 23/04/2026 - PostgREST ב-db.bios.co.il לא מקבל את ה-service-role key של הפרויקט (HTTP 401 לבקשות REST). הוסר @supabase/supabase-js, נוספו pg + @types/pg. כל גישת DB מהאפליקציה דרך src/lib/db.ts (singleton Pool ב-globalThis עם max=10, על DATABASE_URL = transaction pooler 6543). הסקריפט create-first-user.ts משתמש ב-DIRECT_URL (session pooler 5432) כי הוא one-shot.
- 23/04/2026 - bcryptjs (pure JS, בלי native deps), 10 rounds
- 23/04/2026 - session id ו-reset token: crypto.randomBytes(32).toString('base64url'); נשמרים כ-plain ב-DB (token reset חד-פעמי + expires 60 דקות + HTTPS); TODO הרדנינג בפאזה ב'
- 23/04/2026 - "זכור אותי": session expires_at = 30 יום + cookie maxAge = 30 יום. לא מסומן → 24h + session cookie (ללא maxAge). httpOnly + sameSite=lax + secure בפרודקשן.
- 23/04/2026 - RESEND_COOLDOWN_SECONDS = 60
- 23/04/2026 - Middleware בודק רק נוכחות cookie (אין DB hit). ולידציה ב-(app)/layout.tsx ו-API routes דרך getSession().
- 23/04/2026 - /reset-password מוחרג מ-middleware: המשתמש מגיע מ-link עם token ב-query string. ה-token הוא ה-gate היחיד.
- 23/04/2026 - reset password = invalidate כל ה-sessions של אותו user (best practice)
- 23/04/2026 - Email: stub ב-src/services/email.ts (console.log). TODO חיבור ל-Resend בפאזה ב'.
- 23/04/2026 - Google OAuth: כפתור קיים בעיצוב, מציג toast "בקרוב — ייושם בפאזה הבאה". TODO יישום OAuth2 בפאזה ב'.
- 23/04/2026 - RLS לא מוגדר: גישת DB רק דרך src/lib/db.ts בקוד שרת. אם נפתח בפאזה ב' גישה ישירה מהלקוח — נוסיף אז.
- 23/04/2026 - Production deployment: https://billing.bios.co.il (DNS wildcard *.bios.co.il → 51.195.82.57). nginx reverse proxy ל-127.0.0.1:3003, SSL מ-Let's Encrypt (certbot --nginx, HTTP→HTTPS 301). Next.js standalone output (next.config.ts `output: 'standalone'`) רץ דרך systemd unit `billing.service` (User=ubuntu, Group=devops-www, EnvironmentFile=/etc/billing/billing.env). הקונפיגים תואמים לדפוס של invoice.bios.co.il / pms.bios.co.il.
- 24/04/2026 - **Slice 2 הושלם**: Dashboard (6 KPIs + LastImportIndicator + 6 tabs + readonly DebtorsTable) + Import wizard (3-step: upload → mapping/mode → preview/progress) + Sidebar restructured (2 sections: תפריט ראשי + ניהול).
- 24/04/2026 - Schema 002: טבלאות `debtors` (apartment_number unique, סכומים numeric(10,2), legal_status nullable, is_archived) + `import_runs` (mode/status/counts/error). הוספת `is_admin boolean default true` ל-`users` (חזית: `SessionUser.is_admin`).
- 24/04/2026 - Excel parsing: `xlsx` (SheetJS) — sheet 0, row 1 = header, parse from row 2. עמודות A→apt, B→owner_name, C→phone_owner (merge: רק אם ריק), D→total_debt, E→management_fees, F→monthly_debt (text), G→hot_water_debt, H→details. שורות עם A ריק = skipped.
- 24/04/2026 - Import API: `POST /api/debtors/import` (FormData: file + mode + adminPassword?) → מחזיר {runId} מיד; runner רץ async ברקע (lifecycle של systemd Node process). polling דרך `GET /api/debtors/import/status/[runId]` כל 500ms. Batch של 50 שורות + throttle 50ms.
- 24/04/2026 - Replace mode דורש אישור כפול + bcrypt verify של סיסמת ה-user הנוכחי. Merge mode: שדות סכומים תמיד מתעדכנים, phone_owner רק אם ריק, שדות ידניים (phone_tenant, emails, tenant_name, legal_status, operator_id) לא נוגעים. דירות שלא בקובץ במצב merge → סכומים מתאפסים, טקסט נשאר.
- 24/04/2026 - LastImportIndicator: severity ok (<24h, blue) / yellow (24-48h) / red (>48h או null). כפתור "סנכרן עכשיו" קורא ל-`https://crm.bios.co.il/api/admin/jobs/syncBllinkDebt` (cross-origin, credentials:include) — מוצג רק ל-`is_admin=true`. **הקוד המקורי מ-Base44 לא הועבר; שוחזר מ-screenshot + ספק.**
- 24/04/2026 - **CVE ידוע**: ספריית `xlsx` כוללת 2 פגיעויות פתוחות (Prototype Pollution GHSA-4r6h-8v6p-xvw6, ReDoS GHSA-5pgg-2g8v-p4x9) ללא fix זמין מהמתחזקים. mitigation: רק משתמשי auth admin מעלים קבצים. TODO: מעבר ל-`exceljs` כש-`xlsx` יישאר ללא תיקון. — **✅ בוצע 23/06/2026 (גל תיקון 5): `xlsx` הוסר והוחלף ב-`exceljs`; שתי הפגיעויות נסגרו. ראה הרשומה בראש ה-Log.**
- 24/04/2026 - Header של App השתנה: ה-AlmogLogo עבר ל-Sidebar header ("ניהול אלמוג" + bell badge "+9"). Header נשאר רק UserMenu בצד שמאל.
- 24/04/2026 - סנכרון מ-Almog CRM דרך server-side proxy (`POST /api/sync/bllink`) עם header `x-cron-secret`. ה-endpoint מאומת ב-session של billing ודורש `is_admin=true`; ה-URL והסוד נקראים מ-`process.env.CRM_SYNC_URL` + `CRM_CRON_SECRET`. הסיבה: מפתח סוד אסור לחיות בדפדפן, וגם מאפשר ל-billing להחזיק policy משלו (admin-only, בעתיד rate-limit). LastImportIndicator קורא ל-`/api/sync/bllink` same-origin (בלי `credentials:'include'`) במקום ל-`crm.bios.co.il` ישירות.
- 24/04/2026 - **Standalone deploy gotcha**: `output: 'standalone'` ב-Next.js 16 לא מעתיק את `public/` ו-`.next/static/` לתוך `.next/standalone/`, ולכן build ללא העתקה → 404 על כל ה-CSS והנכסים הסטטיים (שרת ה-standalone מגיש רק מ-`.next/standalone/.next/static/` ו-`.next/standalone/public/`). התווסף `"postbuild"` ב-`package.json` שמריץ `rm -rf` + `cp -r` לשתי התיקיות אחרי כל build. חובה להריץ `npm run build` (ולא `next build` ישירות) כדי ש-postbuild ירוץ. nginx של billing רק proxy-pass ל-127.0.0.1:3003 — לא מגיש static בעצמו.
- 25/04/2026 - נוצר GIT_PUSH_SKILL.md ב-root: workflow ידני לדחיפה ל-origin/main. עקרונות: רק קבצים ששונו (לא git add -A), הודעות commit מנוסחות לפי תוכן ה-diff, pull --rebase ועצירה על קונפליקטים אמיתיים, אסור --force בלי אישור מפורש. מופעל ידנית כשהמשתמש מבקש ("תדחוף", "push", "commit ודחוף"). מופנה מ-CLAUDE.md כדי שייטען אוטומטית בכל שיחה.
- 25/04/2026 - **Slice 3 הושלם**: TenantDetailPanel (Sheet side=left, 55vw, 1.2s easing premium) עם Header gradient כחול-כהה + 5 סקשנים (פרטים עיקריים / מידע נוסף / פעולות מהירות 3-buttons / DebtsCard / NextAction + LegalManagement / Comments) + Footer sticky + AlertDialog ליציאה. PanelTabs (פרטי דייר active, מסמכים+היסטוריה disabled). Optimistic UI + router.refresh + toast לכל mutation. VIEWER banner + disabled fields. DESIGN.md מאסטר design system + skill `design-system` + skill מורחב `side-panel`.
- 25/04/2026 - **Slice 3.5 — Complete-action mechanism**: migration 004 (`completed_actions` table, description NOT NULL), 2 API routes (`POST /complete-action` בטרנזקציה: insert + clear next_action_* + set last_contact_date=current_date; `GET /completed-actions`), CheckCircle2 button בטאב "פעולות" עם AlertDialog ירוק, CompletedActionsCard בפאנל. Fallback `(ללא תיאור)` ב-API כש-next_action_description=null אבל יש date. ה-action מתועד עם snapshot של description+due_date+author — קישור לdebtor רק דרך FK (cascade).
- 25/04/2026 - **Tab filter logic refined**: טאב "חייבים" (active) משתמש ב-`NOT IN ('מכתב התראה','לטיפול משפטי','בהליך משפטי')` — מבטיח שכל דייר non-archived מופיע ב**בדיוק** טאב אחד. 3 הסטטוסים הפנימיים (במעקב נעמה/בניהול פתאל/לטיפול רונן) נופלים ל"חייבים". טאב "פעולות" הופעל: filter `next_action_date IS NOT NULL`, sort כפוי `next_action_date asc nulls last` (אינו תלוי בפרמטר sort של URL).
- 25/04/2026 - **Phone validation rich**: `validatePhone()` עם 3 types (mobile 10ספרות /^05[0-9]{8}$/ , landline 9-10ספרות /^0[2-9][0-9]{7,8}$/, intl /^\+972[0-9]{9}$/) ו-error messages עבריות מפורטות. EditPhoneDialog מציג ⚠️ inline (text-[12px] font-semibold text-red-500). validation range מיושר ל-formatPhoneDisplay (9-11 ספרות) — מונע "phantom saves" שבהם מספר נשמר אבל מוצג "אין".
- 25/04/2026 — **Slice "Status Management" הושלם**. הוסיף לטבלת statuses הקיימת (מ-003): `is_system`, `created_by`/`updated_by`, partial unique index ל-default יחיד, CHECK על color hex. הסטטוס 'רגיל' סומן is_system=true. debtors.legal_status_id הקיים משמש את ה-UI החדש (ללא עמודה כפולה). notification_emails נשאר text[], המרת CSV↔array באפליקציה. ולידציות ידניות (לא zod). Switch על Base UI לעקביות. מסך /statuses חדש: Sheet ליצירה/עריכה (`side="left"` `sm:w-[55vw]`), עמודת מקושרים עם count + Sheet רשימת דיירים, חסימת מחיקה כשיש מקושרים. הוסף תמיכה ב-`?apt=X&open=details` ב-DebtorsTable לדיפ-לינק. שלב ג' (התראות מייל אמיתיות, legal_status_lock ב-Import) נדחה.
- 25/04/2026 — **Status Management hotfix**: הוסר שדה `is_default` מטופס היצירה/עריכה ומ-API contract. רק סטטוס המערכת ('רגיל') הוא ברירת המחדל, התכונה אינה ניתנת לשליטה ידנית. תוקן באג שמנע עריכת שם/צבע/תיאור של סטטוס system.
- 25/04/2026 — **Slice 4 — Deploy pipeline**: נוצר `deploy.sh` ב-root + `GET /api/health` שעושה DB ping (`SELECT 1`). ה-script: safety check על שינויים לא-committed (read-p אינטראקטיבי, אישור y/N) → `npm run build` (כולל postbuild) → migrations מותנה (אם `scripts/run-migrations.ts` קיים — כרגע לא קיים, מורצות ידנית) → `sudo systemctl restart billing.service` → polling healthcheck ל-`https://billing.bios.co.il/api/health` (10 ניסיונות × 2 שניות + 3 שניות sleep ראשוני). Deploy מניח שהקוד כבר על השרת (אין `git pull`) — מתאים לזרימה הנוכחית של עריכה ישירה ב-`/var/www/billing/`. אסור להריץ `next build` ישירות; חובה דרך `deploy.sh` או `npm run build` כדי להפעיל postbuild.
- 26/04/2026 — **Slice 5 — Gmail SMTP**: nodemailer דרך `smtp.gmail.com:587` (STARTTLS) עם App Password של `ronen.yeadim@gmail.com`. Singleton transporter ב-`src/lib/email/transporter.ts` (pool=3, maxMessages=100) עם hash-cache (user+pass+fromName) — rebuild רק כש-credentials משתנות. `sendWithRetry` ב-`src/lib/email/send.ts` עם 2 retries (3 ניסיונות סך הכל), backoff `1s,2s`, רק על transient (`ETIMEDOUT/ECONNRESET/ECONNREFUSED/ESOCKET/EDNS/EHOSTUNREACH` או SMTP 4xx); `EAUTH`/5xx → throw מיידי. Templates ב-`src/templates/email/` כ-`.ts` עם inline CSS + `dir=rtl` + Heebo. registry ב-`src/lib/email-templates.ts` (כרגע רק `reset-password`). Stub הוחלף ב-`sendResetPasswordEmail`. `forgot-password` route עוטף את השליחה ב-try/catch ומחזיר 200 גם בכישלון מייל (anti-enumeration). `sendStatusChangeNotification` הקיים מ-Slice 3 **לא נגעו בו** — נשאר console.log; ייהפך לאמיתי בסליס נפרד. DESIGN.md סקשן 24 מתעד inline-CSS / hex-equivalents / dir=rtl / table-layout / Heebo+Arial fallback / plaintext חובה.
- 26/04/2026 — **Slice 5b — SMTP settings UI (Gmail-only)**: טבלה `app_settings` (key text PK, value jsonb, updated_at, updated_by FK→users) ב-migration `006_app_settings.sql`. App Password נשמר מוצפן AES-256-GCM (iv/ct/tag base64) עם `SETTINGS_ENC_KEY` (32 bytes base64) — fail-closed אם המפתח חסר. `SMTP_HOST=smtp.gmail.com`, `SMTP_PORT=587`, `SMTP_FROM_EMAIL=SMTP_USER` קבועים בקוד (לא חשופים ב-UI). settings ב-DB גוברים על env; אם אין רשומה → fallback ל-env. `getTransporter()` הפך async — קורא `getSmtpSettings()` בכל send (1 query קצרה) וה-hash-cache מבטיח שה-Transporter עצמו לא נבנה מחדש. UI: `/settings` (admin only — `is_admin` redirect ל-/dashboard), `SmtpSettingsCard` עם 3 שדות (כתובת Gmail / שם השולח / App Password) + לינק `https://myaccount.google.com/apppasswords` + כפתור "שלח מייל בדיקה" (Dialog). Validation: `^[^\s@]+@gmail\.com$` (regex client+server), App Password 16 תווים אחרי `replace(/\s+/g,'')`, fromName 1-50. API: `GET/PUT /api/settings/smtp`, `POST /api/settings/smtp/test` (כל השלושה `is_admin` בלבד).
- 27/04/2026 — **Slice 6d — מטריצה ב-InviteUserPanel + Switch→Checkbox**: ‏super_admin יכול לכוונן את מטריצת ההרשאות במהלך יצירת invite ל-manager/viewer, לפני שליחת המייל. Migration 009 מוסיפה `user_invites.custom_permissions jsonb` (nullable). InviteUserPanel מאתחל את ה-state מ-`getDefaultPermissions(role)` בכל שינוי role; toggle של checkbox מעדכן state local בלבד; ב-submit נשלח `permissions: [{ module, can_view, can_edit }]` כחלק מה-body. POST /api/users עושה sanitization (only-known-modules, dedupe, boolean coercion) ושומר כ-JSONB. accept-invite קורא את הblob ב-SELECT, ו-`parseCustomPermissions()` ממיר ל-`ModulePermission[]`; אם null/malformed → fallback ל-`getDefaultPermissions(role)` (backward compat ל-invites ישנים שנוצרו לפני 6d). ‏PermissionMatrix קיבלה refactor non-breaking: 2 props אופציונליים `value`/`onChange` שמפעילים controlled mode ללא API/toast (UserSidePanel לא משתמש בהם, התנהגות שלו זהה byte-by-byte). ‏UI: כל ה-Switch ב-`PermissionMatrix` הוחלף ל-`Checkbox` של shadcn (Base UI checkbox primitive) — Checkbox מתאים ל-selection-of-permission, לא ל-state toggle. DESIGN.md סקשן 25 מורחב עם פסקאות "Checkbox vs Switch" ו-"Matrix component modes".
- 26/04/2026 — **Slice 6c — פישוט מטריצת הרשאות**: 2 רמות (view/edit) במקום 3. עריכה כוללת מחיקה, ייצוא, שליחת ווטסאפ, וכל פעולה במודול. Migration 008 עושה UPDATE ל-can_edit=true שם ש-can_delete=true (defense; טבלה ריקה בנוכחי), ואז DROP COLUMN can_delete. `Action` type מצומצם ל-`'view' | 'edit'` — TS אוכף שאין callsite שעוד מעביר `'delete'`. ה-callsite היחיד שהיה (`DELETE /api/statuses/[id]` → `'delete'`) עבר ל-`'edit'`. אפס שינוי הרשאות בפועל לכל המשתמשים: manager/viewer נשארו עם `canEdit=false` ב-`status_management` כברירת מחדל; super_admin/admin עוברים bypass בכל מקרה. DESIGN.md סקשן 25 חדש מתעד את ה-2-tier model.
- 26/04/2026 — **URL config מרוכז**: `src/lib/config.ts` → `appUrl()` הוא ה-helper היחיד לבניית URLs אבסולוטיים (אימיילים, redirects). שינוי דומיין = עדכון `APP_URL` בקובץ env בלבד (`/etc/billing/billing.env` בייצור). בפרודקשן אם המשתנה חסר → `throw new Error('APP_URL is required in production')` (fail-closed — עדיף 500 ברור מאשר אימייל עם לינק שבור). ב-dev fallback ל-`http://localhost:3003`. הוסר ה-fallback הקודם ל-`new URL(req.url).origin` ב-3 routes שולחי-מייל (`forgot-password`, `users` POST, `invites/[id]/resend`). הוסר `NEXT_PUBLIC_APP_URL` מ-billing.env (לא בשימוש בקוד).
- 26/04/2026 — **Slice 6 — RBAC**: 4 roles (super_admin/admin/manager/viewer) במקום `is_admin` boolean. Migration 007 מוסיף `users.role` (CHECK constraint) + `users.is_active`, מקדם משתמשים שהיו `is_admin=true` ל-`super_admin` (השאר נשארים על default `viewer`), ומפיל את `is_admin`. נוצרו 2 טבלאות: `user_permissions` (user_id, module, can_view/edit/delete, UNIQUE על user+module) ו-`user_invites` (email, full_name, role, token unique, invited_by, expires_at, accepted_at). 19 modules מחולקים לשתי קבוצות (main 11 + admin 8); `SUPER_ADMIN_ONLY=['users_management','roles_management']` קשיח בקוד (admin לא יכול ליצור/למחוק משתמשים, רק super_admin). מטריצת permissions חלה רק על manager+viewer; super_admin/admin עוברים גישה דרך checks קשיחים ב-`hasPermission`. ה-default ל-manager: כל המודולים הראשיים view+edit, import/export/status_management view בלבד; ל-viewer: כל הראשיים + export view-only. `Actor` (`src/lib/auth/actor.ts`) טוען role+permissions מ-DB ב-(app)/layout.tsx ומועבר ל-`AuthProvider` עם `usePermissions()` ל-client side. invite flow דרך `user_invites` + מייל `user-invite` (24h) ב-`sendUserInviteEmail`; קבלה ב-`/accept-invite?token=…` יוצרת user (`username = email מלא`) + סשן אוטומטי. Self-protection מלאה ב-`PATCH/DELETE /api/users/[id]`: אסור לשנות role של עצמך, אסור להשבית את עצמך, אסור לשנות role/להשבית super_admin אחר, last-admin guard למניעת מערכת ללא super_admin פעיל. soft delete בלבד (is_active=false) + מחיקת sessions מיידית בכל role/active change כדי לכפות login מחדש. `(app)/layout.tsx` מבחין בין "אין session" ל-"session קיים אבל המשתמש הושבת" דרך `getActiveSessionId()` (helper שלא בודק is_active) ומפנה ל-`/login?reason=disabled`; ה-`LoginForm` מציג toast `'החשבון שלך הושבת'` ומנקה את ה-search param. סקריפט `scripts/create-super-admin.ts` (חדש) מוסיף super_admin ידנית. audit log + last_login נדחים. Sidebar הוסר ה-placeholder "הרשאות" והתווספו "משתמשים" (`/settings/users`, super_admin בלבד) ו-"הגדרות" (`/settings`).
- 11/06/2026 — **מודול היסטוריה ותיעוד (Unified Timeline)**: migration 012 (`debtors.archived_at`) + 013 (`debtor_events` — event log גנרי עם actor snapshot, אינדקס `(debtor_id, created_at desc)`). `GET /api/debtors/[id]/history` מאחד 4 מקורות ב-`UNION ALL` יחיד (comments + legal_status_history + completed_actions + debtor_events) למבנה אחיד ממוין (`limit` 100/מקס 300, ללא N+1). `POST /api/debtors/[id]/events` (contacts:edit) לתיעוד ידני — INSERT + עדכון `last_contact_date` בטרנזקציה; title נגזר ממפת `EVENT_TYPE_META`. ארכוב/שחזור עברו לטרנזקציוני (`POST`/`DELETE /archive`): היפוך `is_archived` + אירוע ARCHIVE/UNARCHIVE אוטומטי באותה טרנזקציה (idempotent), והוסרו `archiveDebtor`/`unarchiveDebtor` מ-db layer. שינויי סטטוס **לא** נכתבים ל-events (מכוסים ב-legal_status_history). פאנל הדייר: טאב "היסטוריה" הופעל → `HistoryTimeline` (ציר RTL, chips פילטר, lazy load) + דיאלוג "תיעוד שיחה". טבלת דשבורד: popover "תיעוד מהיר" כחול על אייקון ההערה (סגנון פאנל — gradient header, נשמר דרך endpoint ההערות הקיים כי הערוצים מתועדים אוטומטית בשליחה) + חיווי ספירת תיעוד (comments+events) ליד שם הדייר. נוסף Popover primitive מעל `@base-ui/react`.
- 11/06/2026 — **InviteUserPanel — custom_permissions רק כששונה (Slice 6d follow-up)**: השלמת ההתנהגות מ-Slice 6d — `InviteUserPanel` שולח את מטריצת ההרשאות ב-`body.permissions` **רק** כשהמטריצה שונה מברירות המחדל של ה-role (`permsDifferFromDefaults`, השוואה לפי module ולא לפי סדר כי toggle מסדר מחדש). זהה ל-default → השדה מושמט → `POST /api/users` שומר `custom_permissions = NULL` → `accept-invite` נופל ל-`getDefaultPermissions(role)`. כך הזמנות לא-מותאמות עוקבות אחרי ברירות המחדל של ה-role במקום לקבע snapshot. המטריצה (controlled, checkboxes) כבר מוצגת ל-manager/viewer ומוסתרת ל-admin/super_admin; השכבות data/accept היו תקינות.
- 12/06/2026 — **מודול WhatsApp — Slice 1 (שליחה יוצאת) הושלם**: שליחת הודעות מטבלת החייבים דרך `WhatsAppSendPanel` (Sheet side=left 55vw) — בחירת נמען לפי שדה הטלפון עם תווית בעלים/שוכר. תבניות בעמוד עצמאי `/whatsapp-templates` (gate `whatsapp_templates`) עם משתנים `{{name}}`/`{{debt}}`/`{{monthly}}`/`{{special}}` (`{{monthly}}`→management_fees כי `monthly_debt` הוא טווח-חודשים טקסטואלי, ברירת מחדל שם "דייר יקר", פורמט כסף he-IL + ₪). interpolation מתבצע **בשרת** כ-single source of truth — גם התצוגה-מקדימה בלקוח קוראת לאותו `interpolateTemplate`. היסטוריית הודעות בכרטיס הדייר. Green API credentials (instanceId + token) מוצפנים AES-256-GCM ב-`app_settings` (key=`green_api`, אותו helper של SMTP). כל שליחה מוצלחת מתועדת ב-timeline דרך `debtor_events` (type=`WHATSAPP`) + עדכון `last_whatsapp_sent_at`, הכל בטרנזקציה. שליחה לעולם לא מחזירה 200 על כשל (502 + שורת failed בהיסטוריה). Migration 014 (`whatsapp_templates`, `chat_messages`, `debtors.last_whatsapp_sent_at`, backfill הרשאת whatsapp).
- 12/06/2026 — **מודול WhatsApp — Slice 2 (קליטה + שליחה מרוכזת) הושלם**: webhook ציבורי `POST /api/whatsapp/webhook` (ללא session) עם אימות `Bearer webhook_token` (נוצר אוטומטית `randomBytes(24)`, נשמר ב-`app_settings.green_api` jsonb — **אין migration**), השוואה constant-time, תמיד 200 מהיר (Green API עושה retry על שגיאות), dedup דרך `ON CONFLICT (external_message_id) DO NOTHING`. `pullGreenApiMessages` (fallback, `lastIncomingMessages`, ברירת מחדל 1440 דק') מזרים דרך **אותה** `processIncomingMessage` → הרצה כפולה בטוחה; חשוף ב-`POST /api/whatsapp/pull` + כפתור "משוך הודעות" בכרטיס Green API. parsers טהורים ובדוקים (`parseWebhookNotification`/`parseLastIncomingItem`/`chatIdToLocalPhone`); הצלבה לחייב לפי שדות הטלפון הנקיים → `linked`/`unlinked` (מדלג על קבוצות `@g.us` ועל webhooks לא-נכנסים). עמוד `/whatsapp-unlinked` לשיוך הודעות לא-מזוהות (דיאלוג חיפוש חייב דרך `GET /api/debtors/search`; מצרף את כל השיחה לפי `contact_phone`). שליחה מרוכזת: checkboxes per-page בטבלה → `WhatsAppBulkSendPanel`, `POST /api/whatsapp/send-bulk` משדר **NDJSON** progress (שורה לכל נמען + סיכום `{sent, failed, skipped}` עם פירוט כשלים), delay 2.5ש' anti-ban, תקרה 50, `X-Accel-Buffering: no` כדי שההתקדמות תזרום דרך nginx. זנב ה-send+record חולץ ל-`sendAndRecordWhatsApp` (`src/lib/whatsapp-send.ts`) משותף בין שליחה יחידה למרוכזת — האירוע/שורת ה-failed/`last_whatsapp_sent_at` לא יכולים להתפצל.
- 12/06/2026 — **מדיניות נתונים — שדות טלפון (migration 015)**: שדה טלפון ב-DB מחזיק מעכשיו **מספר נקי בודד** בפורמט מקומי (`0XXXXXXXXX`) בלבד — בלי תוויות `(בעלים)`/`(שוכר/ת)`, בלי Markdown `[..](tel:..)`, בלי `tel:`, בלי מספרים מרובים בשדה. התווית נגזרת מסמנטיקת השדה (`phone_owner`=בעלים, `phone_tenant`=שוכר), לא מהמחרוזת. נאכף בכל נקודות הכניסה (`bllinkPull.mapRow`, `excel/parse.ts`, `import/runner.ts` הכותב את שני השדות עם merge "שומר קיים") דרך helpers משותפים `cleanPhoneField`/`splitOwnerTenantPhones` (`src/lib/whatsapp.ts`), + ולידציה ב-UI (`EditPhoneDialog`). Migration 015 הוסיפה עמודות backup `phone_*_raw_backup`, ו-`scripts/clean-phone-fields.ts` (אותם helpers, dry-run→backup→apply) ניקה 226 רשומות קיימות (288 גובו). `parsePhoneCandidates` נשאר כשכבת הגנה לערכים חריגים; `normalizeParsedDigits` מקבל גם מספר 9-ספרתי בלי 0 מוביל (CRM שומר "523344580"→0523344580).
- 12/06/2026 — **RBAC — מודול whatsapp**: המודול `whatsapp` (`שליחת ווטסאפ`, group=main) נוסף ל-`MODULES` (19→20). שליחה (יחיד + מרוכז) ו-pull = `whatsapp:edit`; קליטת inbound + עמוד "הודעות לא משויכות" = `whatsapp:view`; שיוך הודעה + חיפוש חייב = `whatsapp:edit`; webhook ציבורי = ללא הרשאה (bearer token בלבד); ניהול תבניות = `whatsapp_templates`; credentials + רישום webhook = `admin` בלבד. backfill ב-migration 014: managers מקבלים edit, viewers view.
- 12/06/2026 — **תפריט ועמודים**: 2 פריטים בקבוצת "ניהול" ב-Sidebar — "תבניות ווטסאפ" (`/whatsapp-templates`, עמוד עצמאי שהועבר מ-`/settings`) ו-"הודעות לא משויכות" (`/whatsapp-unlinked`). עמוד `/settings` יושר עיצובית (`mx-auto max-w-3xl`, ניהול התבניות הוסר ממנו ונשארו רק כרטיסי SMTP + Green API).
- 12/06/2026 — **SETTINGS_ENC_KEY (תפעולי)**: קיים ב-`/etc/billing/billing.env` (נוצר 06/2026, 32 bytes base64). **אסור לסובב/לשנות** — סיבוב יהפוך את כל הסודות המוצפנים (SMTP App Password, Green API token) לבלתי-פענוחים. ההצפנה AES-256-GCM, fail-closed אם המפתח חסר.
- 12/06/2026 — **Roadmap — Wave 2**: הבא בתור אחרי WhatsApp — **Issues (תקלות)**. סנכרון תמונות פרופיל WhatsApp של אנשי קשר (avatar) — קוסמטי, **נדחה**.
- 12/06/2026 — **מודול ספקים יחיד — לא יוקם מודול Vendors נפרד**: הוחלט **שלא** להקים מודול נפרד ל"אנשי מקצוע" — מודול `suppliers` היחיד משרת גם ספקי שירות/בעלי מקצוע. היכולות שתוכננו לאותו מודול מוזגו לתוך suppliers: (1) **קטגוריות דינמיות** — טבלת `supplier_categories` + `suppliers.category_id` (suppliers החזיק רק `supplier_type` קבוע), עם ניהול קטגוריות (הוספה/שינוי-שם/השבתה/מחיקה) ב-Side Panel, סינון לפי קטגוריה, ו-delete-guard החוסם מחיקת קטגוריה כשמשויכים אליה ספקים חיים; (2) **ניקוי טלפון** דרך `cleanPhoneField` (מספר לוקלי נקי אחד) בשרת — phone + mobile; (3) **ולידציית פורמט אימייל** בשרת; (4) **קישורי `tel:`/`mailto:`** + תג קטגוריה בטבלת הספקים. ולידציה משותפת ל-POST+PATCH ב-`src/lib/validation/suppliers.ts`. **Migration 016** (`016_supplier_categories.sql`) — `supplier_categories` + `suppliers.category_id` + seed 7 קטגוריות (אינסטלציה/חשמל/מעליות/ניקיון/גינון/אינטרקום/כללי), additive + idempotent. יכולות שכבר היו ב-suppliers (soft-delete דרך `deleted_at`, dirty-tracking בפאנלים, דפוס ה-Side Panel) — לא שונו.

- 12/06/2026 — **אינדיקטור דשבורד — סנכרון וייבוא מוצגים בנפרד**: `LastImportIndicator` הציג טיימסטמפ אחד (`max(debtors.last_imported_at)`, מתעדכן רק בייבוא קובץ ידני), כך שלאחר "סנכרן עכשיו" מוצלח הזמן נשאר ישן — מטעה. הפתרון: מעקב נפרד. טבלה חדשה **`sync_runs`** (`017_sync_runs.sql`: id, started_at, finished_at, status running/success/error, error_message, triggered_by FK→users, idx על started_at desc). `POST /api/sync/bllink` פותח שורת sync_run בתחילת הפעולה ומסיים אותה success/error (ה-`createImportRun('merge')` הפנימי נשאר — הוא חלק מצינור ה-merge). `GET /api/sync/status` חדש מחזיר את שני הזמנים יחד (`lastImportAt` + `lastSyncAt`). האינדיקטור מציג **שני טיימסטמפים מובחנים**: "ייבוא אחרון" (ראשי, מניע severity) ו"סנכרון אחרון" (משני); אחרי סנכרון מוצלח הזמן מתעדכן מיידית מ-`/api/sync/status`. **severity (ok/yellow/red לפי 24/48ש') נשאר מבוסס על הייבוא בלבד** — הוא משקף את טריות הנתונים. סנכרון ללא היסטוריה → "טרם בוצע סנכרון". DESIGN.md §5b עודכן לדפוס שני-הטיימסטמפים.

- 12/06/2026 — **Slice 6 — מודול הודעות WhatsApp (תיבת שיחות + תפוצות) הושלם**: עמוד `/messages` ("הודעות") — צ׳אט דו-פאנלי RTL מלא-גובה. **החלטות התאמה מרכזיות** (המפרט נכתב מול מודל-עולם שונה — אין טבלת `contacts`, וכבר קיים מודול WhatsApp מ-Slices 1-2):
  - **שימוש חוזר ב-`chat_messages`** (לא טבלת `whatsapp_messages` כפולה) כמקור-אמת יחיד — כך שהתיבה מציגה גם הודעות שנשלחו מטבלת החייבים, גם נכנסות, וגם תפוצות. `contact_id` של המפרט → `debtor_id` הקיים; `phone_raw` → `contact_phone`.
  - **Migration 018** (`018_whatsapp_chat_inbox.sql`, additive): טבלה חדשה `whatsapp_broadcasts` (name/body/audience_filter jsonb/total/sent/failed/status/created_by/timestamps); הרחבת `chat_messages` ב-`broadcast_id` (FK→broadcasts SET NULL), `media_url`, `read_at`; הרחבת CHECK של `status` ל-`pending/queued/sent/delivered/read/failed`; אינדקסים על `chat_id` ו-`broadcast_id`; backfill הרשאת `whatsapp_chat` ל-manager(edit)/viewer(view). שימוש חוזר ב-`whatsapp_templates` הקיימת (לא נוצרה מחדש).
  - **מודול הרשאות**: gate ל-`/messages` הוא המודול השמור הקיים **`whatsapp_chat`** ("צ׳אט ווטסאפ", group=main, כבר ב-MODULES + ברירות-מחדל) — לא מודול `messages` חדש (כדי לא לכפול). שליחה/תפוצה = `whatsapp_chat:edit`; צפייה/סימון-נקרא = `whatsapp_chat:view`.
  - **Webhook קנוני חדש** `POST /api/webhooks/greenapi?secret=…` מול env `GREEN_API_WEBHOOK_SECRET` (נוצר ב-billing.env, 32-byte hex; השוואה constant-time). מטפל ב-`incomingMessageReceived` (כולל **הודעות קבוצה** `@g.us` — שומר את טלפון השולח ב-`contact_phone`, חוצץ לפי chat_id הקבוצתי, לעולם לא משויך לחייב יחיד), `outgoingMessageStatus` (עדכון status מונוטוני delivered/read), ו-`outgoingAPIMessageReceived`/`outgoingMessageReceived` (insert שורה יוצאת + dedup). תמיד מחזיר 200. אימות נכנס דרך `processIncomingMessage` המשותף (אותו pipeline של ה-pull). רישום ה-webhook ב-`POST /api/settings/green-api/webhook` עודכן להצביע ל-URL החדש (עם `?secret`) ולהפעיל את כל ה-notifications (incoming + outgoing message + outgoing status); הסוד ממוסך בתגובות הסטטוס ללקוח. ה-webhook הישן `/api/whatsapp/webhook` (Bearer) נשאר כ-legacy אך אינו הרשום עוד.
  - **שכבת אינטגרציה**: `src/lib/whatsapp.ts` הורחב — `sendWhatsAppFileByUrl`, `parseWebhookNotificationFull` (person+group), `parseOutgoingStatus`, `parseOutgoingMessage`, `setWebhookSettings` עם דגלי notifications מלאים + bearer אופציונלי. credentials נטענים פר-קריאה מ-`app_settings.green_api` (אותו דפוס SMTP) — אין singleton.
  - **DB layer חדש**: `whatsappConversations.ts` (listConversations עם CTE שמקבץ לפי chat_id, מונה לא-נקראו, JOIN לחייב לשם תצוגה; listThread עם pagination לפי `before`; markConversationRead) ו-`whatsappBroadcasts.ts` (CRUD + bump counters). `chatMessages.ts` קיבל `media_url`/`broadcast_id` ב-insert + `updateMessageStatusByExternalId` (מונוטוני, לא מוריד read→delivered).
  - **תפוצה**: `POST /api/whatsapp/broadcasts` פותר נמענים מ-**חייבים** (אין contacts) — `all`/`owners`(phone_owner)/`tenants`(phone_tenant)/`debtor_ids`, רק עם טלפון תקין, dedup לפי מספר. רץ ברקע fire-and-forget בתהליך השרת (כמו import runner): לולאה סדרתית עם השהיה אקראית 3–6ש' anti-ban, interpolation פר-חייב, insert עם `broadcast_id`, bump של sent/failed, status סופי. כשל בודד לא עוצר. `GET /api/whatsapp/audience-count` מציג מספר נמענים מחושב חי. `{{apartment}}` נוסף ל-`interpolateTemplate` (סה״כ: name/apartment/debt/monthly/special).
  - **API נוסף**: `GET conversations`, `GET thread`, `POST chat-send` (chat_id או phone; person→linking לחייב + reuse של `sendAndRecordWhatsApp`, group→שליחה ישירה), `POST conversations/[chatId]/read`, `GET/POST broadcasts`. templates GET (active) הורחב לאפשר `whatsapp_chat:view` בנוסף ל-`whatsapp:view`.
  - **UI**: `MessagesClient` (פוlinג כל 5ש' לשיחות+thread פתוח), `ConversationList` (חיפוש, badge לא-נקראו, avatar), `ChatThread` (בועות יוצאות ירקרק/נכנסות לבן, מפרידי-יום, סימוני סטטוס ✓/✓✓/כחול-נקרא, composer Enter-לשליחה), `NewConversationDialog` (טלפון או חיפוש דייר), `BroadcastPanel` (Sheet side=left 55vw — שם/צ׳יפי קהל/תבנית-או-חופשי/מונה נמענים/פריוויו/תפוצות אחרונות עם progress), `TemplatesTab` (reuse של `WhatsAppTemplateSheet`, מחיקה ב-AlertDialog). פריט הסיידבר `whatsapp_chat` חובר ל-`/messages` בשם "הודעות". כניסה לשיחה מסמנת כנקראה.
- 12/06/2026 — **Slice 7 — WhatsApp רב-משתמשי (instance לכל עובד) + תיקון webhook + מדיה/resend הושלם**:
  - **שורש הבאג הקריטי (חלקים א'):** ה-webhook **מעולם לא נרשם** ב-Green API (`app_settings.green_api` היה ללא `webhookToken`, וה-instance הצביע ל-URL ישן) — לכן **אפס** הודעות נכנסות ו**אפס** עדכוני סטטוס יוצא (תקוע על ✓ אחד). הקוד שמפעיל את ה-notifications כבר היה תקין; חסרה הרשמה בפועל. תוקן: `scripts/register-whatsapp-webhooks.ts` (כלי תפעולי, pg+crypto standalone) רושם `setSettings` לכל instance עם ה-URL הקנוני, מאמת ב-`getSettings`, ומעדכן state מ-`getStateInstance`. הורץ חי על `7107545950` → `incoming/outMsg/outApi/state=yes`, state=authorized. E2E סינתטי מול ה-webhook החי אימת: נכנסת נשמרת, status עולה ל-delivered, תיוג instance נכון, instance לא-מוכר נדחה, סוד שגוי→401.
  - **רב-משתמשיות (חלק ב'):** Migration 020 (`whatsapp_instances`: `user_id` UNIQUE, `display_name`, `green_instance_id` UNIQUE, `green_token_enc` jsonb AES-GCM אותו cipher, `api_url`, `state`, `state_checked_at`) + עמודת `instance_id` (FK nullable) ל-`chat_messages` ו-`whatsapp_broadcasts`. **מיגרציית נתונים**: ה-credential הגלובלי מ-`app_settings.green_api` קודם ל-instance #1 (שויך ל-super_admin הראשון `adi`, token הועתק verbatim), וכל ההודעות/תפוצות הקיימות backfill ל-instance זה. הקוד הפעיל **הפסיק לקרוא** את ה-credentials הגלובליים — כל send/broadcast/pull/avatars/webhook עובר עכשיו דרך `whatsappInstances.ts` (resolver של creds מפוענחים + `getInstanceByGreenId` ל-webhook + `resolveViewInstanceId` להרשאת צפייה).
  - **שכבת שירות:** `whatsapp.ts` קיבל `apiUrl` אופציונלי בכל הקריאות (host פר-instance) + `getQrCode` + `logoutInstance` + parsers ל-`stateInstanceChanged`/`webhookInstanceId`. `setWebhookSettings` מפעיל גם `stateWebhook=yes`. כל ה-call-sites (`whatsapp-send`/`whatsapp-chat-send`/`whatsapp-broadcast`/send/send-bulk/chat-send/pull routes) מקבלים `InstanceCreds` במקום `{instanceId,token}` גלובליים: שליחה דרך ה-instance של המשתמש המחובר, תפוצה דרך ה-instance של יוצר התפוצה.
  - **Webhook רב-instance:** מזהה לפי `instanceData.idInstance` → `getInstanceByGreenId` → מתייג כל נכנסת/יוצאת/סטטוס ב-`instance_id` הנכון; לוג שורה-אחת לכל notification (type+idInstance+instance); instance לא-מוכר → warn+ack. גם `stateInstanceChanged` מעדכן את `state`.
  - **תיחום שיחות:** `listConversations`/`listThread`/`markConversationRead` מסוננים לפי `instance_id`; ל-admin/super_admin בורר instance בראש רשימת השיחות (`GET /api/whatsapp/instances`), ברירת מחדל ה-instance שלהם אחרת הראשון. `resolveViewInstanceId` כופה על לא-admin את ה-instance שלו.
  - **חיבור QR (Part B UI):** `src/app/(app)/settings/components/WhatsAppConnections.tsx` — admin מנהל את רשימת ה-instances (יצירה/עריכה/מחיקה ב-Side Panel side=left, בחירת עובד+ID+Token+API URL עם הסבר על קונסולת Green API), כל עובד רואה את החיבור שלו עם state; `notAuthorized`→QR (polling `GET …/connection` כל 15ש') עד `authorized`, ואז כפתור ניתוק (`POST …/connection {action:logout}`). יצירת instance מריצה אוטומטית `setWebhookSettings`. הכרטיס הגלובלי הישן (`GreenApiSettingsCard`) הוסר מ-`/settings`.
  - **API instances חדש:** `GET/POST /api/whatsapp/instances` (list/create+register), `PATCH/DELETE /api/whatsapp/instances/[id]`, `GET/POST /api/whatsapp/instances/[id]/connection` (poll state+qr / logout+register).
  - **שליחה מחדש (חלק ד'):** `POST /api/whatsapp/messages/[id]/resend` שולח מחדש דרך ה-instance של השורה (fallback ל-instance של המשתמש) ו**מעדכן את השורה הקיימת** ל-sent (אין כפילות); כפתור "שלח שוב" על בועה כושלת.
  - **צירוף קבצים (חלק ה'):** `src/lib/storage/whatsappMedia.ts` (bucket ציבורי `whatsapp-media`, `getPublicUrl`) + `POST /api/whatsapp/chat-send-file` (multipart, 50MB, `sendFileByUrl`, message_type image/document, dedup). כפתור אטב בקומפוזר; תמונות נכנסות/יוצאות inline (פתיחה מלאה בלחיצה), מסמכים כקישור. **תלות תפעולית:** דורש `SUPABASE_SERVICE_ROLE_KEY` תקין ו-bucket ציבורי נגיש ל-Green API (ראה infra gap).
  - **חלק ג' (חיפוש):** אומת מול ה-DB החי — שאילתת `listConversations` (חיפוש שם ILIKE / טלפון מנורמל / דירה JOIN) **תקינה**; הבאג היה שהמשתמש בדק build ישן (gotcha של build-בלי-restart שתוקן ב-`npm run deploy`).
- 12/06/2026 — **WhatsApp — סגירת Slice 7: המודול פעיל במלואו בפרודקשן.** עובד מקצה-לקצה — הודעות נכנסות מגיעות וסטטוסי מסירה/קריאה מתעדכנים (אומת חי: שורות `received` ו-`read` **אמיתיות** ב-`chat_messages`, מעבר ל-E2E הסינתטי שנוקה). פעילים: **multi-instance פר עובד** עם חיבור QR, שליחה/קבלה, סטטוסים ✓/✓✓/כחול-נקרא, תפוצות, תבניות, וחיפוש (שם/טלפון/דירה).
  - **Backlog — ריק/לא-רלוונטי:** כש-custom webhook URL מוגדר, Green API מספק את כל ה-notifications ישירות ל-webhook ו**חוסם** את תור ה-`receiveNotification` (`"Message cannot be received because custom webhook url is set"`) — אין תור לרוקן ידנית, וההודעות שנצברו בזמן התקלה כבר זרמו דרך ה-webhook הפעיל. כלי `scripts/drain-whatsapp-backlog.ts` נוסף למקרה עתידי (יש להריץ עם webhook מנוטרל זמנית). 0 הודעות עובדו ידנית.
  - **ידועים פתוחים (לא חוסמים את הליבה):** (1) **צירוף קבצים** ממתין להחלפת `SUPABASE_SERVICE_ROLE_KEY` הפגום (פעולה ידנית) + bucket ציבורי `whatsapp-media` נגיש לשרתי Green API; (2) **יצירת instance לעובד** נעשית ידנית בקונסולת Green API — אוטומציה דרך Partner API היא **backlog**; (3) שני קבצי **legacy מנוטרלים** (`/api/settings/green-api/*` + `components/settings/GreenApiSettingsCard.tsx`) מסומנים למחיקה ב-cleanup עתידי.
- 12/06/2026 — **Slice 4 Track A — Contacts foundation (DB + CRUD API) הושלם** (ללא Bllink — זה Track B). Migration **021_contacts.sql** (לא "005" כמבקש המפרט — 005 כבר תפוס ע"י `005_status_management.sql`; הרצף החי הגיע ל-020, אז המספר הפנוי הבא הוא 021), additive+idempotent: טבלת `contacts` (שורה לכל דירה, `apartment_number` UNIQUE, שדות בעלים/שוכר/מפעיל, `resident_type` CHECK owner/tenant/operator, 3 דגלי `*_is_primary_contact`, `tags text[]`, מטא-דאטה של פרופיל WhatsApp, `last_synced_at`) + `debtors.contact_id uuid REFERENCES contacts(id) ON DELETE SET NULL` + אינדקסים (GIN על tags, btree על operator_id ועל debtors.contact_id) + טריגר `touch_updated_at`. **DB layer** `src/lib/db/contacts.ts`: `normalizeApartmentNumber` (trim → strip non-digits → strip leading zeros → '' הופך ל-'0') משמש כמפתח קנוני בכל הכתיבות; `listContacts` (search ILIKE על apt/שמות/טלפונים, `tags @> $1::text[]` every-semantics, sort טבעי לפי מספר דירה מספרי NULLS LAST), `getContactById`, `createContact` (תופס 23505→`ConflictError`), `updateContact` (apartment_number immutable — מסונן לפני SQL; אם לא נשאר שדה — דילוג על UPDATE), `deleteContact`; ולסנכרון (Track B): `upsertContactByApartment` (UPSERT לפי מפתח מנורמל, `protectManualFields` שומר notes/address/tags/resident_type/operator_id/3 דגלים/owner_email/tenant_email דרך `COALESCE(contacts.f, EXCLUDED.f)`, ושדות owner_name/owner_phone/tenant_name/tenant_phone תמיד מתעדכנים אם הערך החדש NOT NULL; `created` נקבע דרך `xmax=0`), `linkDebtorToContact` (UPDATE לפי מפתח-דירה מנורמל ב-SQL, חסר-debtor אינו שגיאה), ושתיהן עטופות יחד ב-`upsertContactAndLinkDebtor` בטרנזקציה אחת. **Validation** `src/lib/validation/contacts.ts` (partial-by-design, מנקה טלפונים דרך `cleanPhoneField` לפי מדיניות-הטלפון, אוכף enums/email/uuid/tags). **5 endpoints** תחת `/api/contacts`: GET רשימה + GET/[id] לכל מאומת (`getSession`); POST/PATCH/DELETE לאדמין בלבד (`requireAdmin`, דפוס session+admin מ-bllink route). POST→201, כפילות→409, body לא חוקי→400, PATCH מתעלם מ-apartment_number, DELETE→204 ו-`debtors.contact_id` מתאפס אוטומטית דרך ה-FK. **בדיקות** `scripts/test-contacts-api.sh` (מנפיק session אדמין זמני ישירות ב-DB → CRUD מלא → ניקוי) — **15/15 עברו**; אומת בנוסף נורמליזציה ("0042"→"42"+דדופ 409), ולידציית email, ו-every-semantics של tags. הותקן בפרודקשן דרך `npm run deploy`, `/api/health`=200. **הצעד הבא: Track B — סנכרון Bllink** (קליטת לקוחות/דיירים מ-Bllink אל `contacts` דרך `upsertContactAndLinkDebtor`).
- 12/06/2026 — **Slice 4 Track A — חיזוק חוזה ה-upsert לסנכרון (allowedFields + מחרוזת ריקה).** הוספת שני התנהגויות ל-`upsertContactByApartment` הנדרשות כשקוראים לו **ישירות** (מסלול Track B, שעוקף את שכבת ה-validation של ה-HTTP שממירה `''`→`null`): (1) **`allowedFields?: string[]`** ב-`UpsertContactOpts` — whitelist של עמודות שה-upsert רשאי לגעת בהן; כל עמודה שאינה ברשימה נושרת מהפיילוד לגמרי (לא insert ולא update), כך שסנכרון יכול להכריז במדויק אילו שדות בבעלותו ולעולם לא לדרוס שדה ידני (notes/tags) שלא התכוון. (2) **`sanitizeUpsertValue`** — מחרוזת ריקה/רווחים בלבד הופכת ל-`null` לפני המיזוג, כך ש-`owner_phone: ''` נכנס כ-`coalesce(null, contacts.owner_phone)` ו**שומר** את הטלפון הקיים במקום לדרוס אותו ב-`''` (הבאג: `coalesce('', existing)` מחזיר `''`). אומת ב-`scripts/test-upsert-empty.ts` (קריאה ישירה ל-db layer דרך `npx tsx --tsconfig scripts/tsconfig.scripts.json` — stub ל-`server-only` ב-`scripts/server-only.stub.ts` כי Next מספק את החבילה רק בבילד; ה-root tsconfig לא נגעו בו): seed ידני → upsert עם owner_name חדש/owner_phone ריק/tenant חדש → **6/6 עברו** (owner_name נדרס, owner_phone נשמר, tenant_name נוסף, notes+tags ללא נגיעה, created=false). אפס רגרסיה ב-API (`test-contacts-api.sh` 15/15) — מסלול ה-HTTP ממילא מנרמל `''`→`null` ב-validation לפני ה-db layer.
- 13/06/2026 — **Slice 4 Track B — סנכרון דיירים מ-Bllink הושלם** (קוד; ממתין ל-credentials כדי לרוץ חי). **Cognito SRP** פורט 1:1 verbatim מקוד Base44 (`functions/importBuildingDebtReport/entry.ts`, Deno) ל-Node 20 native ב-`src/lib/bllink/cognito-srp.ts` — משתמש ב-`crypto.subtle`/`getRandomValues`/`atob`/`btoa`/`TextEncoder`/`BigInt` הגלובליים (אין `node:crypto`); כל הקבועים (N_HEX, client/pool id) וכל פונקציות העזר (sha256, hmacSha256, padHex, modPow, computeK/U/X/S, computeHkdfKey, cognitoTimestamp, cognitoPost) verbatim; זרם `USER_PASSWORD_AUTH` → fallback ל-`USER_SRP_AUTH` המלא (InitiateAuth→RespondToAuthChallenge עם PASSWORD_CLAIM_SIGNATURE). TS strict: הוספת טייפים בלבד + cast ל-`BufferSource` ב-3 קריאות `crypto.subtle` (TS 5.9 `Uint8Array<ArrayBufferLike>`), `2n`→`BigInt(2)` כבר במקור. **Client** `src/lib/bllink/client.ts`: `fetchBllinkTenants` קוראת ל-srpAuth ואז GET ל-**endpoint נפרד** `/api/v1/managers/buildings/udnp/tenants` (לא endpoint החובות `/debts/per_building` — זה נשאר של הסנכרון הקיים) עם headers `Authorization: Bearer`, `Ismanager: true`, `Accept`, `Origin: app.bllink.co`; parsing גמיש (array ישיר → data/value/tenants/apartments/results); **לוג sample keys/row חד-פעמי** בריצה ראשונה (TODO: להסיר אחרי שה-schema יציב ומאומת). `mapBllinkTenantToContact` ממפה רק 5 שדות (apartment_number/owner_name/owner_phone/tenant_name/tenant_phone), מסנן ''/‏'000000000', ו-owner_name **לא** נופל ל-tenantDetails.RenterName (באג של Base44). **Endpoint** `POST /api/sync/contacts` (`requireAdmin`, דפוס session+admin) — env-check (חסר creds→500), `fetchBllinkTenants`, לולאה **sequential** עם `upsertContactAndLinkDebtor(mapped, { allowedFields: [5 שדות] })`, מונה created/updated/skipped/failed + `failedSample.slice(0,20)`, try/catch ברמת-סנכרון→500. **ENV**: דורש `BLLINK_USERNAME`+`BLLINK_PASSWORD` ב-`/etc/billing/billing.env` — נוספו כ-placeholders ריקים עם `# FILL ME` (Ronen ימלא ידנית ויבצע restart). **מצב בדיקה: no-credentials** (placeholders ריקים) — `scripts/test-bllink-auth.ts` הדפיס "not set" (שרשרת ה-import נפתרה — server-only stub + srpAuth נטענו ללא שגיאת crypto/BigInt); `scripts/test-sync-contacts.sh` קיבל 500 "BLLINK credentials not configured" (צפוי→exit 0). guard אומת: unauth→401, GET→405. הותקן דרך `npm run deploy`, service active, `/api/health`=200. **הצעד הבא (כשיהיו creds):** Ronen ממלא env+restart → הרצת סנכרון → קריאת ה-sample keys/row מהלוג לאימות ה-schema → הסרת לוגי ה-debug.
- 13/06/2026 — **Track B (Cognito SRP + sync endpoint ישיר ל-Bllink) הוסר.** הסיבה: ה-API של Bllink לא מחזיר JSON של רשימת דיירים ב-endpoint נגיש; ה-UI של Bllink מציע רק ייצוא Excel. המודל החדש: ייבוא ידני של Excel רשימת דיירים, על תשתית דומה ל-`/api/debtors/import` הקיים. **Track A (טבלת `contacts` + CRUD API) נשמר ומשמש בסיס לייבוא.** נמחקו: `src/lib/bllink/cognito-srp.ts`, `src/lib/bllink/client.ts` (התיקייה `src/lib/bllink/` רוקנה ונמחקה), `src/app/api/sync/contacts/route.ts`, `scripts/test-bllink-auth.ts`, `scripts/test-sync-contacts.sh`; הוסרו `BLLINK_USERNAME`/`BLLINK_PASSWORD` (+הערת `# FILL ME`) מ-`/etc/billing/billing.env` + restart. אומת: build נקי, `/api/health`=200, `POST /api/contacts`→401 (Track A תקין), `POST /api/sync/contacts`→404 (הוסר), `POST /api/sync/bllink`→401 (סנכרון החובות הקיים לא נגעו בו). הקבצים `scripts/server-only.stub.ts` + `scripts/tsconfig.scripts.json` (תשתית הרצת tsx של Track A) נשארו — הם כלליים ולא תלויי-Bllink.
- 13/06/2026 — **Slice 4 Track C — ייבוא Excel של רשימת דיירים הושלם.** 3 endpoints חדשים (כולם `requireAdmin`, דפוס session+admin): **`POST /api/contacts/import`** (FormData `file`, ולידציית סיומת `.xlsx/.xls` — mime לא אמין; יוצר `import_runs` עם kind='contacts'/status='running', שומר ל-`/tmp/billing-contacts-import-{runId}.xlsx`, מפעיל runner async fire-and-forget, מחזיר `{ runId }` **201 מיד**), **`GET /api/contacts/import/status/[runId]`** (404 אם לא קיים **או** kind!='contacts' — בידוד מ-runId של debtors; אומת חי: debtors runId→404), **`GET /api/contacts/import/runs`** (10 אחרונים WHERE kind='contacts' להיסטוריה ב-UI עתידי). **Runner** `src/lib/contacts/import-runner.ts` (גם מחזיק את שלושת ה-DB helpers): parsing דרך SheetJS (`XLSX.read` buffer, `sheet_to_json header:1 range:1`), batch=50 + throttle=50ms, `processed_rows` כל 25 שורות; מיפוי 8 עמודות A-H כאשר **H (תשלום חודשי) לא מיובא** (שייך ל-debtors); A=apartment_number (ריק→skip), B-G→owner/tenant name/phone/email; טלפון '000000000' (כולל רווחים/all-zeros) → null; payload כולל רק שדות לא-null; `upsertContactAndLinkDebtor(payload, { allowedFields: [7 שדות בדיוק: apartment_number+owner_*+tenant_*] })` — **שדות ידניים (notes/tags/address/resident_type/operator_id/primary flags) לא נגעים**; סטטוס סופי `completed`/`partial`/`failed`; `error_details` jsonb עם עד 50 שורות כושלות `{row,apartment_number,error}`; `fs.unlink` ל-temp ב-finally. **merge בלבד** — לא נחשף parameter `mode` כלל, אין replace. **Migration 022** הרחיבה את `import_runs` (additive+idempotent): עמודת `kind` (CHECK debtors/contacts, default 'debtors', index) + `file_name`/`failed_rows`/`error_details` + הרחבת `import_runs_status_check` ל-superset (debtors: running/success/error; contacts: parsing/processing/completed/partial/failed). `createImportRun` של debtors עודכן לכתוב `kind='debtors'` explicit (שורה 1). מיפוי תשובות: created←created_rows, error_summary←error_message, created_by reuse של `initiated_by`. **בדיקות** `scripts/test-contacts-import.sh` (בונה Excel עם SheetJS — placeholder '000000000', שדות ריקים, שורה בלי דירה, שורה כפולה, ושורת clobber): **14/14 עברו** — created=3/updated=2/skipped=1/failed=0, '000000000'→null, owner_phone ריק **לא דרס** טלפון קיים, note ידני לא נגע, runs history. הותקן דרך `npm run deploy`, service active, `/api/health`=200. **UI מחכה לעיצוב מ-Claude Design.**
- 13/06/2026 — **Slice 4 Track D — UI של רשימת דיירים הושלם.** דף `/contacts` (`(app)/contacts/page.tsx` Server Component → `listContacts({})` initial data → `ContactsPageClient`). RBAC: view לכל מאומת (מודול `contacts` כבר ב-permissions — manager/viewer view), **edit לאדמין בלבד** (`canEdit = super_admin||admin`, תואם ל-guards של ה-endpoints). Sidebar: הפריט "רשימת דיירים" הצביע ל-placeholder — נוסף `href: '/contacts'`. **כל העיצוב לפי DESIGN.md** (לא הומצא סטייל): כותרת `text-2xl font-extrabold` + subtitle "{count} דירות במערכת"; פעולות (admin) "ייבוא Excel"/"דייר חדש" כחול brand + "ייצוא CSV" outline; חיפוש Input `h-10 ps-9` עם אייקון Search ב-start; שורת תגיות toggle (scroll אופקי, every-semantics ב-backend); **טבלה** (`contacts-table.tsx`) לפי §9 (דירה/בעלים/טלפון בעל/שוכר/טלפון שוכר/badge סוג/פעולות) עם badge resident_type (§10: owner=כחול/tenant=סגול/operator=כתום), **mobile (`md:hidden`) cards** לפי §9b, שורות clickable→פאנל, empty-state "אין דיירים. ייבא קובץ Excel או הוסף ידנית."; טלפונים `dir=ltr tabular-nums` + `formatPhoneDisplay`/`phoneTelHref`. **ייבוא Excel דרך Side Panel** (`contact-import-panel.tsx`, Sheet `side="left" sm:w-[55vw]` §12): drag-and-drop 200px border-dashed, `POST /api/contacts/import`→polling `status/[runId]` כל 500ms (timeout 5דק'), Progress bar "מייבא {processed} מתוך {total}", סיכום ירוק "{created} נוצרו, {updated} עודכנו" + שורות צהוב/אדום ל-skipped/failed, collapsible "הצג שגיאות", auto-close 2ש' + `router.refresh()`+refetch ב-completed/failed=0, השארה פתוחה ב-partial/failed. **יצירה/עריכה דרך Side Panel** (`contact-form-panel.tsx`, אותו Sheet, §12 — **לא Dialog**): קומפוננטה אחת create+edit, apartment_number immutable בעריכה (disabled), Select סוג דייר, סקשנים §8 (פרטי בעל הדירה / פרטי השוכר [רק tenant] / פרטים נוספים) עם `Section`+`Field`+`PanelFooter` המשותפים, checkbox "מקבל הודעות" (primary flags), chip-input לתגיות (Enter מוסיף/X מסיר), ולידציית טלפון `validatePhone` §7, AlertDialog confirm-on-close, toast §13, צופה → read-only. **ייצוא CSV** client-side Blob (8 עמודות עברית + BOM, `contacts_YYYY-MM-DD.csv`). **אין מחיקה ב-UI** — החלטה מודעת: contacts מתעדכנים בייבוא Excel חוזר, מחיקה ידנית מסכנת נתונים שיחזרו; דייר שעזב → הסרה מהקובץ + ייבוא; V2 ישקול soft-archive. אומת חי: `/contacts` unauth→307, authed→200 (h1/count/actions/empty-state), טבלה מרנדרת שורות אמיתיות server-side (apt/שם/badge בעלים+שוכר/tag chip), `GET /api/contacts`→`{items}`. Build נקי, `npm run deploy`, service active, `/api/health`=200. אין placeholder ל-Contacts ב-DESIGN.md (לא נדרשה הסרה).
- 12/06/2026 — **WhatsApp — תגובתיות real-time (SSE + optimistic UI).** מדידה הראתה שהשרת מהיר (webhook→DB ‎20–40ms, Green API RTT ‎180–460ms) — צוואר הבקבוק היה ה-**polling כל 5ש'** (נכנסות+סטטוסים, עד 5000ms עיכוב) וחוסר **optimistic** בשליחה. נבנה: event bus בתהליך (`src/lib/whatsapp-events.ts`, מעוגן על `globalThis` כדי לשרוד כפילות מודולים ב-Next), endpoint `GET /api/whatsapp/stream` (SSE, מאומת ב-session, **מסונן fail-closed לפי instance**, heartbeat 25ש', `X-Accel-Buffering:no` — אין שינוי nginx; הקונפיג כבר עם `proxy_http_version 1.1` + `proxy_read_timeout 300s`). ה-webhook פולט `message_received`/`message_status` (רק על insert/advance אמיתי), נתיב chat-send פולט `message_sent`. הלקוח (`MessagesClient`): `EventSource` עם reconnect+backoff שעושה **עדכוני נקודה** (upsert ל-thread, V מונוטוני, bump לרשימה) ללא refetch; ה-polling הוּרד ל-**fallback** (30ש' כש-SSE חי, 4ש' כשנפל). שליחה **אופטימית**: בועה מיידית `pending` → `sent`/`failed`. שליחה אופטמה ל-query יחיד (`resolveSendCreds`). **אומת חי בפרודקשן** (session מזויף + EventSource): שלושת סוגי האירועים הגיעו תוך **פחות משנייה** מה-webhook, מסוננים ל-instance הנכון. עבר **code-review אדוורסרי רב-עדשתי** (29 agents); תוקנו 14 ממצאים מאומתים — הקריטי: דליפת אירועים של instance אחר כש-`instanceId=null` (תוקן ל-fail-closed); ה-high: reconnect-storm ב-401 (תוקן עם זיהוי fatal); וכן dedup ל-self-echo, הגנה מ-stale-response, network-unknown ללא כפילות, cancel()/abort cleanup ב-SSE.

- 13/06/2026 — **Module 2 — תקלות (Issues) הושלם** מקצה-לקצה והותקן בפרודקשן (`npm run deploy`, service active, `/api/health`=200, `/issues`=307, `/api/issues`=401). smoke E2E מאומת **14/14** (יצירה→notification→העלאת תמונה PNG+דחיית TXT→שינוי סטטוס→חסימת resolve ללא הערות→resolve עם הערות+resolved_at→**create-task עם קישור דו-כיווני מאומת** task.issue_id↔issue.linked_task_id→409 על משימה כפולה→404 על id לא-UUID). **בנוי על תשתית Module 1** — שימוש חוזר ב-`notifications`/`reminders` (אין שכפול). ראה הסיכום המבני "Module 2 — תקלות" למטה.
- 13/06/2026 — **Module 4 — צ׳אט פנימי (Internal Chat) הושלם** מקצה-לקצה והותקן בפרודקשן (`npm run deploy`, service active, `/api/health`=200, `/chat`=307, `/api/chat/conversations`=401). צ׳אט בין משתמשי המערכת (direct 1:1 + קבוצות). **בנוי על תשתית Module 1** — שימוש חוזר ב-`notifications` (אין שכפול). **החלטה ארכיטקטונית: polling, לא SSE/WebSocket** — Next standalone מאחורי nginx+systemd, polling פשוט/יציב/מספיק לצוות קטן; שיחה פתוחה כל 4ש' (delta דרך `?since=`), רשימה+מוני לא-נקרא כל 20ש'. **Migration 026** (additive, הורצה ידנית `psql "$DIRECT_URL"` — `deploy.sh` לא מריץ מיגרציות): `internal_conversations` (type direct/group CHECK, name לקבוצות, created_by snapshot, updated_at שמתעדכן בכל הודעה) + `internal_conversation_participants` (FK cascade, `last_read_at` מניע את מונה הלא-נקרא, unique (conversation_id,user_id)) + `internal_messages` (sender snapshot, content text CHECK 1..4000, index על (conversation_id,created_at)). **DB layer** `src/lib/db/internalChat.ts` — כל גישה participant-scoped (`isParticipant` הוא ה-gate היחיד; `listConversations` מעוגן על שורות ה-participant של המשתמש כך ששיחה זרה לעולם לא תופיע). `findExistingDirect`+re-check בתוך טרנזקציה → **dedupe של direct** (לא נוצרת כפילות). `postMessage` בטרנזקציה: insert + bump updated_at + fan-out `createNotification` לכל משתתף אחר עם `dedupeKey=internal_chat:{conv}:{recipient}:{YYYY-MM-DD}` (התראה אחת ליום לשיחה לכל נמען — לא מציף). **Endpoints** (כולם `requirePermission('internal_chat','view')`, גישה ל-data ברמת participant): `GET/POST /api/chat/conversations` (list עם last-message+unread / יצירת direct\|group), `GET/POST /api/chat/conversations/[id]/messages?since=` (thread/delta + mark-read / שליחה), `GET /api/chat/users` (picker, מסנן את עצמך). **UI** `/chat` (sidebar — הפריט "צ׳אט פנימי" קיבל `href`): פריסת שני פאנלים RTL (רשימה מימין, חלון משמאל), אווטאר ראשי-תיבות על פלטת **brand** (וריאציה מכוונת מ-WhatsApp הירוק), זמן יחסי עברי ("עכשיו"/"לפני N ד׳"/"אתמול"), badge לא-נקרא; חלון: בועות (שלי=brand מיושר לימין, אחרים=לבן לשמאל, שם השולח בקבוצות), הפרדת ימים, גלילה-לתחתית חכמה (רק אם המשתמש כבר בתחתית), composer Enter=שליחה/Shift+Enter=שורה. **"שיחה חדשה" = Dialog קטן** (חריג מאושר — בחירה, לא טופס ישות): direct=בחירת משתמש יחיד, group=בחירה מרובה (checkbox) + שם. **QA**: typecheck נקי, build נקי, smoke חי דרך HTTP מול הפרודקשן עם שני ה-super_admins (sessions ישירות ב-DB): direct create + **dedupe (אותו id)** + send + **since-delta=בדיוק 1** + **unread=1 ואיפוס ל-0 אחרי קריאה** + group (3 משתתפים) + edge cases (self→400, empty→400, ineligible-uuid→400). נתוני ה-smoke נוקו לחלוטין (טבלאות ריקות). **אבטחה** (סקירת Security Agent עצמאית — 7 הבדיקות הקריטיות PASS): **IDOR חסום מאומת חי** — משתמש שאינו משתתף מקבל **403** ב-GET וב-POST, שיחה זרה לא מופיעה ברשימתו, ו-403 לשיחה לא-קיימת זהה ל-403 forbidden (אין existence-oracle); **אין `dangerouslySetInnerHTML`** (render דרך `{m.content}` — React escaping); פרמטריזציה מלאה (כולל multi-row insert — רק placeholders משורשרים, ערכים ב-params). תוקנו 2 ממצאים: (1) **MEDIUM — allow-list לחברות**: `member_ids`/`user_id` עברו `filterEligibleUserIds` (משתמשים active קיימים בלבד) → לא ניתן להזריק user_id שרירותי/מנוחש לשיחה (forced-subscription/notification-spam נחסם); (2) **LOW — תקרת קבוצה** `MAX_GROUP_MEMBERS=50` חוסמת fan-out לא-מוגבל. תוקן גם באג precision ב-since-delta: timestamptz נשמר ב-microseconds אבל JSON מחזיר milliseconds → השוואה גולמית החזירה את הודעת הגבול; תוקן ל-`date_trunc('milliseconds', m.created_at) > $2`. RBAC ברמת-מודול (כל התפקידים רשאים) — גישת data תמיד participant-scoped.
- 13/06/2026 — **Module 3 — יומן (Calendar) הושלם** מקצה-לקצה והותקן בפרודקשן (`npm run deploy`, service active, `/api/health`=200, `/calendar`=307, `/api/calendar`=401). **בנוי על תשתית Module 1** — תזכורות דרך מנוע ה-`reminders` הגנרי (אין שכפול). **Migration 025** (additive, הורצה ידנית מול ה-DB עם `psql "$DIRECT_URL"` — `deploy.sh` לא מריץ מיגרציות): טבלת `calendar_events` (אירוע/פגישה, תאריך+שעות+כל-היום, `color_key` מפלטת DESIGN.md, status, owner FK→users, שדות חזרתיות, `parent_series_id` FK עצמי cascade, `is_exception`, `source_type` manual/generated_occurrence; אינדקסים על event_date ו-parent_series_id) + `calendar_event_participants` (polymorphic user/contact, attendance_status, unique על (event_id,source,id)). **חזרתיות = מופעים מטריאליים**: בשמירת אירוע חוזר נכתב שורת-parent (recurrence_enabled=true) + שורות occurrence (`parent_series_id`=parent), בתקרת **78 מופעים או 18 חודשים** (הנמוך), מימוש pure ב-`src/lib/calendar/recurrence.ts` (clamp לחודשים קצרים, 8 unit-tests ב-`tests/calendar-recurrence.test.ts`). עריכת מופע בודד → `is_exception=true`. עריכה/מחיקה "מכאן והלאה" — דיאלוג scope (כאן Dialog מותר, זה אישור), scoped ל-`seriesRoot=parent_series_id ?? id` עם `FOR UPDATE` — **בידוד סדרות מלא**, exceptions ושורות לפני העוגן שורדים. הכל בטרנזקציות (`withTransaction`). **API**: `GET /api/calendar?from&to` (from/to חובה, regex, מסודרים, תקרת 400 ימים≈13 חודש→`range_too_large`; מחזיר אירועים+משימות-עם-due_date בטווח כ-`kind:task` read-only מקושרות ל-`/tasks?task=`), `POST /api/calendar/events`, `GET/PATCH/DELETE /api/calendar/events/[id]` (PATCH/DELETE עם `scope=single|future`), `GET /api/calendar/participants?search` (מאחד active-users + contacts). הרשאות: מודול `calendar` (כבר היה במטריצה — manager view+edit, viewer view-only), edit ל-mutations. תזכורת נשמרת ב-`reminders` הגנרית עם `entity_type='calendar_event'`, מעוגנת ל-series-root; offset 15ד'/שעה/יום לפני; ה-engine קיבל ענף `calendar_event` (title/location/שעה, **`action_url=/calendar`**, `sourceModule='calendar'`). **UI** `/calendar` (sidebar): תצוגות **חודש/שבוע/יום** native grid (לא ספריית calendar) — RTL מלא, שבוע מתחיל בראשון, שמות ימים/חודשים בעברית; אירועים כבלוקים צבעוניים לפי color_key, משימות באייקון מקווקו → ניווט ל-`/tasks`; לחיצה על תא/אירוע פותחת **Side Panel** (Sheet side=left 55vw, §12) ליצירה/עריכה: כותרת/סוג/סטטוס/תאריך/שעות/כל-היום/מיקום/תיאור/בורר-צבע/בעלים, חזרתיות מתקפלת (Switch), בורר משתתפים עם חיפוש debounced (משתמשים+אנשי קשר), תזכורות; dirty-tracking + AlertDialog confirm-exit; deep-link `?event=<id>`. **QA**: typecheck נקי, build נקי, **124/124 vitest** (8 recurrence + 9 validation חדשים), smoke DB-layer **8/8** (single, series-of-5, exception-edit, delete-future עם שמירת exceptions, series-isolation, updateSeriesFuture isolation, reminder→cron, range) + smoke חי דרך HTTP cron (`POST /api/cron/reminders` עם secret → `due:1 processed:1 notified:1`, notification action_url=/calendar) + tasks-overlay (task due_date מופיע בטווח/מסונן מחוץ). **אבטחה**: auth על כל endpoint, פרמטריזציה מלאה (שמות עמודות מ-allowlist `WRITABLE_COLUMNS` בלבד), from/to bound, בידוד סדרות מאומת. סקירת Security Agent עצמאית → תוקנו 2 ממצאים: (1) **UUID guard** ב-`GET/PATCH/DELETE /api/calendar/events/[id]` לפני כל גישת DB → `id` לא-UUID מחזיר 404 (לא 500/22P02 דלף), עקבי עם Module 2; (2) ולידציית תאריך round-trip דרך `parseDateOnly` (יוּצא מ-`recurrence.ts`) ב-`event_date` וב-recurrence `until_date` → תאריכים בלתי-אפשריים (2026-02-30, 29/2 בשנה לא-מעוברת) נדחים במקום לגלוש. ‏RBAC ברמת-מודול (לא per-row) — by-design, עקבי עם tasks/issues.

- 13/06/2026 — **Module 3 — בורר משתתפי פגישה שונה: משתמשי מערכת (picker) + משתתפים חיצוניים (טקסט חופשי).** **contacts הוסרו מהבורר.** ההבחנה: **משתתפים רשומים = משתמשי מערכת בלבד** (picker, יש להם login) → `participant_source='user'` עם `participant_id`; **משתתפים חיצוניים = טקסט חופשי** (עו"ד, קבלן, נציג עירייה — תיעוד נוכחות בלבד, בלי login ובלי picker) → `participant_source='external'`, `display_name_cache` בלבד, **`participant_id=NULL`**. **רשומות `contact` היסטוריות נשמרות לקריאה בלבד** — מוצגות תחת "משתתפים חיצוניים" כשם (chip עם תווית "(איש קשר)"), ה-UI לעולם לא יוצר חדשות מסוג contact (בפועל: 0 שורות contact קיימות, 1 שורת user). **Migration 027** (`027_calendar_external_participants.sql`, additive+idempotent, הורצה ידנית `psql "$DIRECT_URL"` — `deploy.sh` לא מריץ מיגרציות): הרחבת `calendar_event_participants_participant_source_check` ל-`('user','contact','external')` (DROP IF EXISTS → ADD באותו שם; 'contact' נשמר לתאימות אחורה) + `alter column participant_id drop not null` (external = NULL; אינדקס הייחודי `(event_id,source,id)` ממשיך לעבוד כי כמה NULLs נבדלים ב-Postgres → כמה externals באותו אירוע לא מתנגשים). אומת ב-`pg_get_constraintdef` + `is_nullable=YES`; **אפס מחיקת נתונים** (counts נשמרו). **endpoint** `GET /api/calendar/participants` שוכתב — שולף **אך ורק** `listAssignableUsers()` (`is_active=true`), הוסרו import+call ל-`listContacts`; התגובה `{users:[{source,id,name}]}` בלבד (אין מפתח `contacts`, אין email/phone/role/hash). הצרכן היחיד הוא `event-form-panel.tsx` (אומת ב-grep). **ולידציה** `coerceParticipants` (`src/lib/validation/calendar.ts`): ל-`external` — שם trimmed לא-ריק (`invalid_external_name`), ≤100 (`external_name_too_long`), ו-**`participant_id` נכפה NULL בשרת** (לא נסמך על הקליינט); ל-`user` — UUID חובה; ל-`contact` (legacy) — שומר id-תקין-או-NULL+שם (round-trip לא מפיל רשומה ישנה); dedupe (user/contact לפי id, external לפי שם case-insensitive); תקרת 200. **UI** (`event-form-panel.tsx`, באזור "משתתפים" בתוך ה-Side Panel הקיים — לא שונה ה-Sheet): שני חלקים — **"משתתפים רשומים"** (picker משתמשים בלבד, badge ראשי-תיבות, נבחר לא חוזר בתוצאות, click-outside סוגר dropdown) + **"משתתפים חיצוניים"** (Input טקסט חופשי + כפתור/Enter → chip עם X להסרה, ולידציית client תואמת שרת: trim/≤100/no-dup). שמירה: registered→user+id, externals→external+NULL. טעינה לעריכה: פיצול ל-2 החלקים לפי source (contact היסטורי → external read-only). **QA**: tsc נקי, build נקי, **vitest 22/22** (`tests/calendar-validation.test.ts` — 13 בדיקות חדשות ל-coerceParticipants כולל אכיפת NULL ל-external). **smoke חי בפרודקשן** (session אמיתי ב-DB): endpoint החזיר **0 contacts** (2 users, כל source=user, שדות {id,name,source} בלבד); מחזור צור (1 user + 2 externals, כולל שליחת UUID זדוני ב-external) → **DB אישר external.participant_id=NULL** → load (3 חוזרים לחלקים הנכונים) → הסרת external אחד → save → DB אישר 2 שורות (השלישי נמחק) → שרת דחה שם >100 (400) ושם ריק (400); נתוני smoke נוקו לחלוטין (counts חזרו ל-1 user/0 contact/0 external). **deploy**: `npm run deploy`, service active, `/api/health`=200, `/calendar`=307.
- 13/06/2026 — **Module 3 — בעלים של אירוע יומן = נגזר מה-session של היוצר, לא נבחר.** עד כה שדה "בעלים" היה dropdown לבחירת משתמש בטופס היצירה/עריכה — תבנית שגויה (פגישה שייכת למי שיצר אותה). **התיקון (אין מיגרציה — אדיטיבי, code-only):** (1) **הוסר dropdown הבעלים מהטופס לחלוטין** (`event-form-panel.tsx`) — כולל `owner_user_id` מ-`FormState`/`emptyForm`/`fromEvent`/`buildBodyCore` והפרופ היתום `currentUserId` (הוסר גם מ-`calendar-page-client.tsx` ומה-page). במקומו **תווית קריאה-בלבד עדינה** `text-xs text-slate-500` עם אייקון `UserCircle`: "נוצר ע״י: {שם}" — בעריכה שם היוצר המקורי (נגזר מ-`loadedEvent.owner_user_id` דרך רשימת ה-`owners` הקיימת), ביצירה שם המשתמש המחובר (פרופ `currentUserName` חדש = `actor.full_name ?? actor.username`). (2) **POST** (`api/calendar/events/route.ts`): `owner_user_id` **תמיד** `actor.id` מה-`requirePermission('calendar','edit')` — הוסר ה-fallback `?? fields.owner_user_id`; payload של הקליינט **לא יכול לדרוס** בעלות. (3) **ולידציה** (`coerceEventInput`): `owner_user_id` **לא נקרא יותר** מה-body ולא נכתב ל-`fields` (גם create וגם update) — שכבת הגנה שנייה. (4) **עריכה (PATCH)**: ב-`calendarEvents.ts` הופרד `WRITABLE_COLUMNS` לשניים — `INSERT_COLUMNS` (כולל `owner_user_id`, רק ל-insert) ו-`UPDATABLE_COLUMNS` (**ללא** `owner_user_id`) שבו משתמשים `updateSingleEvent` ו-`updateSeriesFuture` → **owner לא ניתן לשינוי בעריכה** בשום scope. **owner לא ניתן לזיוף דרך payload, לא ב-POST ולא ב-PATCH.** הבורר של משתמשים (`/api/calendar/participants`) נשאר ללא שינוי — הוא משרת את בורר המשתתפים, session-gated, id+name בלבד. **QA**: tsc נקי, build נקי. **smoke חי בפרודקשן** (session אמיתי ב-DB, 3/3): (א) יצירה ללא owner → DB owner == המשתמש המחובר; (ב) יצירה עם `owner_user_id` של משתמש אחר ב-payload → השרת **התעלם**, owner נשאר המחובר; (ג) PATCH עם owner מזויף + שינוי כותרת → הכותרת השתנתה (PATCH רץ) אך owner **לא השתנה**. נתוני smoke נוקו. **אבטחה**: סקירת Security Agent עצמאית — כל 4 התכונות CLEAN (אין forgery ב-create, owner immutable ב-edit single+future, אין דליפת users, התווית display טהור/escaped). הגנה רב-שכבתית (ולידציה מסירה + column-list מחריג + route כופה). RBAC ברמת-מודול נשמר (`calendar:edit` עורך כל אירוע — by-design, עקבי עם tasks/issues; owner אינו gate הרשאות).

- 13/06/2026 — **Module 1 (משימות) — סגירת פערי-ספק ל-backend, additive בלבד (אין rebuild).** התבקש "לבנות backend למשימות מקצה-לקצה" אך התגלה ש-**המודול כבר קיים במלואו** (Migration 023 + `/api/tasks` CRUD מלא דרך `@/lib/db/tasks` + RBAC `tasks` במטריצה view/edit + sidebar "משימות" + UI חי `/tasks`, kanban/table/side-panel + overlay ביומן + קישור issue→task). **החלטה: לא לבנות מחדש ולא לשנות שמות עמודות** (היה שובר את ה-UI/calendar/reminders/notifications) — רק לסגור additive את הפערים מול ה-spec. **Migration 028** (`028_tasks_completed_at_related_entity.sql`, additive+idempotent, **טרם הורצה על ה-DB** — להריץ ידנית `psql "$DIRECT_URL" -f ...` כמו 025/026/027; `deploy.sh` לא מריץ מיגרציות): על `public.tasks` בלבד — `completed_at timestamptz`, `related_entity_type text` (CHECK 'debtor'/'building'/'supplier'/'contact'), `related_entity_id uuid` (ללא FK — polymorphic; כל ישות לינקבילית היא uuid; ל-'building' אין עדיין טבלה), + אינדקס `tasks_related_entity_idx`. (אינדקסי assigned_to/status כבר קיימים מ-023.) **soft-delete**: `DELETE /api/tasks/[id]` שונה מ-hard-delete ל-**`is_archived=true`** (`deleteTask` עכשיו UPDATE; idempotent; reminders עדיין מנוקים כדי שמשימה מחוקה לא תצלצל). אין caller ל-DELETE ב-UI → לא נשבר דבר; listTasks ממילא מסנן `is_archived=false`. **completed_at נגזר מ-status בלבד (לא client-writable)**: נחתם `now()` במעבר ל-`done` (שומר זמן קיים אם כבר done), מתאפס ל-NULL ביציאה מ-`done` — מיושם ב-`createTask`/`updateTask`/**`reorderTasks`** (גרירת kanban ל/מ-done עקבית; הביטוי ב-SET מסתמך על ערכי-השורה הישנים בסמנטיקת UPDATE של Postgres). **related_entity additive ליד `debtor_id` שנשאר ה-link הראשי לדייר** — ללא data-migration, בלי לגעת בקוד שמשתמש ב-debtor_id; נוסף ל-`WRITABLE_COLUMNS`+ולידציה (`coerceTaskInput`: type מול allow-list, id כ-UUID) + פילטרים `relatedEntityType`/`relatedEntityId` ל-`listTasks` ול-`GET /api/tasks`. **audit_log נדחה במכוון**: אין טבלת audit_log מרכזית בכל הפרויקט — דרישת ה-audit-on-mutation היא החלטת-תשתית רוחבית (משפיעה על כל מודול), לא נכון להזריק audit ספציפי-ל-tasks שיהיה לא-עקבי. נרשם כ-TODO תשתית נפרד (ראו למטה). **אי-התאמה בין-מודולית מתועדת (כבקשת המשתמש, בלי לשנות עמודות קיימות)**: soft-delete ב-`tasks` = `is_archived boolean`, בעוד ב-`debtors` = `archived_at timestamptz` (Migration 012). שתי קונבנציות soft-delete תקפות אך שונות בצורה; יושרו עתידית רק במסגרת החלטת-תשתית מכוונת. **QA**: `npm run build` נקי (כולל postbuild→standalone); לא בוצע `systemctl restart` (לפי הנחיה). הקוד נדחף לגיט; המיגרציה והפריסה (`npm run deploy`) הם צעד ידני נפרד.
- 13/06/2026 — **תשתית audit_log מרכזית — הוקמה (Migration 029 + helper).** סוגר את ה-TODO שנפתח במודול tasks: עד כה לא הייתה טבלת audit מרכזית — רק trails פר-ישות (`debtor_events`/`completed_actions`/`legal_status_history`), ו-`suppliers`/`tasks` ציינו "central audit deferred". audit מקדים אישר: **אין** מנגנון מרכזי קיים → הוקם additive בלבד, בלי לגעת ב-trails הקיימים. **Migration 029** (`029_audit_log.sql`, additive+idempotent, **טרם הורצה על ה-DB** — להריץ ידנית `psql "$DIRECT_URL" -f ...`; `deploy.sh` לא מריץ מיגרציות): טבלת `public.audit_log` **append-only** (אין `updated_at`, אין trigger) — `id uuid`, `actor_user_id uuid` **FK→users `on delete set null`** (שורת audit שורדת מחיקת משתמש → actor=NULL; NULL מכסה גם אירועי system), `action text not null`, `entity_type text not null`, `entity_id text` (polymorphic, **ללא FK**, text ולא uuid כדי לתמוך בכל ישות; NULL לאירועי bulk/global), `changes jsonb` (diff before/after מומלץ), `metadata jsonb` (context חופשי), `created_at timestamptz default now()`. **`entity_type`/`action` ללא CHECK נוקשה** — פתוח להרחבה בלי מיגרציה. אינדקסים: `(entity_type, entity_id)` (היסטוריית ישות — השאילתה הראשית), `actor_user_id`, `created_at desc`. **הבחנת תכנון מ-`debtor_events`**: שם actor הוא snapshot (לא FK) ששורד מחיקה; כאן בחירה מודעת ל-FET+set-null (לפי ה-spec) — מי שרוצה תווית-actor עמידה ישמור אותה ב-`metadata`. **Helper** `src/lib/db/audit.ts` — `writeAudit({ actorUserId?, action, entityType, entityId?, changes?, metadata? })`, כותב דרך ה-Pool הקיים (`@/lib/db` `query`), jsonb עם `$n::jsonb` + `JSON.stringify` (קונבנציית הפרויקט). **fail-safe**: עטוף ב-try/catch שמלוגג (`console.error`) ולא זורק — כשל audit לעולם לא יפיל את הפעולה העסקית; לקרוא **אחרי** שהכתיבה העסקית הצליחה (לא חלק מהטרנזקציה). **callers טרם חוברו** — חיווט הדרגתי פר-מודול (debtors/tasks/וכו') בהמשך; כרגע תשתית+helper בלבד. **QA**: `npm run build` נקי (כולל postbuild). אין route handler במשימה → breaking changes של Next 16 ב-route handlers לא רלוונטיים; `import 'server-only'` כמו שאר `src/lib/db/*`. לא בוצע `systemctl restart`, לא force-push.
- 13/06/2026 — **מודול תזכורות (Reminders) — backend עצמאי `user_reminders`, additive לצד מנוע התזכורות הקיים.** audit מקדים גילה ש-`public.reminders` (Migration 023) הוא **מנוע תזמון-נוטיפיקציות** (entity_type/entity_id-uuid/user_id/remind_at/channel/sent_at, נסרק ע"י `src/lib/reminders/engine.ts`+`/api/cron/reminders` → יורה in-app/email → `sent_at`; צרכנים: tasks/calendar/issues), **לא** מודול תזכורות עצמאי (אין title/status/owner/CRUD/UI). ה-RBAC module `reminders` והפריט בסיידבר "תזכורות" היו **placeholders שמורים** (רק הסיידבר הצביע ל-`reminders`; אף route לא אכף `requirePermission('reminders')`), ו-`/api/reminders` היה פנוי. עצרתי ודיווחתי (כבקשת ה-gate). **החלטת המשתמש**: לבנות מודול עצמאי בשם **`user_reminders`** לצד המנוע — **בלי לגעת בטבלת `reminders`/cron** — items עצמאיים (לא מחווטים למנוע; spec ללא channel/sent_at). **Migration 030** (`030_user_reminders.sql`, additive+idempotent, **טרם הורצה על ה-DB** — להריץ ידנית `psql "$DIRECT_URL" -f ...`): `public.user_reminders` — `id uuid`, `title text not null`, `remind_at timestamptz not null`, `status text not null default 'pending'` (CHECK pending/done/dismissed), `entity_type text`/`entity_id text` (polymorphic, **ללא CHECK**, text-id, שניהם nullable), `assigned_to uuid FK→users on delete set null`, `created_by uuid not null FK→users` (ללא on-delete = NO ACTION: יוצר עם תזכורות לא נמחק hard; משתמשים מנוטרלים, לא נמחקים), `completed_at timestamptz`, `is_archived boolean not null default false` (soft-delete, עקבי עם tasks), `created_at`/`updated_at` + trigger `touch_updated_at`. אינדקסים: `remind_at`, `assigned_to`, `(entity_type, entity_id)`, `status`. **API path — נבדק והוחלט**: `/api/reminders` היה פנוי (אין route), אך נבחר **`/api/user-reminders`** לעקביות עם הטבלה/מודול ולמניעת בלבול עם namespace המנוע (`/api/cron/reminders`). **DB layer** `src/lib/db/userReminders.ts` (דרך ה-Pool הקיים): list עם פילטרים (status/assignedTo/entityType/entityId/**due**=`remind_at<now() and status='pending'`), get, create, update, **soft-delete** (`is_archived=true`); `completed_at` **נגזר מ-status** (now() במעבר ל-done ושומר זמן קיים, NULL ביציאה מ-done — ב-create+update), עקבי עם הפטרן של tasks; timestamptz מוחזר `::text` (RSC-safe). **Routes** `src/app/api/user-reminders/{route,[id]/route}.ts`: `GET/POST` + `GET/PATCH/DELETE` (DELETE=soft), כולם `requirePermission('user_reminders', view/edit)` (אימות דרך getSession → actor; super_admin/admin עוקפים), ולידציה server-side ב-`src/lib/validation/userReminders.ts`, UUID-guard על `[id]` (לא-UUID→404, עקבי עם calendar/issues). **audit — caller ראשון של תשתית 029**: כל create/update/delete קורא `writeAudit` (`entity_type='reminder'`, action created/updated/deleted, `changes={before?,after?}`, actor=session); fail-safe (לא מפיל את הפעולה). **RBAC**: ה-module key `reminders` השמור-והלא-בשימוש **שונה ל-`user_reminders`** ב-`MODULES`+`DEFAULT_MANAGER`+`DEFAULT_VIEWER` (label "תזכורות" נשמר) — כדי לא ליצור כפילות "תזכורות" במטריצה; היה מופנה רק מהסיידבר. manager view+edit, viewer view-only, אין delete נפרד. **Sidebar**: הפריט "תזכורות" (אייקון `Bell`) קיבל `module:'user_reminders'` (הדף עצמו יגיע בסשן UI — אין UI מעבר לרישום). **[עודכן 15/06/2026]**: ה-`href:'/user-reminders'` שניתן כאן במקור **הוסר** — דף ה-UI לא נבנה, וקישור פעיל לדף שלא קיים הוביל ל-404. הפריט חזר ל-placeholder לא-קליקבילי (מציג "בקרוב", `cursor-not-allowed`), עקבי עם שאר הפריטים שה-backend שלהם קיים אך אין דף UI (מסמכים/ייצוא/ניהול אזורים/לוח מחוונים). את ה-href יש להחזיר רק כשייבנה הדף. **[עודכן שוב 15/06/2026 — הוחזר]**: דף ה-UI `/user-reminders` נבנה בסשן זה (ראו רשומת ה-categories+UI למטה), וה-`href:'/user-reminders'` **הוחזר** לפריט הסיידבר (פעיל, עטוף ב-`Link`) — מבטל סופית את ה-404. **QA**: `npm run build` נקי (כולל postbuild; `/api/user-reminders` + `[id]` רשומים). מיגרציות 030 (וגם 029 אם עוד לא) להריץ ידנית לפני deploy. לא בוצע `systemctl restart`, לא force-push.
- 15/06/2026 — **WhatsApp Template Editor — שכבת-מראה (עיצוב בלבד) על אזור התוכן של חלונית עריכת/יצירת תבנית** (`whatsapp-templates/components/WhatsAppTemplateSheet.tsx`). אפס שינוי בשדות/תוויות/תוכן/צ'יפים/לוגיקה; ה-header (gradient) וכפתורי footer (שמור/ביטול) — לא נגעו (shell גנרי). אזור הגוף עוצב מחדש כ-2 עמודות עם **container query** (`@container` + `@2xl:grid-cols-[minmax(0,1fr)_19rem]` — טופס מימין, **תצוגה מקדימה חיה** משמאל; נערם מתחת לרוחב). **הטמעה דרך טוקנים קיימים בלבד** — לא הומצא אף טוקן חדש: פלטת ה-brand (`#3d5afe` ונגזרותיו) ופונט המספרים Inter (`font-num`) **כבר היו מחווטים** ב-`globals.css @theme` + `layout.tsx`; ה-prose של DESIGN.md (§1.4 "אין Inter", §2 "primary=blue-600") היה **מיושן** ולכן יושב מול ה-theme החי (לא היה conflict אמיתי שמחייב עצירה). **עודכן ב-DESIGN.md** (תיעוד השפה כחלק מה-MR): §1.4 (Heebo לטקסט / Inter למספרים דרך `font-num`); §2 — נוספה טבלת **Skin tokens** (`bg-brand`/`-dark`/`-soft`/`border-brand-border`/`text-brand-text`, `text-ink`/`-2`/`-3`/`-ghost`, `border-line`/`-soft`/`-strong`, `bg-app`/`bg-surface-2`/`bg-row-hover`, `shadow-soft-xs/sm/md`) + focus-ring מותגי (`ring-4 rgba(61,90,254,.12)`) + אדום חובה `#e5484d`; §3 — `font-num` למספרים/שעות/`{{var}}`; **§26 חדש** — variable-chips (pill brand-soft + hover מילוי מלא), שורת "משתנים נתמכים", **Active toggle card** + **`Switch size="lg"`** (תוספת **additive** ל-`ui/switch.tsx` — 52×30/ידית 24px; ענף ה-default ללא שינוי, אפס השפעה על call-sites קיימים; פעיל=ירוק `#16a34a`), ופלטת ה-**WhatsApp preview** (header `#075e54→#054c44`, צ'אט `#e5ddd5`+נקודות, בועה `#dcf8c6` עם פינה תחתונה-מובילה חדה, טקסט `#111b21`, meta+✓✓ `#667781`). הפריוויו הוא render טהור של ה-textarea (ללא interpolation; `{{var}}` מודגשים) — אפס data/logic. **כלל §21 כובד**: hover של הצ'יפים = מילוי צבע + צל רך **בלבד, ללא transform-lift** (המפרט ביקש "הרמה קלה"; DESIGN.md §21 אוסר hover-transform → נבחר DESIGN.md). אין שינוי API/DB/Next.js. QA: `tsc --noEmit` נקי, **`npm run deploy` בוצע כיחידה אחת** (build→restart→אימות `active` + תהליך חדש אחרי כתיבת BUILD_ID).

- 15/06/2026 — **מודול מסמכים (Documents) — backend גנרי (תיקיות מקוננות + קישור פולימורפי), additive לצד `supplier_documents`.** audit מקדים (ללא נגיעה בקוד) אישר: **אין** מודול מסמכים גנרי — אין טבלאות `documents`/`document_folders`, אין `/api/documents`, אין דף `/documents`. קיימים רק: (1) placeholder בסיידבר "מסמכים" (`module:'documents'`, **ללא href** → מוצג disabled עם tooltip "בקרוב"); (2) ה-RBAC module `documents` **כבר רשום** ב-`MODULES`+`DEFAULT_MANAGER` (view+edit)+`DEFAULT_VIEWER` (view) — לא נדרש שינוי; (3) מודול הספקים שמימש אחסון מסמכים **supplier-scoped** (`supplier_documents` FK→suppliers cascade, `supplierStorage.ts`, bucket `supplier-documents`, signed-URLs) — שימש כפטרן-מקור. אין כפילות → נבנה. **Migration 031** (`031_documents.sql`, additive+idempotent, **טרם הורצה על ה-DB** — להריץ ידנית `psql "$DIRECT_URL" -f supabase/migrations/031_documents.sql`; `deploy.sh` לא מריץ מיגרציות): שתי טבלאות לפי מבנה Base44. `public.document_folders` — `id uuid`, `name text not null`, `parent_folder_id uuid FK→document_folders **on delete set null**` (תיקיות מקוננות; hard-delete של תיקיה מרים תת-תיקיות לשורש ולא מוחק subtree — soft-delete הוא המסלול הרגיל), `created_by uuid not null FK→users` (NO ACTION — משתמשים מנוטרלים לא נמחקים), `is_archived boolean default false`, `created_at`/`updated_at`+trigger `touch_updated_at`. `public.documents` — `id uuid`, `folder_id uuid FK→document_folders **on delete set null**` (hard-delete של תיקיה מרחף מסמכים ל-unfiled, לא משמיד שורות), `file_name text not null`, `storage_path text not null`, `mime_type text` (nullable), `size_bytes bigint` (nullable), `entity_type text`/`entity_id text` (polymorphic — debtor/supplier/task, **ללא CHECK** נוקשה, text-id, שניהם nullable), `uploaded_by uuid not null FK→users`, `is_archived boolean default false`, `created_at`/`updated_at`+trigger. אינדקסים: `documents(folder_id)`, `documents(entity_type, entity_id)`, `documents(uploaded_by)`, `document_folders(parent_folder_id)`, `document_folders(created_by)`. **אחסון — אותו פטרן כמו suppliers** (`src/lib/storage/documentStorage.ts`): `StorageClient` ישיר (בלי supabase-js/realtime), **bucket פרטי `documents`**, `storage_path` שומר את ה-object PATH בלבד, ה-API מחזיר signed-URL קצר-טווח (TTL שעה). תוספות מעל הפטרן: `buildPrefix()` לארגון אובייקטים (`<entity_type>/<entity_id>` → `folder/<folder_id>` → `unfiled`), ו-`ensureBucket()` best-effort (יוצר את ה-bucket בהעלאה ראשונה, בולע "already exists"/409 → idempotent). **`SUPABASE_SERVICE_ROLE_KEY` חסר ⇒ שגיאה ברורה, לא כשל שקט**: ה-helper זורק `supabase_storage_not_configured`; route ההעלאה ממפה ל-**HTTP 503 `{error:'storage_not_configured'}`** + `console.error` (כשל אחסון אחר → 502 `upload_failed`). **DB layer** `src/lib/db/documents.ts` (דרך ה-Pool הקיים `@/lib/db`): folders CRUD (list עם counts של תת-תיקיות+מסמכים, get, create, update, **soft-delete** `is_archived=true`, `folderExistsActive`, **`isFolderInSubtree`** למניעת מעגלים בהזזה דרך recursive CTE) + documents CRUD (list עם פילטרים folderId/entityType/entityId/includeArchived, get, insert, update-metadata, soft-delete); timestamptz מוחזר `::text` (RSC/JSON-safe). **Routes** (כולם `runtime='nodejs'`, אימות דרך `requirePermission('documents', view/edit)` → getSession→actor; super_admin/admin עוקפים; UUID-guard על `[id]`→404): `GET/POST /api/documents` (POST=multipart upload, ולידציית type מול allowlist + גודל ≤25MB server-side), `GET/PATCH/DELETE /api/documents/[id]` (GET=metadata+signed-url טרי, PATCH=rename/move/re-tag/archive/restore — הקובץ immutable, DELETE=soft), `GET/POST /api/documents/folders`, `GET/PATCH/DELETE /api/documents/folders/[id]` (PATCH=rename/move/archive עם בדיקות self-parent + parent-active + cycle). הערה Next 16 routing: segment סטטי `folders` גובר על `[id]` הדינמי → אין התנגשות. ולידציה pure ב-`src/lib/validation/documents.ts`. **audit — caller של תשתית 029**: כל upload/update/delete של מסמך קורא `writeAudit` עם **`entity_type='document'`** (actions uploaded/updated/deleted), ופעולות תיקיה עם `entity_type='document_folder'` (open-text לפי תכנון 029) — `changes={before?,after?}`, actor=session, fail-safe. **RBAC**: ללא שינוי — `documents` כבר במטריצה (manager view+edit, viewer view, אין delete נפרד; soft-delete=edit). **Sidebar — החלטה מתועדת**: הפריט "מסמכים" **נשאר placeholder ללא href** (לא חובר ל-`/documents`) — חיווט href לדף שלא קיים היה מייצר 404 ושובר את UX ה-"בקרוב" המכוון (עקבי עם placeholders אחרים: `export`, `rooms_areas`). ה-href + הדף יחוברו בסשן ה-UI. אייקון `FileText` (lucide) כבר משויך. **QA**: `tsc --noEmit` נקי, **`npm run deploy` בוצע כיחידה אחת** (build→restart→אימות `active` + תהליך חדש אחרי כתיבת BUILD_ID; 4 ה-routes נרשמו ב-`.next/standalone`). מיגרציה 031 (וגם 029/030 אם עוד לא) להריץ ידנית לפני שימוש ב-API. לא בוצע force-push.

- 15/06/2026 — **מודול מסמכים (Documents) — דף UI `/documents` (frontend על ה-backend החי מ-031).** נבנה דף ניהול קבצים/תיקיות מלא מעל ה-API (folders + documents) שכבר חי. **DESIGN.md = מקור-האמת**: כל הוויזואל מהפטרנים הקיימים (הושווה ל-`/suppliers` ו-`/tasks`); התמונה הגדירה מבנה בלבד — **לא הועתקו צבעים/באנר ממנה** (הכותרת היא טקסט כהה על רקע בהיר לפי §3, לא הגרדיאנט הכחול שבתמונה). **לא הומצא אף טוקן ויזואלי חדש** → אין שינוי ב-DESIGN.md (שימוש ב-skin tokens קיימים: `border-line`/`bg-surface-2`/`bg-brand-soft`/`text-ink*`/`shadow-soft-xs` + tone-variants amber/violet/rose/emerald/blue). **קבצים**: `src/app/(app)/documents/page.tsx` (Server Component — `getCurrentActor`→redirect /login, gate `documents:view`→redirect /dashboard, מחשב `canEdit` ומעביר ל-client; `force-dynamic`, מירור של suppliers/page.tsx) + `src/components/documents/{DocumentsPageClient,FolderCard,DocumentCard,FolderFormPanel,DocumentRenamePanel,UploadPanel,helpers}.tsx`. **מבנה הדף**: כותרת "מסמכים" + תת-כותרת "ניהול קבצים ומסמכים מרכזיים" + counter (§3); toolbar עם חיפוש ("חיפוש בקבצים...", סינון client-side על השם בתצוגה הנוכחית) + בורר מיון ("שם"/"תאריך", client-side); **breadcrumb** מקונן (stack של `path:{id,name}[]`, ניווט לפי `parent_folder_id`); **dropzone** מקווקו (§ border-dashed, `bg-surface-2`, `bg-brand-soft` ב-drag); **רשת כרטיסים** רספונסיבית (`grid sm:grid-cols-2 lg:grid-cols-3`) — סקשן "תיקיות" (FolderCard, אייקון `Folder` amber, counts) + סקשן "קבצים" (DocumentCard, אייקון לפי mime). **פעולות (מחוברות ל-API)**: יצירת תיקייה→`POST /api/documents/folders` (עם `parent_folder_id`=התיקייה הנוכחית); העלאה (גם דרך drag-drop על הדף וגם כפתור)→`POST /api/documents` multipart; הצגה→`GET` עם `parentId`/`folderId` (root↔`parentId=root`, unfiled↔`folderId=unfiled`); שינוי-שם→`PATCH`; מחיקה (soft)→`DELETE`. **§12 כובד**: כל CREATE/EDIT דרך **Sheet** (`side="left"` `sm:w-[55vw]`, header gradient, `PanelFooter`) — `FolderFormPanel` (יצירה+שינוי-שם תיקייה), `DocumentRenamePanel` (שינוי-שם קובץ), `UploadPanel` (Sheet עם dropzone פנימי + רשימת קבצים מדורגת + סטטוס פר-קובץ + retry); **Dialog רק לאישור מחיקה** (`AlertDialog` destructive). **UploadPanel** הוא נתיב ההעלאה היחיד — גם ה-dropzone של הדף וגם הכפתור פותחים אותו (drop מזין קבצים staged); ולידציית client (`ALLOWED_DOC_TYPES`/`MAX_DOC_SIZE_BYTES`) לפני שליחה. **טיפול בשגיאות (§13)**: `documentErrorMessage()` ממפה קודי-שגיאה לעברית — **`storage_not_configured` (503, חסר `SUPABASE_SERVICE_ROLE_KEY`) → toast ברור "האחסון אינו מוגדר…"** (לא כשל שקט), `file_too_large`/`invalid_file_type`/`upload_failed`/`folder_cycle` וכו'. **צפייה/הורדה**: לחיצה על קובץ פותחת את ה-`signed_url` (synchronous מה-list, נמנע popup-blocker; ה-list מתרענן בכל ניווט/mutation). **toast** על כל mutation. **RBAC**: `view` → קריאה בלבד (אין כפתורי יצירה/העלאה/dropzone, אין rename/delete בכרטיסים — רק הורדה); `edit` → פעולות מלאות (הכל מאחורי `canEdit`). **Sidebar**: פריט "מסמכים" (אייקון `FileText`) **חובר ל-`href:'/documents'`** (הוסר מ-placeholder → פריט פעיל עטוף ב-`Link`) — מותר כעת כי הדף קיים. RTL מלא. **QA**: `tsc --noEmit` נקי, **`npm run deploy` כיחידה אחת** (build→restart→אימות `active`+תהליך חדש אחרי BUILD_ID); `/documents`→307 (קודם 404), `/api/documents/folders`→401, ה-route נבנה. לא בוצע force-push.

- 15/06/2026 — **מסמכים — תיקון באג העלאה (Supabase "Invalid key" על שמות עבריים) + עריכת שם לפני העלאה.** **שורש הבאג** (אומת מול ה-storage החי): `POST /api/documents` קרס עם `storage_upload_failed: Invalid key` כש-`storage_path` נבנה משם הקובץ המקורי הכולל תווים לא-ASCII — **Supabase Storage דוחה object keys שאינם ASCII**. כל העלאה עם שם עברי נכשלה (502 ללקוח). **התיקון — הפרדה מלאה בין שם-תצוגה ל-storage key**: ב-`src/lib/storage/documentStorage.ts` ה-key נבנה כעת כ-**`<prefix>/<uuid><.ext>`** — שם המשתמש **לא** חלק מה-key (רק UUID + סיומת ASCII דרך `extOf`); ה-`safeName` הישן (שהתיר את טווח העברית `֐-׿`) הוחלף ב-**`asciiSegment`** (`[^A-Za-z0-9._-]` → הסרה) שמשמש גם ל-prefix (folder/entity ids — כבר ASCII, מסונן הגנתית). **`file_name`** (העמודה הקריאה ב-`documents`, סכמה 031) שומר את השם המלא העברי כפי שהמשתמש רוצה לראות — **נפרד מ-`storage_path`**. **הורדה**: `signedUrlForPath(path, downloadName?)` מקבל כעת שם-הורדה ומעביר ל-`createSignedUrl(..., { download })` → ה-Content-Disposition משתמש ב-`file_name` העברי (לא ב-key ה-ASCII); ה-routes (`GET /api/documents`, `GET /api/documents/[id]`, ותשובת ה-POST) מעבירים את `file_name`. **תוספת — עריכת שם לפני העלאה**: ב-`UploadPanel` כל קובץ ברשימת ההעלאה קיבל שדה `Input` (h-10, פטרן §6) לעריכת `file_name` (ברירת מחדל = שם הקובץ המקורי, מוצג גם כ-"מקור:"); נשלח כ-`file_name` ב-multipart; משנה **רק** את שם-התצוגה, לא את ה-storage key. ה-POST קורא `file_name` אופציונלי (fallback ל-`file.name`, clamp ל-`MAX_FILE_NAME_LEN`). **`DocumentRenamePanel` (שינוי-שם אחרי העלאה) לא נגעתי בו** — כבר עקבי (PATCH של `file_name` בלבד, ללא נגיעה ב-storage). **אימות בפועל (אחרי deploy)**: (1) script מול ה-storage החי — key עברי → `Invalid key` (שחזור הבאג), key ASCII `uuid.ext` → OK, signed-url עם שם-הורדה עברי → OK; (2) **בדיקת route מלאה** (session זמני ל-super_admin) — יצירת תיקייה (201) → העלאת קובץ בשם "דוח שנתי 2026.pdf" לתוכה → **201 (לא 502)**, שורת ה-DB: `file_name='דוח שנתי 2026.pdf'` ‖ `storage_path='folder/<uuid>/<uuid>.pdf'`, `signed_url` קיים; כל ה-residue נוקה (object + שורות + session). **QA**: `tsc --noEmit` נקי, `npm run deploy` כיחידה אחת. לא בוצע force-push.

- 15/06/2026 — **מסמכים — תיקון באג הורדה (קובץ יורד בלי סיומת ולא נפתח) + download proxy.** **שורש הבאג**: אחרי תיקון ה-"Invalid key" (storage_path = UUID), ההורדה הסתמכה על signed-URL של Supabase עם param `download` — שלא הבטיח סיומת ולא קידד עברית נכון, כך שהקובץ ירד ללא `.pdf` ולא נפתח. **התיקון — download proxy ייעודי בשליטתנו** (`GET /api/documents/[id]/download`, `documents:view`): מושך את ה-bytes מ-Storage (helper חדש `downloadDocumentFile` — `StorageClient.download`, מחזיר `Blob`; `getStorage` זורק `supabase_storage_not_configured` → 503) ומחזיר `NextResponse(blob)` עם headers מפורשים: **`Content-Type` = `documents.mime_type`** (מסכמה 031; ברירת מחדל `application/octet-stream`) כדי שהדפדפן יפתח נכון, ו-**`Content-Disposition` עם קידוד RFC 5987**: `filename="<ascii fallback>"` (עברית→`_` ללקוחות ישנים) + `filename*=UTF-8''<pct-encoded>` (דפדפנים מודרניים מעדיפים) — כך שם הקובץ העברי לא נשבר. **הבטחת סיומת**: `downloadName(file_name, storage_path)` לוקח את הסיומת מה-storage key הקנוני (ASCII `<uuid>.pdf`, דרך `extOf` שיוצא כעת) ומצרף אותה ל-`file_name` אם חסרה — כך גם מי ששינה "דוח.pdf"→"דוח שנתי" יקבל `דוח שנתי.pdf`. **`file_name` (עברי, קריא) הוא מקור שם-ההורדה — לא ה-storage_path**. הלקוח (`DocumentsPageClient.openFile`) מוריד דרך anchor same-origin ל-route (cookie נשלח; `attachment` משאיר את הדף במקום) במקום `window.open(signed_url)`. storage_path כבר כלל סיומת מאז התיקון הקודם (`<uuid>.pdf`) — נשמר; אם חסרה, ה-proxy עדיין מבטיח אותה משם-הקובץ. **אימות בפועל (אחרי deploy, session זמני)**: העלאת PDF בשם "דוח שנתי 2026.pdf" → הורדה: 200, `Content-Type: application/pdf`, `filename*` מפוענח = "דוח שנתי 2026.pdf", body md5 תואם; וגם שם **בלי סיומת** ("דוח שנתי") → הורדה החזירה "דוח שנתי**.pdf**" (הסיומת שוחזרה), md5 תואם. residue נוקה. **QA**: `tsc --noEmit` נקי, `npm run deploy` כיחידה אחת. לא בוצע force-push.
- 15/06/2026 — **מודול תזכורות — השלמה מקצה-לקצה: קטגוריות (backend, Migration 032) + דף UI מלא `/user-reminders` + החזרת href בסיידבר.** audit מקדים (ללא נגיעה בקוד) אישר: ה-backend מ-030 קיים (`public.user_reminders`), **אין** טבלת קטגוריות ואין `category_id` — לכן הורחב additive. **החלטת סריאל — 032 ולא 033**: המפרט ביקש "033", אך המיגרציה האחרונה היא 031 (`031_documents.sql`) → הסריאל הבא בפועל הוא **032**. קונבנציית הפרויקט היא רצף נוקשה ללא חורים (024…031), ושימוש ב-033 היה משאיר 032 רפאים; בחרתי 032 כדי לכבד את "הסריאל הבא" ואת הרצף (אף מיגרציה לא רצה — שינוי שם טריוויאלי אם 033 באמת רצוי). **Migration 032** (`032_reminder_categories.sql`, additive+idempotent, **טרם הורצה על ה-DB** — להריץ ידנית, ראו פקודה למטה): טבלת `public.reminder_categories` — `id uuid`, `name text not null`, `color text not null` (hex שהמשתמש בוחר — **דאטה, לא טוקן עיצוב**; מאומת app-side ב-`COLOR_HEX_RE` שמשותף עם statuses), `created_by uuid not null FK→users` (NO ACTION — משתמשים מנוטרלים לא נמחקים), `display_order int default 0`, `is_archived boolean default false` (soft-delete עקבי עם tasks/user_reminders/documents), timestamps + trigger `touch_updated_at`. אינדקסים: `created_by`, `(display_order, name)`. + עמודה **`user_reminders.category_id uuid FK→reminder_categories ON DELETE SET NULL`** (ADD COLUMN IF NOT EXISTS) + אינדקס `user_reminders_category_idx`. **on-delete set null**: hard-delete של קטגוריה מרחף תזכורות ל-"ללא קטגוריה" ולא משמיד שורות (soft-delete הוא המסלול הרגיל). **החלטות מתועדות**: (1) **אין auto-seed** של קטגוריות ברירת-מחדל — `category_id` nullable, המודול עובד עם אפס קטגוריות, וה-UI מציע CTA "חדשה" בולט; הימנעות מחיווט מסוכן לזרימת יצירת-משתמש. (2) **קטגוריות גלויות ארגונית** — `created_by` הוא בעלות/audit ולא scope-נראוּת; כל משתמש יכול לתייק תחת כל קטגוריה (כמו ששיוך תזכורות חוצה משתמשים). (3) **אין עמודת description** ל-user_reminders (לא בסקופ) → "התיאור" בכרטיס הוא **שורת-meta** (מועד `font-num` + קטגוריה + משויך + סטטוס). (4) **פלטת צבעי קטגוריות vivid** ב-`@/lib/constants/userReminders` (`REMINDER_CATEGORY_COLORS`) + קלט hex חופשי — דאטה, **לא** טוקן → לא נוסף טוקן ל-DESIGN.md (תועד ב-§2 "Category colors מ-DB" כמקבילה ל-statuses.color). **API**: `src/app/api/reminder-categories/{route,[id]/route}.ts` — REST CRUD מלא (list עם **open_count** מסונן-לצופה=pending&non-archived&involving-actor, get, create, update, **soft-delete**), `runtime='nodejs'`, אימות `requirePermission('user_reminders', view/edit)` (**reuse של RBAC module קיים** — קטגוריות הן חלק מפיצ'ר התזכורות, לא module חדש; super_admin/admin עוקפים), UUID-guard, `writeAudit` עם **`entity_type='reminder_category'`** (caller נוסף לתשתית 029), fail-safe. ולידציה pure ב-`src/lib/validation/reminderCategories.ts`. **`/api/user-reminders` הורחב**: `category_id` ב-create/update (דרך `coerceUserReminderInput` + `WRITABLE_COLUMNS`), ופילטרים חדשים ל-list — `categoryId`, `createdBy`, **`involvingUser`** (`created_by = X OR assigned_to = X`); ה-SELECT מצרף `LEFT JOIN reminder_categories` ומחזיר `category_name`/`category_color` (מזין את פס-הצד בכרטיס). `writeAudit` של reminder נשמר fail-safe. **דף UI `/user-reminders`** (DESIGN.md = מקור-אמת; התמונה = מבנה בלבד, **לא הועתקו צבעים ממנה**): `src/app/(app)/user-reminders/{page.tsx,user-reminders-client.tsx}` + `src/components/user-reminders/{ReminderCard,ReminderFormPanel,CategoryFormPanel,helpers}.tsx` + `src/lib/constants/userReminders.ts`. page = Server Component (gate `user_reminders:view`→redirect, `canEdit`, `currentUserId`, assignees דרך `listAssignableUsers`). **מבנה**: כותרת "תזכורות"; **4 טאבים** (§16: active `bg-blue-600`+counter `bg-white/25`, idle `bg-white border`+counter `bg-blue-100/700`, `cursor-pointer`, h-11 touch-target) — "שלי"(`created_by=me`)/"משותף איתי"(`assigned_to=me && created_by≠me`)/"פתוחים"(`pending`)/"הושלמו"(`done`), **נגזרים client-side** מסט-ה-`involvingUser` (fetch בודד) עם מוני-טאבים; **שתי עמודות** (`flex-row-reverse` → קטגוריות מימין ב-RTL, תזכורות ראשונות/עליונות במובייל): **main** "כל התזכורות"+counter+`ReminderCard` (פס-צד בצבע הקטגוריה דרך inline-style, נקודת-סטטוס, badge "באיחור" כש-overdue, לחיצה→עריכה), **aside** "קטגוריות" עם נקודת-צבע+שם+open_count+כפתור "חדשה", לחיצה מסננת (toggle), עריכה/מחיקה ב-hover. **כל CREATE/EDIT דרך Sheet** (§12: `side="left" sm:w-[55vw]`, header gradient, `Section`+`PanelFooter`, dirty-guard עם AlertDialog ביציאה) — `ReminderFormPanel` (כותרת/תאריך+שעה/סטטוס/קטגוריה עם נקודת-צבע ב-SelectValue children-function/משויך) + `CategoryFormPanel` (שם + בורר-צבע inline: grid פלטה + קלט hex + preview). **Dialog רק לאישור מחיקה** (`AlertDialog` destructive, soft-delete). **toast** על כל mutation (§13), **skeleton** בטעינה (§17), **empty states**. **RBAC ב-UI**: `view`=קריאה בלבד (אין כפתורי יצירה/עריכה/מחיקה, אין CategoryFormPanel), `edit`=מלא. RTL מלא. **Sidebar**: הפריט "תזכורות" (אייקון `Bell`) **חובר ל-`href:'/user-reminders'`** (placeholder→פעיל עטוף ב-`Link`) — מבטל את ה-404 שבגללו ה-href הוסר ב-030. **QA**: `tsc --noEmit` נקי, **`npm run deploy` כיחידה אחת** (build→restart→אימות `active`+תהליך חדש אחרי BUILD_ID; `/user-reminders` נרשם). **אימות routes בפועל** (port 3003, PID חדש): `/user-reminders`→**307→/login** (לא 500), `/api/reminder-categories` GET/POST+`[id]`→**401** (רשום, לא 404), `/api/user-reminders`→401. **Migration 032 (וגם 029/030/031 אם עוד לא) — צעד ידני לפני שה-API יעבוד**; עד אז קריאות ה-API נכשלות (500) וה-UI מציג toast+empty — צפוי. לא בוצע force-push.
- 15/06/2026 — **באג פרודקשן קריטי: סנכרון חוב מבלינק לא איפס סעיפים ששולמו — תוקן (סנכרון מהריצה הנוכחית של בלינק בלבד + מנגנון בטיחות).** **תופעה מאומתת**: דירה 1035 שילמה מים חמים (אין לה חוב בבלינק) אך billing הציג `hot_water_debt=261`. **חקירה מקבילה (4 sub-agents על היפותזות שונות) + נתונים חיים → root cause יחיד**: הארכיטקטורה היא **בלינק → scrape של CRM (almog) → `debtor_records` → סנכרון billing → `debtors`**. דוח ה-udnp של בלינק כולל **רק דירות שחייבות כרגע** (אומת: 163 רשומות `imported_this_run=true`, אף אחת עם total 0). דירה ששילמה **נושרת מהדוח**; ה-CRM **לא מאפס** דירות שנעלמו — הוא שומר את הערך האחרון (`imported_this_run=false`, 1035: `special_debt=261` מ-06/11). הבאג ב-billing: `fetchCrmDebtorRows` משך את **כל 297 רשומות ה-CRM** (כולל 134 תקועות), וה-merge העתיק את ה-261 התקוע נאמנה — ה-overwrite הפר-דירתי מעולם לא קיבל הזדמנות לאפס כי השורה התקועה עדיין נכחה במשיכה. (ה-`zeroOutAptsNotInImport` הקיים מאפס רק דירות **נעדרות** מהמשיכה — וכל ה-297 נמשכו.) **ההחלטה (אישר המשתמש): בלינק הוא מקור-האמת; הסנכרון הוא דריסה אבסולוטית מהריצה הנוכחית בלבד.** התיקון (`src/lib/sync/bllinkPull.ts` בלבד): (1) **משיכת הריצה הנוכחית בלבד** — `&imported_this_run=eq.true` (הדגל מתאפס בכל ריצה; אומת ש-163 ה-true חולקים `last_import_at` יחיד). הדירות שנשרו מסוננות, וה-`zeroOutAptsNotInImport` הקיים מאפס אותן ב-billing (נעדר ⇒ שולם ⇒ 0). (2) **`total_debt` נבנה מחדש** = `management_fees + hot_water_debt` (לא הערך הגולמי). (3) **מנגנון בטיחות לפני כל כתיבה** (כי הפעולה מאפסת נעדרים → דריסה חלקית מסוכנת): מבטל את כל הסנכרון (`finishRunError`+throw→502, **לא כותב ולא מאפס כלום**) אם הריצה מתחת לרצפה אבסולוטית (`BLLINK_SYNC_MIN_ROWS=50`, env-override) או מתחת לאחוז מהחייבים הקיימים (`BLLINK_SYNC_MIN_FRACTION=0.4`), או אם המקור לא עקבי (`Σtotal ≠ Σרכיבים`, tol 1%). "עדיף לא לסנכרן מאשר למחוק חוב אמיתי". (4) **שדות ידניים נשמרים** — `legal_status` ונגזרותיו, notes, phone_tenant, מיילים, tenant_name, operator_id, phones_manual_override (ה-`updateDebtorMerge` SET-list לא שונה). **billing.special_debt** הוא עמודה legacy כל-אפס (Σ=0) שלא מוזנת מבלינק — הושארה 0, לא נגעתי (כדי לא לשנות התנהגות WhatsApp). **מסלול ה-Excel הידני (`runImport`) לא שונה** — רק מסלול ה-CRM-pull. **בדיקת בטיחות קריטית לפני ההרצה** (מול ה-DB החי): מתוך 120 דירות-נעדרות-עם-חוב, **0 בטיפול משפטי פעיל** — כל 8 דירות "בהליך משפטי" **נוכחות** בדוח → **שום חוב משפטי לא נמחק**; הנעדרים כולם "רגיל"/ללא-סטטוס שמחקו את החוב. **אימות בפועל** (deploy → סנכרון אמיתי דרך ה-route עם session זמני → DB): **1035: `hot_water_debt` 261→0, `total_debt` 261→0** (legal_status נשמר); דגימות נעדרות אופסו (1429 5606→0, 1027 1516→0, 613 2388→0, 913 1389→0); 3 דירות נוכחות תואמות את ה-CRM **לאגורה** (1620/1041/933); **סך billing: 282 חייבים/₪536,833.08 → 163 חייבים/₪459,811.50 = מקור בלינק בדיוק** (reconciliation delta **0.00**, `total_debt = mgmt+hot_water` בכל שורה — `total_minus_components=0`). הריצה **תיקנה את הדאטה בפרודקשן** (119 דירות אופסו, ₪77,021 חוב-רפאים נוקה). **QA**: `tsc --noEmit` נקי, **`npm run deploy` כיחידה אחת** (PID 3311682, build `J3UWkdlLgyA4AhN5snZWm`). נדחף ל-main (commit `5e74b5a`) **רק אחרי אישור מפורש** של המשתמש; לא force-push. **TODO עתידי** (ראו למטה): carve-out שלא מאפס דירות בסטטוס משפטי פעיל אם יעלמו מה-udnp + עמודת אודיט `cleared_at` (מיגרציה additive) — לא נדרש היום (אף דירה משפטית לא נעדרת).
- 15/06/2026 — **טבלת חייבים (dashboard) — שיפורי toolbar: חיפוש clearable, ייצוא Excel/PDF, הדפסה, וכותרות.** הדף server-paginated (URL params `tab/q/apt/sort/page` → `listDebtors` → `{rows,total,page,totalPages}`), לכן **היקף הייצוא/הדפסה = כל הסט המסונן הנוכחי** (טאב+חיפוש+מיון), לא העמוד הנראה. **Backend**: `listAllDebtorsForExport({tab,q,apt,sort})` ב-`src/lib/db/debtors.ts` (אותו `tabFilter`/`sortToSql`/q/apt כמו `listDebtors`, בלי pagination, cap 100k) + route `GET /api/debtors/export` (`requirePermission('dashboard','view')`, מחזיר `{rows}`). **(1) תיקון חפיפת חיפוש**: האייקון ב-`start-3` אך הריפוד היה `pe-9` (הצד הלא-נכון) → הטקסט נכנס מתחת לאייקון; תוקן ל-`ps-9` (+`pe-9` כשיש X). **(2) X לניקוי**: כפתור `X` ב-`end-2` שמופיע רק כשיש ערך, `text-slate-400 hover:text-slate-600`, מנקה את השדה ומאפס את הסינון. **(3) הדפסה**: כפתור `Printer` הופעל; `PrintableDebtorsTable` (portal ל-`document.body`, `#debtors-print-root`) + `app/styles/print.css` שמסתיר `body > *:not(#debtors-print-root)` ומציג רק את אזור ההדפסה (כותרת "טבלת חייבים" + "סה״כ N רשומות" + תאריך + טבלה נקייה, A4 landscape, `₪`+tabular-nums). **(4) ייצוא Excel** (`FileSpreadsheet`, ירוק) — **SheetJS (`xlsx`, dependency קיים)**, צד-לקוח; aoa עם כותרות עבריות §6 (ללא "פעולות"), כספים כ-numbers (לסיכום), טלפון string, גיליון "חייבים", `debtors_YYYY-MM-DD.xlsx`. **(5) ייצוא PDF** (`FileText`, אדום) — **`jspdf` + `jspdf-autotable` (תלויות חדשות) + Heebo מוטמע** (`src/lib/pdf-heebo.ts` — google/fonts `Heebo[wght]` instanced wght=400 + subset fonttools ל-Hebrew/Latin/₪, base64 ~18KB, רשום normal+bold). jsPDF ללא bidi → **היפוך תווי-עברית ידני** (מחרוזת שמכילה עברית) + **היפוך סדר העמודות** (מס׳ דירה מימין) + מספרים/תאריך LTR (התאריך ב-`text()` נפרד כדי לא להתהפך). `debtors_YYYY-MM-DD.pdf`. **אומת ויזואלית** (node→PDF→`pdftoppm` PNG→בדיקת עין): עברית תקינה — לא הפוכה, לא ריבועים, RTL נכון. כל ייצוא: `toast.success('הקובץ יוצא')`/`toast.error`. ה-import של jspdf/xlsx/pdf-heebo הוא **dynamic** (code-split, נטען רק בלחיצה). **(6) כותרות** §6: מס׳ דירה · שם בעל הדירה · טלפון · סה״כ חוב · דמי ניהול · מים חמים · מצב משפטי · פעולות (כבר תאמו). **(7) גופן כותרות** `TableHead` → **`text-[18px]`** (שאר §9 נשמר: font-semibold, text-ink-2, h-11, יישור); גוף השורות נשאר `text-sm`. **DESIGN.md עודכן**: §5c (toolbar export buttons — Printer/Excel/PDF + tones), §6 (clearable search input + הדגשת ps-9/pe-9). **QA**: `tsc --noEmit` נקי, `npm run deploy` כיחידה אחת. לא נדחף לגיט (לפי הנחיה — review בלבד).

### Module 2 — תקלות (Issues) — סיכום מבני (13/06/2026)

מודול שלם מקצה-לקצה: ניהול תקלות בבניין (טבלה + KPI + Side Panel) עם תמונות, תגובות, שיוך, ויצירת משימה מקושרת. **משתמש מחדש בתשתית ההתראות והתזכורות של Module 1** — לא שוכפל דבר.

**Migration 024** (`024_issues.sql`, additive+idempotent):
- `issues` — title/description/location_type(apartment/area/general)/location_text/priority(low/normal/high/urgent)/status(open/in_progress/resolved/closed)/assigned_to_user_id(FK users SET NULL)/**images text[]** (storage paths)/resolution_notes/resolved_at/is_archived/created_by+created_by_name snapshot (מדווח)/timestamps. אינדקסים על status, priority, assigned_to. status/priority/location_type כ-**text+CHECK** (קונבנציית הפרויקט).
- `issue_comments` — issue_id(FK cascade)/content/author snapshot/timestamps. אינדקס (issue_id, created_at).
- **`tasks.issue_id`** — עמודה **אדיטיבית** (ADD COLUMN IF NOT EXISTS) `uuid REFERENCES issues(id) ON DELETE SET NULL` + אינדקס. מקשרת משימה לתקלה שממנה נוצרה (אפס נגיעה ב-data קיים).

**Storage** — bucket חדש **`issue-attachments`** (פרטי, נוצר דרך Storage REST API עם service-role key). `src/lib/storage/issueStorage.ts` (דפוס `supplierStorage`): `uploadIssueImage` (key נגזר-שרת `<issueId>/<uuid>-<safeName>`, ה-`safeName` מסיר רכיב-נתיב + מקריס `..` → **אין path traversal**), `signedUrlForIssueImage` (signed URL לשעה, נוצר per-request — הסוד לעולם לא חוזר ללקוח, `images` מחזיק paths בלבד), `removeIssueImages`, `isPathUnderIssue` (guard ש-path שייך לתקלה). ולידציית קבצים בשרת: JPG/PNG/WebP בלבד, ≤5MB, ≤6 תמונות.

**DB layer** `src/lib/db/issues.ts` (דפוס `tasks.ts`, parameterized מלא): `listIssues`(filters status/priority/assignedTo/search ILIKE/sort) עם JOIN ל-assignee + comment_count + **linked_task_id** (lateral), `getIssueById`, `createIssue`(resolved_at נגזר מ-status), `updateIssue`(resolved_at: coalesce-on-enter / null-on-leave), `deleteIssue`, `getIssueAssigneeStatus`, `getIssueImages`/`setIssueImages`/**`appendIssueImage`** (append אטומי ב-SQL עם cap בתוך ה-UPDATE — אין race), comments, `getIssueKpis`(open/urgent/resolvedThisMonth), **`createTaskFromIssue`** (טרנזקציוני — guard משימה-אחת-לתקלה + INSERT ל-tasks עם issue_id).

**API** תחת `/api/issues` (gate `requirePermission('issues', …)`): `GET`(view) + `POST`(**view** — viewer רשאי לדווח, לפי האיפיון); `GET/[id]`(view, מחזיר signed URLs) + `PATCH`(edit, חוסם resolve ללא resolution_notes מול הרשומה החיה) + `DELETE`(edit, מנקה תמונות); `GET/POST /[id]/comments`(**view** — viewer רשאי להגיב); `POST/DELETE /[id]/images`(edit, multipart, ולידציה בשרת); `POST /[id]/create-task`(edit, 409 אם כבר מקושר); `GET /assignees`(view). כל route עם **UUID guard** על `[id]` → 404 (מונע 500 מ-22P02). התראות: שיוך/שינוי-סטטוס יוצרים notification ל-assignee (≠עורך) דרך `notifyIssue` עם dedupe_key (`issue_assigned:<id>:<uid>` / `issue_status:<id>:<uid>:<status>`); create-task שולח `notifyTask` ל-assignee.

**UI** `(app)/issues/`: עמוד עם KPI bar (פתוחות/דחופות/נפתרו החודש), טבלה (§9) עם חיווי תמונות/תגובות/קישור-משימה + סינון status/priority/assignee + חיפוש + מיון, ו-`IssueFormPanel` (Sheet `side="left" sm:w-[55vw]`, **לא Dialog**, dirty-tracking + AlertDialog confirm-exit) עם סקשנים: פרטי תקלה (כולל resolution_notes כשנפתרת), מיקום, שיוך, **תמונות** (העלאה + thumbnails + מחיקה + lightbox `ImageLightbox`), משימה מקושרת (כפתור "צור משימה מתקלה" כשאין מקושרת / קישור למשימה כשיש), תגובות. Sidebar: "תקלות" קיבל `href: '/issues'` (היה placeholder). RBAC `issues` כבר היה ב-`MODULES`+DEFAULT_MANAGER/VIEWER — אפס שינוי הרשאות. מנוע התזכורות (`reminders/engine.ts`) קיבל ענף `entity_type='issue'` (תוכן עשיר) — אדיטיבי.

**אבטחה** — עבר security review ייעודי: אפס CRITICAL. תוקנו: UUID guards בכל route (404 נקי במקום 500 מ-22P02), והפיכת append-תמונה לאטומי (cap בתוך SQL). אומת: כל endpoint מגודר, viewer לא יכול לשנות תקלה קיימת, אין path traversal (key נגזר-שרת + `safeName` + `isPathUnderIssue`), SQL parameterized מלא, service-role key לא חוזר ללקוח, resolve-ללא-הערות חסום בשני המסלולים.

### Module 1 — משימות + התראות + מנוע תזכורות — סיכום מבני (13/06/2026)

מודול שלם מקצה-לקצה: ניהול משימות (קנבן + טבלה) + תשתית התראות per-user + מנוע תזכורות גנרי שירות בעתיד גם את לוח השנה. הותקן בפרודקשן דרך `npm run deploy`; smoke E2E **14/14 עברו** (יצירה→notification→reorder→comment→reminder→cron→sent→idempotent).

**Migration 023** (`023_tasks_notifications_reminders.sql`, additive+idempotent, 4 טבלאות חדשות, אפס נגיעה בקיים):
- `tasks` — title/description/status(open/in_progress/done/cancelled)/priority(low/normal/high/urgent)/due_date/due_time/assigned_to_user_id(FK users SET NULL)/debtor_id(FK SET NULL)/apartment_number/sort_order(קנבן)/is_archived/created_by+created_by_name snapshot/timestamps. אינדקסים על status, assigned_to, due_date, (status,sort_order). status+priority כ-**text+CHECK** (קונבנציית הפרויקט — לא pg ENUM).
- `task_comments` — task_id FK **cascade**, content, author snapshot, timestamps.
- `notifications` — user_id FK cascade, type/title/message/is_read/source_module/source_entity_type/source_entity_id/action_url/priority/dedupe_key, אינדקס (user_id,is_read) + (user_id,created_at desc) + **partial unique** על dedupe_key WHERE not null.
- `reminders` (גנרי) — entity_type/entity_id (לא FK — מפתח גנרי)/user_id FK cascade/remind_at timestamptz/channel(in_app/email/both)/sent_at, **partial index** על remind_at WHERE sent_at IS NULL.
- כל הטבלאות עם trigger `touch_updated_at` (מ-migration 001).

**RBAC** — המודולים `tasks` ו-`reminders` **כבר היו** ב-`MODULES` ובברירות-המחדל (manager edit, viewer view) — **לא נדרש שינוי מטריצה**. Notifications הן per-user (ללא module gate — `requireActor` בלבד). **החלטה**: comment POST מגודר ב-`tasks:view` (לא edit) — לפי המפרט "צופה יכול ליצור תגובות"; כל שאר ה-mutations ב-`tasks:edit`. תיעוד החריגה במפורש בקוד.

**DB layer**: `src/lib/db/tasks.ts` (listTasks עם JOIN assignee + comment_count subquery, getById, createTask [sort_order=max+1 פר-status, created_by server-side], updateTask [dynamic SET מ-allowlist], reorderTasks [batch בטרנזקציה], comments, getTaskKpis [open/overdue/done_this_month ב-filter aggregate יחיד]), `notifications.ts` (createNotification עם `ON CONFLICT (dedupe_key) DO NOTHING`, מקבל client אופציונלי לטרנזקציה; list/countUnread/markRead/markAllRead — **כולם scoped ל-user_id**), `reminders.ts` (create/listForEntity/delete/deleteForEntity/listDueReminders/markSent). כל השאילתות פרמטריות מלאות, LIMIT clamped int.

**Validation** `src/lib/validation/tasks.ts` (partial-by-design; coerceTaskInput [whitelist בלבד — אין mass-assignment של created_by/sort_order/is_archived; caps: title 300, description 20k, apt 50] + coerceReminders [cap 50, channel enum, Date.parse]).

**מנוע תזכורות** `src/lib/reminders/engine.ts` — `runReminders()` שולף due+unsent, פר-תזכורת: notification (in_app/both) + email (email/both, best-effort outside tx) + `markReminderSent` בטרנזקציה עם ה-notification, dedupe_key=`reminder:{id}` → ריצה כפולה בטוחה (idempotent — אומת: ריצה שנייה due=0). entity_type='task' מקבל תוכן עשיר, סוגים אחרים → notification גנרי.

**Email**: template חדש `src/templates/email/task-notification.ts` (inline-CSS/dir=rtl/Heebo/table-layout/plaintext לפי DESIGN.md §24), רשום ב-`email-templates.ts` (`task-notification`), `sendTaskNotificationEmail` ב-`services/email.ts`. `services/notifications.ts` → `notifyTask()` מאחד notification+email (best-effort, מחפש email דרך `findUserById`, swallow על כשל).

**API endpoints** (7 חדשים): `GET/POST /api/tasks` (POST: יצירה + reminders + assignment notification עם dedupe), `GET/PATCH/DELETE /api/tasks/[id]` (PATCH: עדכון + assignment-change notification + reminders replacement; DELETE: מנקה גם reminders [entity_id לא FK] + 204), `GET/POST /api/tasks/[id]/comments` (POST ב-`tasks:view`), `PATCH /api/tasks/reorder` (batch קנבן, cap 500, UUID+enum+int validation), `GET /api/tasks/assignees` (משתמשים פעילים לבורר, `tasks:view`), `GET/PATCH /api/notifications` (של המשתמש המחובר בלבד; PATCH single-id/mark-all, scoped ל-actor), `POST /api/cron/reminders` (x-cron-secret, **fail-closed** אם אין secret→503, **constant-time** `timingSafeEqual`→401 על שגוי/חסר).

**UI** (כל העיצוב לפי DESIGN.md, לא הומצא סטייל): דף `/tasks` (Server → `TasksPageClient`). **KPI bar** (3 כרטיסים reuse של dashboard `KpiCard`: פתוחות/באיחור/הושלמו החודש). **מתג תצוגה** קנבן/טבלה. **קנבן** (`tasks-kanban.tsx`) 4 עמודות לפי status, **drag-and-drop native** (HTML5 — בחירה מודעת לפשטות, אפס תלות חדשה) עם optimistic move→`PATCH /reorder`→rollback בכשל. **טבלה** (`tasks-table.tsx`) §9 עם sortable headers (created/due/priority) + badges status/priority §10. **סינון** סטטוס/דחיפות/משויך + חיפוש. **TaskFormPanel** (`task-form-panel.tsx`) — **Side Panel** (Sheet side=left 55vw §12, **לא Dialog**) עם `Section`+`PanelFooter` המשותפים, dirty-tracking + AlertDialog confirm-exit: פרטים (כותרת/תיאור/status/priority/due date+time), שיוך (Select משתמשים מציג full_name + מספר דירה אופציונלי), תזכורות (רשימת date+time+channel עם add/remove, replacement semantics), תגובות (edit mode, Ctrl+Enter, send-circle בתוך Textarea §5). **NotificationBell** (`app-shell/NotificationBell.tsx`) בכותרת — Popover, **polling 60ש'**, מונה לא-נקראו (badge אדום §10), dropdown רשימה, לחיצה→mark-read אופטימי + ניווט ל-action_url, "סמן הכל כנקרא". החליף את הפעמון הסטטי בה-Header. Sidebar: פריט "משימות" קיבל `href: '/tasks'`.

**תזמון** — `BILLING_CRON_SECRET` (32-byte hex) נוסף ל-`/etc/billing/billing.env`. systemd: `billing-reminders.service` (oneshot, User=ubuntu, **EnvironmentFile=/etc/billing/billing.env** — systemd מזריק את הסוד; הסקריפט לא קורא את הקובץ הroot-only ישירות) + `billing-reminders.timer` (OnBootSec=2min, OnUnitActiveSec=**5min**, Persistent). `scripts/run-reminders.sh` קורא ל-curl עם הסוד דרך stdin header (לא ב-`ps`). `enable --now` — enabled+active, ריצה ידנית הצליחה (Result=success). **גילוי**: הניסיון הראשון נכשל כי הסקריפט ניסה `grep` את ה-env הroot-only כ-ubuntu (Permission denied) — תוקן למסלול EnvironmentFile.

**אבטחה** — עבר Security review ייעודי (9 קטגוריות): **אפס MUST-FIX**. אומת: auth בכל route, RBAC נכון, **בידוד notifications** (user רואה/מסמן רק את שלו, גם עם id מנוחש), פרמטריזציה מלאה (column names מ-allowlist קבוע, לא user input; ORDER BY מ-switch; LIMIT clamped), cron fail-closed+constant-time, אין דליפת סוד ללוג/קליינט, אין mass-assignment. יושמו 3 hardenings מומלצים (caps ל-reminders array/description/apt, truncate ל-limit).

**קבצים חדשים**: `supabase/migrations/023_tasks_notifications_reminders.sql`, `src/lib/types/tasks.ts`, `src/lib/db/{tasks,notifications,reminders}.ts`, `src/lib/validation/tasks.ts`, `src/lib/reminders/engine.ts`, `src/lib/constants/tasks.ts`, `src/services/notifications.ts`, `src/templates/email/task-notification.ts`, `src/app/api/tasks/{route,[id]/route,[id]/comments/route,reorder/route,assignees/route}.ts`, `src/app/api/notifications/route.ts`, `src/app/api/cron/reminders/route.ts`, `src/app/(app)/tasks/{page,tasks-page-client}.tsx`, `src/components/tasks/{task-form-panel,tasks-kanban,tasks-table}.tsx`, `src/components/app-shell/NotificationBell.tsx`, `scripts/run-reminders.sh`, `scripts/test-tasks-api.sh`, `/etc/systemd/system/billing-reminders.{service,timer}` (מחוץ ל-repo). **קבצים שעודכנו**: `src/lib/db/users.ts` (+listAssignableUsers), `src/lib/email-templates.ts` (+task-notification), `src/services/email.ts` (+sendTaskNotificationEmail), `src/components/app-shell/{Header,Sidebar}.tsx`.

### Slice 3 + 3.5 — סיכום מבני (25/04/2026)

**DB schema**:
- Migration 003 (`003_debtor_panel.sql`): טבלאות `statuses` (id, name unique, description, color hex, is_default, is_active, sort_order, notification_emails text[]), `legal_status_history` (audit: old/new status snapshot, changed_by, source MANUAL/IMPORT/AUTO_DEFAULT/SYSTEM_FIX), `comments` (debtor_id FK cascade, content, author snapshot). הרחבת `debtors` ב-11 שדות חדשים (legal_status_id FK, legal_status_source, legal_status_lock, legal_status_updated_at/by/by_name, notes, next_action_date, next_action_description, last_contact_date, phones_manual_override). drop של `legal_status` text הישן.
- Migration 004 (`004_completed_actions.sql`): `completed_actions` (debtor_id FK cascade, apartment_number, description NOT NULL, due_date, completed_at, completed_by, completed_by_name).
- Seed 7 סטטוסים: רגיל (default `#f3f4f6`), מכתב התראה (`#fde68a`), לטיפול משפטי (`#fecaca`), לטיפול רונן (`#bfdbfe`), במעקב נעמה (`#ddd6fe`), בניהול פתאל (`#bfdbfe`), בהליך משפטי (`#fca5a5`).

**API endpoints — Slice 3 (5)**:
- `GET /api/statuses` — כל הסטטוסים הפעילים, ממוין sort_order
- `GET /api/debtors/[id]` — tenant detail + 3 הערות אחרונות
- `PATCH /api/debtors/[id]` — admin: phones / notes / next_action / last_contact (validation rich לטלפון)
- `PUT /api/debtors/[id]/legal-status` — admin: בטרנזקציה — update + audit row + email notification (stub) ל-notification_emails של הסטטוס
- `GET /api/debtors/[id]/comments` + `POST` — admin בלבד ל-POST

**API endpoints — Slice 3.5 (2)**:
- `POST /api/debtors/[id]/complete-action` — admin: בטרנזקציה — INSERT ל-completed_actions עם snapshot (description / due_date / author) + clear next_action_* + set last_contact_date = current_date. Fallback `(ללא תיאור)` כש-description=null
- `GET /api/debtors/[id]/completed-actions` — היסטוריה מלאה ממוינת desc

**Components ב-`src/components/tenant-detail-panel/`**:
TenantDetailPanel (orchestrator), Section (card עם icon-chip), StatusBadge, KpiCard (rose/blue/violet), MainDetailsCard (Home), AdditionalInfoCard (FileText), QuickActionsCard (3 buttons grid: WhatsApp/Email/SMS — disabled עם Lock), DebtsCard (Wallet, full-width עם 3 KPIs), NextActionCard (Calendar, draft state), LegalManagementCard (Scale, Select עם dropdown צבעוני, optimistic), CompletedActionsCard (CheckCircle2 emerald, history list), CommentsSection (avatar + Send circle in textarea, Ctrl+Enter), PanelTabs (פרטי דייר active, מסמכים+היסטוריה disabled), PanelFooter (סגור / Print / FileDown / היסטוריה / שמור שינויים), EditPhoneDialog (rich validation).

**Sheet patches ב-`src/components/ui/sheet.tsx`** (לפי skill `side-panel`):
- Overlay: `bg-slate-950/40` (≈0.65 contrast feel) + duration-[400ms] ease-out
- Content: w-[55vw] sm:min-w-[720px] (lift width default), shadow-2xl shadow-slate-900/30, transition duration-[1200ms] ease-[cubic-bezier(0.16,0.84,0.26,1)]
- Translate from off-screen-left (-translate-x-full) במקום ה-2.5rem הסוביטיים שלshadcn
- Border-r physical (לא border-e שמתהפך ב-RTL)
- showCloseButton=false → custom X button בHeader

**UX patterns מרכזיים**:
- **Optimistic UI**: שינוי סטטוס/טלפון → UI מתעדכן מיד → API ברקע → rollback אם נכשל. כל success מלווה ב-toast + `router.refresh()` שמרענן את הטבלה במקביל.
- **Dirty tracking**: `hasMutated` (sticky flag — auto-saved status/phones/comment) + `nextActionDirty` (draft compare). `isDirty = hasMutated || nextActionDirty`. הכפתור "שמור שינויים" enabled כש-isDirty, לחיצה שומרת draft (אם יש) וסוגרת את הפאנל.
- **AlertDialog לאישור יציאה**: כל 3 דרכי הסגירה (X / overlay / ESC) עוברות ב-`requestClose()`. אם isDirty → AlertDialog destructive ("האם לצאת ללא שמירה?" / "צא ללא שמירה" red). אם לא → סגירה מיידית.
- **VIEWER mode**: `!isAdmin` → Alert עליון amber + כל השדות disabled + tooltip "אין הרשאה" על "שמור שינויים".

**החלטות לוגיות**:
- 3 הסטטוסים הפנימיים (במעקב נעמה / בניהול פתאל / לטיפול רונן) משוייכים לטאב "חייבים" (יחד עם רגיל + null) — לא יוצרים יתומים.
- טאב "פעולות" מוצג רק כש-`next_action_date IS NOT NULL`, ממוין `asc nulls last` כפוי (לא תלוי URL sort param).
- `completed_actions` שומר היסטוריה (אופציה ב' של schema אסרטיבי description NOT NULL + fallback ב-API).
- Status pill: ברירת מחדל ("רגיל") מוצגת תמיד בכותרת הפאנל באפור בהיר; בטבלה מוצגת כ-"—" כדי לא להבליט.
- `__clear__`: BE מקבל `status_id=null`, אבל UI לא חושף "הסר סטטוס".

**עיצוב**:
- `DESIGN.md` בשורש הפרויקט — מאסטר design system, 23 סקשנים שמכסים: צבעים, טיפוגרפיה, spacing, buttons (כולל Section 5b "Sync & Import"), form fields, validation (Section 7 — phone), cards, tables, badges, KPI, modals/sheets/dialogs, toasts, sidebar, header, tabs, empty/loading, auth, wizards, RTL, animation, disabled/loading, misc.
- Skill `~/.claude/skills/design-system/SKILL.md` — אוטו-טריגר על כל אלמנט UI חדש.
- Skill `~/.claude/skills/side-panel/SKILL.md` — מורחב עם dirty tracking, footer, VIEWER, optimistic, confirm-exit (לפי הפאטרן הסופי של TenantDetailPanel).

**קבצים חדשים ב-Slice 3 + 3.5**:
```
DESIGN.md
supabase/migrations/003_debtor_panel.sql
supabase/migrations/004_completed_actions.sql
src/types/tenant.ts
src/lib/db/statuses.ts
src/lib/db/legalStatusHistory.ts
src/lib/db/comments.ts
src/lib/db/completedActions.ts
src/lib/phone.ts
src/lib/validation.ts                 (חדש — נדרש גם ל-phone)
src/app/api/statuses/route.ts
src/app/api/debtors/[id]/route.ts
src/app/api/debtors/[id]/legal-status/route.ts
src/app/api/debtors/[id]/comments/route.ts
src/app/api/debtors/[id]/complete-action/route.ts
src/app/api/debtors/[id]/completed-actions/route.ts
src/components/tenant-detail-panel/  (14 קבצים)
src/components/ui/sheet.tsx           (patched)
src/components/ui/alert-dialog.tsx    (התקנה חדשה)
src/components/ui/textarea.tsx        (התקנה חדשה)
~/.claude/skills/design-system/SKILL.md
~/.claude/skills/side-panel/SKILL.md
```

**קבצים שעודכנו**:
```
src/lib/db.ts                         (+withTransaction)
src/lib/db/debtors.ts                 (rewrite: JOIN עם statuses, sort keys, getDebtorById, updateDebtorFields, updateDebtorLegalStatus, tab filters refined)
src/services/email.ts                 (+sendStatusChangeNotification)
src/app/(app)/dashboard/page.tsx      (sort param expanded, currentTab passed)
src/app/(app)/dashboard/components/DebtorsTable.tsx  (rewrite: sortable headers, status pill from DB hex, ₪-prefix LTR cells, conditional actions-tab columns + CheckCircle2)
src/app/(app)/dashboard/components/DebtorsTabs.tsx   (cursor-pointer, actions tab enabled)
src/app/(app)/dashboard/components/DebtorsToolbar.tsx (text-xl title, slate-400 subtitle)
src/app/(app)/dashboard/components/LastImportIndicator.tsx (Sync/Import buttons spec, white/yellow/red severity)
src/app/(app)/import/components/Step3PreviewProgress.tsx (legal_status_id in protected fields)
src/app/(app)/import/components/ReplaceConfirmDialog.tsx (AlertTriangle icon)
src/lib/import/runner.ts              (comment update)
src/components/auth/ResetPasswordForm.tsx (Section 7 validation pattern)
src/app/api/debtors/route.ts          (sort keys validation)
PROJECT_CONTEXT.md                    (decisions log + DESIGN.md instruction)
```

TODO:
- **[טופל 26/07 — נותר רק CHECK] `source_entity_type` מנורמל: `chat_message` 969 · `issue` 159 · `task` 73 · `conversation` 7 · NULL 1 (1209 סה"כ), אפס שורות שאינן `lower_snake_case`.** שלושה שלבים: (1) 068+069 מחקו את 146 ההתראות היתומות; (2) `09391c0` יישר את שלושת המפיקים שכתבו CamelCase — `reminders/engine.ts:64` `'Task'`→`'task'`, `api/issues/route.ts:45` `'Issue'`→`'issue'`, `whatsapp-inbound.ts:88` `'ChatMessage'`→`'chat_message'` (המוסכמה מתועדת על `CreateNotificationInput`); (3) מיגרציה 070 נרמלה 1126 שורות קיימות. **`'ChatMessage'` מופה ל-`'chat_message'` ולא ב-`lower()` עיוור** — `'chatmessage'` היה יוצר פיצול חדש של 969 שורות מול מה שהמפיק כותב היום. **מה שנותר: אין CHECK constraint** — האינווריאנטה נכונה אך לא נאכפת, ולכן מפיק חדש עדיין יכול לשבור אותה. הניסוח חייב להתיר NULL (`source_entity_type is null or source_entity_type = lower(source_entity_type)`) כי `system_announcement` הוא סוג לגיטימי חסר-ישות. עד שה-CHECK קיים, ההשוואות ה-case-insensitive ב-`src/lib/db/notifications.ts` נשארות (מיותרות אך לא שגויות), ו**כל מיגרציה/שאילתה שנוגעת בעמודה חייבת `lower(...)`**. אזהרה נוספה ל-`docs/archive/BASE44_REFERENCE.md` (מקור מוסכמת ה-CamelCase). התיעוד המלא של הבאג המקורי נשמר בשורה הבאה.
- **[היסטורי — הבאג המקורי] `source_entity_type` אינו מנורמל, וכבר גרם ל-140 התראות יתומות בפרודקשן.** מפיקי ההתראות כותבים את אותו סוג ישות בשתי צורות — `'Task'` (177 שורות) לצד `'task'` (22), `'Issue'` (117) לצד `'issue'` (48), וכן `'ChatMessage'`/`'conversation'`. נתיב הקריאה ב-`src/lib/db/notifications.ts` **כבר** מפצה על כך (`where lower(source_entity_type) = lower($1)`, עם הערה מפורשת שהמפיקים לא עקביים), אבל **מיגרציה 067 לא**: הניקוי שם הוא `delete from public.notifications where source_entity_type = 'task'` — case-sensitive — ולכן פסח על כל שורות `'Task'`. **אומת 26/07 בהצלבה מול הגיבוי**: 140 התראות `task_due_soon` (21/06–25/07) מצביעות על 37 משימות מחוקות, ו-**37 מתוך 37** נמצאות ב-229 המופעים שנמחקו לפי `/var/backups/billing/067/067-instances-backup.jsonl` — אפס יתומות ממקור אחר. התוצאה בפועל: הפעמון מקשר למשימות שאינן קיימות. נדרש היה: (1) ניקוי חד-פעמי של 140 היתומות — ✅ מיגרציה 068; (2) נרמול הערכים הקיימים + כתיבה אחידה בכל המפיקים — ✅ `09391c0` + מיגרציה 070; (3) `lower(...)` בכל מיגרציה/שאילתה שנוגעת בעמודה — ✅ נשמר, ועדיין מחייב עד שיתווסף CHECK. **המספרים בשורה הזו (`'Task'` 177 / `'task'` 22) הם מלפני 068 ואינם משקפים את המצב — ראה השורה שמעליה.**
- DESIGN.md compliance audit לאזורים שלא נסקרו השבוע: `auth/*` (LoginForm/Forgot/Reset/AuthLayout/FeaturesCard), `import/Step1-3*`. (✅ כוסו: `app-shell/Sidebar.tsx`+`Header.tsx`+`UserMenu.tsx`, `dashboard/KpiGrid.tsx`+`KpiCard.tsx`+`LastImportIndicator.tsx` — נסקרו ועוצבו מחדש בעת החלת מערכת העיצוב "ניהול אלמוג"; §5b עודכן לדפוס שני-הטיימסטמפים.) צריך לעבור על הנותרים ולוודא שאין סטיות נסתרות (גובה כפתור, RTL flips, toast wiring על mutations באוט-pages, וכו')
- `npm run build` מכיל postbuild שמעתיק ל-`.next/standalone/`, אבל לא מריץ `systemctl restart billing`. התוצאה: `.next/` נדרס בזמן שה-process ישן עדיין רץ — Server Actions IDs נשברים ו/או הסכמה מתנגשת עם DB migration שעוד לא רצה. לבחור מסלול: (א) postbuild יריץ אוטומטית `sudo systemctl restart billing` (דורש sudoers rule ל-user של ה-build), או (ב) script `deploy.sh` שכופה build → migrate (אם יש) → restart בסדר הנכון. התיעוד ב-PROJECT_CONTEXT צריך לאסור `npm run build` בודד בסביבה חיה
- Rate-limiting ל-/api/auth/login ו-/api/auth/forgot-password לפני production
- Rate-limiting ל-`/api/sync/bllink` (אדמין בלבד, אבל שכבת הגנה נוספת נגד abuse / לחיצות כפולות)
- [סנכרון בלינק — הגנה עתידית] ה-zero-out של הסנכרון (`zeroOutAptsNotInImport`) מאפס סכומים של **כל** דירה שנעדרת מהריצה הנוכחית. היום בטוח (אומת: כל הדירות בטיפול משפטי פעיל נוכחות ב-udnp). אם **בעתיד** דירה משפטית תיעלם מהדוח — הסכום שלה יתאפס (ה-legal_status יישמר). מומלץ: (א) carve-out שלא מאפס דירות בסטטוס משפטי פעיל (מכתב התראה/לטיפול משפטי/בהליך משפטי), (ב) עמודת אודיט `cleared_at timestamptz` (מיגרציה additive ממוספרת) שתסמן מתי סעיף אופס ע"י סנכרון — לעקיבוּת. לא דחוף.
- [almog — לא billing] fail-closed על `CRON_SECRET`: אם משתנה הסביבה לא מוגדר, endpoint `syncBllinkDebt` חייב להחזיר 500, לא להישאר פתוח
- Google OAuth בפאזה ב'
- Next.js 16 הציג deprecation: middleware → proxy. לעבור בעתיד הקרוב כש-API מתייצב.
- שקילת hash של reset tokens במסד (הצפנה נוספת)

## הוראות לקלוד
- ענה תמיד בעברית (למעט קוד, פקודות, ושמות טכניים)
- אל תתחיל לכתוב קוד בלי תוכנית שאושרה
- כל אלמנט UI חדש בפרויקט (כפתורים, dialogs, alerts, toasts, form validation, cards, tables, sidebar items, badges, וכו') חייב להתאים ל-DESIGN.md בשורש הפרויקט. קרא את הסקשן הרלוונטי לפני יצירת קומפוננטה ויזואלית חדשה. אם דפוס חסר — לא להמציא וריאציה; לעדכן את DESIGN.md כחלק מהשינוי.
- אל תיצור קבצי MD חדשים עם שמות אקראיים — תעדכן את 
  PROJECT_CONTEXT.md עם כל החלטה חדשה תחת Decisions Log
- docs/archive/BASE44_REFERENCE.md הוא חומר עזר בלבד — לא תבנה לפיו 
  בלי אישור מפורש
- לפני מסך חדש — המשתמש יעביר עיצוב מ-Claude Design. 
  חכה לעיצוב לפני שתתחיל לתכנן UI.
- אסור לגשת ל-DBs, קבצים, קונפיגורציה, או מטה-דאטה של 
  פרויקטים אחרים על השרת (proj_pms, proj_invoiceflow, 
  proj_mail_system, proj_temp, או כל פרויקט אחר שאינו 
  proj_billing). הכלל חל על כל הכלים — bash, docker, 
  psql, file read. אם אתה חושב שמידע כזה הכרחי — שאל 
  אישור מפורש לפני שאתה ניגש אליו.