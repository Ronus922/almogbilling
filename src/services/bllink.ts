// Stub. TODO: replace with real Bllink integration in phase B.

export async function importBllinkReport(_credentials: { username: string; password: string }) {
  console.log('[bllink:stub] importBllinkReport called');
  return { ok: true, mocked: true, rowsImported: 0 };
}
